import React from "react";
import { View, Text } from "react-native";

export default function PdfReaderApp() {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text>Web version not supported yet</Text>
    </View>
  );
}
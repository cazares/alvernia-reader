# Codex Summary (Apr 29–30, 2026)

Repo: `/Users/cazares/src/alvernia-reader`  
Branch: `main` (everything below is merged/pushed)  
Timezone: America/Chicago

## What changed (high level)

### Nearby director/follower sync reliability + UX
- Removed the scary follower status red `✕` and replaced the “not connected” indicator with a slow blinking orange/yellow dot (same refresh button cluster).
- Added a “foreground nudge” so when the app returns to the foreground it proactively re-syncs instead of waiting for a page-change.
- Added a **forced current-snapshot request**: when a follower returns to foreground it immediately sends a `"hello"` to the director so the director re-sends the current page snapshot even if nothing changed.

### Test suite hardening (contract/e2e)
- Added lots of **contract-style `node:test` coverage** for nearby sync, including:
  - happy-path expectations (startup, reconnect, indicator UI states)
  - edge cases (protocol mismatch ignored, hello throttling, self-directed gating, retry scheduling, bridge exposure)
- Updated existing tests to match the new asset layout (standard assets under `assets/standard/*`), and removed stale assumptions about removed directories.
- Kept `version.json` / `app.json` / native build metadata drift checks passing by updating expectations where the repo structure changed.

### Repo / assets layout updates (as observed while testing)
- `assets/offline-web/*` and `assets/offline-books/*` are no longer present on `main`; “standard” JSON metadata now lives under `assets/standard/*`.

## Key commits (in order, newest first)
- `0bfb5b68` Merge PR #158: Remove song grid thumbnails entirely (build 307)
- `39490628` Add edge-case sync contracts and align e2e paths
- `de2ac7f6` Add happy-path nearby sync contract tests
- `330ab523` Force snapshot request on follower foreground
- `9ccd772b` Add more nearby sync UX contract tests
- `70e71da5` Fix e2e offline-web asset expectations
- `0fa03f91` Remove red X indicator; update sync contract tests

## Files touched (most relevant)
- `PdfReaderApp.tsx` (foreground nudge + follower UX indicator changes + snapshot request hook)
- `src/nearbyDirectorSync.js` / `src/nearbyDirectorSync.d.ts` (added `requestCurrentSnapshot`)
- `ios/SignoVivo/DirectorSyncModule.swift` + `ios/SignoVivo/DirectorSyncModuleBridge.m` (native implementation + bridge export for snapshot request)
- `e2e/nearby-sync-contract.test.mjs` (bulk of new happy-path + edge-case tests)
- `e2e/native-entrypoint.test.mjs`, `e2e/offline-books-integrity.test.mjs`, `e2e/repo-minimal-footprint.test.mjs` (aligned expectations to current asset layout)

## How to verify quickly
- Run tests:
  - `node --test e2e/*.test.mjs`
- Manual “truth test” (2 devices):
  1. Director + follower connected, both on page N
  2. Director backgrounds/locks
  3. Follower moves ±j pages
  4. Director foregrounds
  5. Expect follower to snap back to director page within ~2 seconds

## Notes
- I created an hourly reminder automation earlier to remember the “8-step scenario” and merge tasks; at this point the relevant changes are already merged to `main`.


# SignoVivo — Cross-Subsystem Contracts (99)

_Every shared message shape, URL, header, storage key, injected-script global, and build-time constant that couples web ↔ worker ↔ native ↔ build. Use when changing anything that crosses a boundary. Generated 2026-07-01._

- Build-time token replacements the build script MUST perform on app.js: __CACHE_VERSION__ (app.js:264, drives cache names + OFFLINE_READY_KEY + fleet cacheVersion), __BUILD_NUMBER__ (267, version label/badge; guarded by BUILD_NUMBER[0] !== '_' check at 3823), __RELAY_BASE__ (3074; unreplaced token falls back to https://signovivo-sync.4j4982y8jp.workers.dev at 3076).
- Inline script blobs the build injects into index.html: <script id="books-data"> = {default, books:{<id>:{label,totalPages}}} (174-185); <script id="pages-data"> = {totalPages[, songIndex, themeIndex]} for the DEFAULT book only (3712-3716); optional <script id="search-data"> = {pages:[{page,text}]} (1453-1463).
- Static asset URL contract with the build/publisher + service worker: /books/<id>/pages.json, /books/<id>/search-index.json, books/<id>/pages/page-<zeropad(String(totalPages).length)>.webp (312-317), plus SHELL_ASSETS ['/','/index.html','/styles.css','/app.js','/manifest.webmanifest','/books.json','/icon.png','/icon-192.png','/icon-512.png'] (282-292); native file:// mode strips leading slashes via resolveAppPath (276-280).
- Service worker contract (/sw.js): registered with updateViaCache:'none' (2323); client posts {type:'SKIP_WAITING'} (2250); sessionStorage flag 'sv-sw-reload-pending' coordinates the controllerchange reload (263, 2248, 2314-2319); cache names shared with the SW: signo-vivo-static-<CACHE_VERSION> and signo-vivo-pages-<CACHE_VERSION> (268-269).
- Native→web injected globals (WKWebView injection, note VINO spelling): window.__SIGNO_VINO_INITIAL_BOOK ('standard'|'hymns-4', 215), window.__SIGNO_VINO_NATIVE_FILE_MODE (274), window.__SIGNO_VINO_NATIVE_BUNDLE_VERSION (3822).
- Native→web event entrypoint: window.__signoVivoReceiveNativeEvent(payload) (1112) accepting {type:'bridge-state',available} | {type:'set-book',book} | {type:'role',role:'director'|'follower'|'none'} | {type:'relay-auth-error',status:401|403} | {type:'sync-event',event:{type:'state',status:'searching'|'connecting'|'resolving-conflict'|...} or {type:'page',page:number,book?:bookId}}.
- Web→native bridge messages via window.ReactNativeWebView.postMessage(JSON.stringify({channel:'signovivo-native', ...})) (363-375): {type:'page-changed',page,totalPages,book} (1186-1191); {type:'render-failed',page,book} (1217-1221, native must clear its optimistic currentPageRef — paired fix); {type:'book-changed',book,page} (1325, native should adopt msg.page atomically — paired fix); {type:'director-code',code} (1417); {type:'exit-director'} (2666); {type:'resync'} (3316); {type:'bridge-ready',page,totalPages,book} (3811-3816).
- Relay worker HTTP/WS contract (RELAY_BASE, room 'alvernia-main'): GET {RELAY_BASE}/r/alvernia-main/state → JSON snapshot {page:number, seq:number, ts:epoch-seconds, bookId?:'standard'|'hymns-4'|legacy, mode?:'standard'|'nonStandard'|'nonstandard'} with response header X-Hymnal: 'standard'|'nonstandard' encoding the requester's IP-geo book (3455, 3525, 3536, 3460); WS wss {RELAY_BASE}/r/alvernia-main/subscribe streaming the same snapshot JSON, client sends literal text frames 'ping' every 4s and expects a snapshot reply (3456, 3630); POST {RELAY_BASE}/r/alvernia-main/unlock with request header X-Director-Code: <digits> → 200 {ok:true} validates a director code server-side (3490-3496); POST {RELAY_BASE}/fleet/checkin with JSON body {deviceId,label,role,surface:'web',webCached:boolean,pagesCached:number,totalPages:number,homeScreen:boolean,cacheVersion:string} (3139-3156). Director liveness window: 90s (RELAY_LIVE_MAX_AGE_S, 3079). Legacy mode mapping mirrors native PdfReaderApp.bookFromSync: 'standard'→standard, 'nonStandard'/'nonstandard'→hymns-4 (3343-3350).
- index.html preconnect/dns-prefetch pins the relay origin https://signovivo-sync.4j4982y8jp.workers.dev (index.html:36-37) — must match __RELAY_BASE__ or the warmup is wasted.
- CSS/JS role contract: html[data-role='director'] shows .search-fab, hides .resync-fab, shifts .song-jump-fab (styles.css:2123, 2154-2156); JS sets it only from native 'role' events (app.js:962); CSS default (no attribute) = follower layout. html[data-book] set by updateBookLabel (1333). body.sv-drawer-open (1344) and #geo-gate.is-hidden/.is-offline classes are shared JS↔CSS state.
- localStorage keys shared across boot sessions and (svGeoBook/svStandardUnlock) with the geo/unlock flow: svGeoBook, svStandardUnlock, sv-offline-ready-<CACHE_VERSION>, svFleetLabel/svFleetRole/svFleetDeviceId/svFleetSkip, sv-recientes, sv-haptic, sv-tip, nc-sort-prefs; sessionStorage sv-sw-reload-pending; IndexedDB signo-vivo-offline→bundle-status→'current'.
- DOM data contract (LOAD-BEARING since 2026-09-05): #page-image's static src in index.html is books/standard/pages/page-001.webp and renderPage stamps pageImage.dataset.page = String(currentPage). app.js's renderedPage() reads data-page, else the src via /page-(\d+)\.webp(?:\?.*)?$/, and initReader's post-gate reconciliation renders that page WEB-LOCALLY (renderPage(..., { notifyNative: false })) when no native page arrived. Changing the static src, its filename pattern, or the <img> element breaks the boot reconciliation — e2e/bootStateMatchesScreen.test.mjs pins all three. (Superseded text: the old initial src books/hymns-4/pages/page-02.webp assumed hymns-4's 2-digit padding and page 2 existing.
- Director-code semantics shared with the worker + native: numeric codes, 5+ digits = code (not song), ≤10 digits enterable on web (appendDigit cap, 1374); Sión public code 1234567890 unlocks only hymns-4 transmission server-side; the web /unlock endpoint pins the device to the standard book on success.
- RELAY BASE URL: build token '__RELAY_BASE__' replaced at build time; runtime fallback 'https://signovivo-sync.4j4982y8jp.workers.dev' if unreplaced (3074–3077). Room constant 'alvernia-main' (3078).
- HTTP GET {RELAY_BASE}/r/alvernia-main/state (3455): response BODY is the snapshot JSON; response HEADER 'X-Hymnal' ∈ {'standard','nonstandard'} drives geo book selection (3460, 3536). Worker must NOT attach X-Hymnal to error responses (web reads it before checking r.ok).
- WS wss://{RELAY_BASE host}/r/alvernia-main/subscribe (3456). Server→client messages: JSON snapshot objects. Client→server: literal text frame 'ping' every 4s (3630); server MUST reply with a snapshot (heartbeat liveness depends on it — 12s silence closes the socket).
- SNAPSHOT WIRE SHAPE (consumed 3343–3453): { page: number (required, finite), seq: number (>0 means a director has published; monotonically compared), ts: unix SECONDS (freshness window 90s = RELAY_LIVE_MAX_AGE_S; missing ts ⇒ treated fresh), bookId?: 'standard'|'hymns-4' (canonical) or legacy value e.g. 'alvernia' (ignored), mode?: 'standard'|'nonStandard'|'nonstandard' (legacy fallback: standard→standard, nonStandard→hymns-4) }. Native's PdfReaderApp.bookFromSync must resolve the SAME book from the same fields (comment 3335–3342).
- HTTP POST {RELAY_BASE}/r/alvernia-main/unlock (3490–3493): request header 'X-Director-Code: <digits ≤10>'; success response must be res.ok with JSON {ok:true}. Worker validates against TRANSMITTER_CODES server-side; book-scoped codes (Sión public 1234567890 vs standard real phone numbers) are entirely worker-side — the web client never sees the code set.
- HTTP POST {RELAY_BASE}/fleet/checkin (3151): JSON body { deviceId, label, role, surface:'web', webCached: boolean, pagesCached: number, totalPages: number, homeScreen: boolean, cacheVersion: string, …extra } with keepalive; consumed by the /fleet-dashboard worker route.
- NATIVE BRIDGE OUTBOUND (web→native via window.ReactNativeWebView.postMessage, JSON with channel:'signovivo-native', 363–375): {type:'page-changed', page, totalPages, book} (1186); {type:'render-failed', page, book} (1217 — native must clear its optimistic currentPageRef; paired fix); {type:'book-changed', book, page} (1325 — native must adopt msg.page atomically; paired fix); {type:'director-code', code} (1417); {type:'exit-director'} (2666); {type:'resync'} (3316); {type:'bridge-ready', page, totalPages, book} (3811).
- NATIVE BRIDGE INBOUND (native→web via injected call to window.__signoVivoReceiveNativeEvent, 1112, handler 1054–1110): {type:'bridge-state', available:boolean}; {type:'set-book', book:'standard'|'hymns-4'}; {type:'role', role:'director'|'follower'|'none'}; {type:'relay-auth-error', status:401|403}; {type:'sync-event', event:{type:'state', status:'searching'|'connecting'|'resolving-conflict'|…}} and {type:'sync-event', event:{type:'page', page:number, book?:bookId}}.
- NATIVE-INJECTED GLOBALS (read at boot): window.__SIGNO_VINO_INITIAL_BOOK (215, book precedence #1), window.__SIGNO_VINO_NATIVE_FILE_MODE (274, forces NATIVE_FILE_MODE), window.__SIGNO_VINO_NATIVE_BUNDLE_VERSION (3822, build badge).
- BUILD-TIME TOKENS replaced by the web build: '__CACHE_VERSION__' (264, keys cache names + OFFLINE_READY_KEY + fleet cacheVersion), '__BUILD_NUMBER__' (267, from version.json — drives the bottom-right badge), '__RELAY_BASE__' (3074).
- INLINE SCRIPT TAGS the build must emit into index.html: #books-data = registry {default, books:{<id>:{label,totalPages}}} (176); #pages-data = {totalPages[, songIndex, themeIndex]} for the DEFAULT book (3712); #search-data = {pages:[{page,text}]} optional (1453).
- html dataset contracts with CSS: document.documentElement.dataset.role='director'|'follower' controls follower ⟳+♪ vs director ♪+⌕ fab layout (962); dataset.book=<bookId> (1333).
- SERVICE WORKER contract: registered at /sw.js with updateViaCache:'none' (2323); web posts {type:'SKIP_WAITING'} to a waiting worker (2250); sessionStorage flag 'sv-sw-reload-pending'='1' marks self-requested activation; controllerchange reload semantics depend on it (2312–2321).
- CACHE/URL contracts shared with sw.js and the page-render pipeline: cache names signo-vivo-static-{CACHE_VERSION} / signo-vivo-pages-{CACHE_VERSION} (268–269); page image path 'books/<bookId>/pages/page-<padded>.webp' with pad width = String(registry totalPages).length per book (308–317); shell asset list (282–292); per-book manifests /books/<id>/pages.json + /books/<id>/search-index.json (295–298).
- STORAGE KEYS shared across sessions/subsystems: localStorage svGeoBook ('standard'|'hymns-4' — geo persistence AND Del-Rio authorization proof, read by isDelRioAuthorized 3480), svStandardUnlock ('1' — director-code unlock pin), svFleetLabel/svFleetRole/svFleetDeviceId/svFleetSkip (fleet identity), sv-offline-ready-{CACHE_VERSION} ('ready'), plus UI prefs nc-sort-prefs / sv-haptic / sv-tip / sv-recientes; IndexedDB 'signo-vivo-offline' store 'bundle-status' record 'current' {version, totalPages, verifiedAt}.
- TIMING CONTRACTS the worker/director side must honor: RELAY_LIVE_MAX_AGE_S=90 — a live director must refresh its published ts at least every 90s or web followers demote it to 'no director'; seq must be monotonically increasing across a Mass (a reset strands non-forced followers until a forced poll); ts must be unix SECONDS.
- DEFAULT_START_PAGE=2 (262) is the page carried in book-changed to native and rendered after any book switch — native's book-changed handler must accept it as the atomic start page.
- Relay origin URL: https://signovivo-sync.4j4982y8jp.workers.dev — hardcoded in index.html:36-37 (preconnect/dns-prefetch) AND as build default for __RELAY_BASE__ in build.mjs:45, overridable via env ALVERNIA_RELAY_BASE; app.js talks to the worker relay at this base.
- Build token __CACHE_VERSION__: replaced identically in app.js (build.mjs:49) and sw.js (build.mjs:59) with '<git short SHA>-<first 8 hex of sha256(app.js,sw.js,styles.css,index.html,manifest.webmanifest,build.mjs)>'; both sides derive cache names signo-vivo-static-<v> / signo-vivo-pages-<v> from it and app.js stores it in offline-bundle metadata.
- Build token __BUILD_NUMBER__: read from repo-root version.json .buildNumber (build.mjs:32-39), baked into app.js, rendered into #build-badge ('v<NNN>') and #app-version-label; native instead injects window.__SIGNO_VINO_NATIVE_BUNDLE_VERSION.
- Build token __RELAY_BASE__: replaced only in app.js (build.mjs:48-51).
- SW postMessage protocol: app.js → SW {type:'SKIP_WAITING'} (app.js:2250, sw.js:133-139); no SW→page messages.
- sessionStorage key 'sv-sw-reload-pending' (SW_RELOAD_FLAG, app.js:263) — set before SKIP_WAITING, consumed in the controllerchange reload handler.
- CacheStorage names/prefixes shared app.js↔sw.js: signo-vivo-static-<v>, signo-vivo-pages-<v>, prefixes 'signo-vivo-static-'/'signo-vivo-pages-' (sw.js:2-5, app.js:268-269); app.js counts PAGE_CACHE entries by bare pathname (getCachedPageSet) so sw.js normalizes page cache keys to bare URLs (sw.js:194).
- Offline-bundle persistence keys (app.js, keyed to SW cache version): 'sv-offline-ready-<CACHE_VERSION>' (app.js:270); IndexedDB database 'signo-vivo-offline', object store 'bundle-status', record id 'current' (app.js:271-273).
- Asset-path contracts the SW routes on: shell = /, /index.html, /styles.css, /app.js, /manifest.webmanifest (SWR); icons /icon.png /icon-192.png /icon-512.png (precached); page images /books/<bookId>/pages/page-NNN.webp (PAGE_CACHE, matched by pathname.includes('/pages/'), query params ?retry=N/?reload= ignored via ignoreSearch); book manifests /books/<id>/pages.json + /books/<id>/search-index.json and registry /books.json (generic cache-first, cached by app.js coreAssetsForBook, NOT SW-precached).
- Cloudflare Pages _headers — NOT A LIVE CONTRACT (build.mjs:706-725, verified 2026-07-31): build.mjs emits '/books/*/pages/*' → 'Cache-Control: public, max-age=31536000, immutable', but Cloudflare Pages REJECTS any _headers path containing more than one '*', so the rule is discarded at parse time and prod actually serves 'public, max-age=0, must-revalidate' + ETag on both signovivo.com and the raw pages.dev origin. (A single mid-path '*' is fine; ':placeholder' segments are unlimited — the valid rewrite is '/books/:book/pages/*', confirmed on a preview deploy.) The SW's cross-version page-cache fallback therefore rests on CONVENTION (stable filenames + additive book updates), not on an HTTP guarantee. Deliberately left inert — see build.mjs:706-721 and web/src/sw.js:6-30 for why arming it is a footgun until M6.
- Build-injected inline JSON script tags (build.mjs:813-821): <script id='books-data' type='application/json'> = {default:'hymns-4', books:{standard:{label,totalPages}, 'hymns-4':{label,totalPages}}} and <script id='pages-data'> = {totalPages:<default book>} — parsed synchronously by app.js (app.js:176, 3712); DEFAULT_BOOK 'hymns-4' must agree with index.html:57's hardcoded img src.
- Native WebView injection globals (native→web): window.__SIGNO_VINO_NATIVE_FILE_MODE (file:// detection fallback, app.js:274), window.__SIGNO_VINO_INITIAL_BOOK (app.js:215), window.__SIGNO_VINO_NATIVE_BUNDLE_VERSION (app.js:3822); native bridge message channel name 'signovivo-native' (app.js:275); in native file:// mode resolveAppPath rewrites '/'→'./' and strips leading slashes, and SW registration is skipped entirely (app.js:2299).
- Role contract (app.js→CSS): html[data-role='director'] attribute drives ⌕ show / ⟳ hide / ♪ reposition (styles.css:2119-2156); body.sv-drawer-open hides right fabs (styles.css:2158-2160).
- Geo contract (worker→web): relay geo response carries X-Hymnal header mapped to a book id (app.js:3536); first-visit-offline UI state = #geo-gate.is-offline + caption 'Sin conexión — reintentando…' (app.js:42-46, styles.css:115-117).
- DOM id contract index.html→app.js (load-bearing ids): geo-gate, geo-gate-text, build-badge, viewer-shell, page-image, director-mode-badge, loading, offline-gate/-title/-body/-spinner, offline-progress-value, offline-admin-note, offline-ready-note, offline-meta-note, offline-test-button, offline-continue-button, offline-retry-button, drawer-handle, resync-fab, song-jump-trigger, search-fab, fullscreen-fab, song-jump-modal/-backdrop, song-display, numberpad-grid, backspace-button, go-button, song-cancel, overlay-controls, drawer-backdrop, navigation-drawer, drawer-close, prev-page, next-page, drawer-nav-center, drawer-back, song-status, song-intro, mode-btn-numpad, mode-btn-browse, numpad-panel, numpad-section (+4 hidden stubs), numpad-tip-wrap, tip-dismiss, browse-panel, drawer-tab-rail, drawer-pane-content, search-row, search-back, search-input, search-clear, search-col-header, search-sort-toggle, search-results, search-cancel, help-panel, help-close, haptic-toggle, app-version-label, fleet-picker/-title/-name/-role/-skip/-save.
- Manifest/home-screen contract: scope '/', start_url '/', display standalone with display_override [fullscreen,standalone], orientation 'any', theme/background #060a18, icons /icon-192.png /icon-512.png (+maskable) /icon.png 1024 — icons generated by build.mjs via sips from assets/icon.png; apple-touch-icon uses icon-512.png (index.html:33).
- Public web identity URLs hardcoded in OG tags: https://signovivo.com/ and https://signovivo.com/icon.png (index.html:20-21, 30).
- SW registration contract: navigator.serviceWorker.register(resolveAppPath('/sw.js'), {updateViaCache:'none'}) at origin scope; skipped when NATIVE_FILE_MODE or non-secure context (app.js:2298-2325).
- Dead/external: email.html POSTs JSON {to, subject, htmlContent} to https://send-email.cb01b2826b7b8ea196b760742b4bb2ee.workers.dev (email.html:14-22) — foreign worker on a different workers.dev account than the relay; not part of the shipped app.
- Base URL (prod): https://signovivo-sync.4j4982y8jp.workers.dev (no custom domain; wrangler.jsonc has no routes). Room name shared with all clients: 'alvernia-main' (README.md:18, test-client.html:29).
- Snapshot wire shape (WS messages, /state body, /publish request body, outer-catch fallback body): {v:1, page:number(1-100000), totalPages:number(0-100000), mode:string(≤64), bookId:string(≤64), seq:number, ts:unixSeconds} — defined src/index.ts:38-48. seq===0 means 'no director yet'; ts drives client liveness via the shared constant RELAY_LIVE_MAX_AGE_S=90 (src/index.ts:36 — comment says it 'matches the web follower's RELAY_LIVE_MAX_AGE_S'; coherence check required). Directors are expected to republish every ~12s (src/index.ts:34-35). Client convention: seq = Date.now() in MILLISECONDS (test-client.html:105); server sanitize cap Date.now()+60000 (src/index.ts:110) assumes this scale.
- bookId vocabulary: 'alvernia_manual_2' = private standard manual, 'hymns-4' = public 'Himnos de Sión' (src/index.ts:511-515, 796). The Sión-code book-scope check compares String(body.bookId)==='hymns-4' exactly (src/index.ts:796). README/test-client's 'standard' bookId is NOT one of these.
- HTTP routes: GET|OPTIONS on all; GET /(=/health) → {ok,service:'signovivo-sync',v,geo:{city,region,postal,country,lat,lon,kmFromDelRio,hymnal}}; GET(WS) /r/:room/subscribe; GET /r/:room/state → Snapshot; POST /r/:room/publish → {ok:true,seq,ignored?} | {ok:false,error}; POST /r/:room/unlock → {ok:boolean,hymnal:'standard'|'nonstandard'} (200/403); POST|GET|DELETE /log; POST /fleet/checkin; POST /fleet/roster; POST /fleet/reset; GET /fleet | /fleet.json → {roster,devices}; GET /fleet-dashboard | /fleet/dashboard → HTML. Room regex [A-Za-z0-9_-]{1,64} (src/index.ts:347).
- Request headers: 'Authorization: Bearer <RELAY_DIRECTOR_TOKEN>' (unrestricted publish + fleet access); 'X-Director-Code: <digits>' (stripped to [0-9] server-side at :741,:763 — clients may send formatted phone numbers) valid for /publish and /unlock; 'X-Fleet-Key: <FLEET_DASHBOARD_KEY>' or query '?k=<FLEET_DASHBOARD_KEY>' for /fleet*; 'Content-Type: application/json' on POSTs.
- Response header: 'X-Hymnal: standard|nonstandard' on EVERY response (src/index.ts:553), exposed to browser JS via Access-Control-Expose-Headers (src/index.ts:316); the web app reads it off its first /r/:room/state fetch to choose the book (src/index.ts:516-517, 716-721). Geo gate: 45km haversine radius around 29.3627,-100.8968 OR ZIP 78840/78841 OR city 'del rio'/'ciudad acuna'/'acuna' OR MX postal 26200-26299 (src/index.ts:529-552).
- CORS contract: Access-Control-Allow-Origin from ALLOWED_ORIGINS var (currently '*', wrangler.jsonc:20 — required for the native file:// WebView's null origin); Allow-Headers 'Authorization,Content-Type,X-Director-Code'; Allow-Methods GET,POST,OPTIONS; Max-Age 86400. Any origin tightening must account for the native WebView.
- WebSocket protocol: on connect server immediately sends the current Snapshot JSON (src/index.ts:258); every accepted publish broadcasts the Snapshot JSON to all sockets (src/index.ts:289-298); client→server the ONLY meaningful messages are the literal strings 'resync' and 'ping', each answered with the current Snapshot (src/index.ts:267-275); all other client messages silently ignored (followers are read-only). No auth, no origin check on subscribe.
- Error vocabulary shared with clients: {ok:false,error: 'unauthorized'|'method_not_allowed'|'payload_too_large'|'bad_json'|'sion_code_book_scoped'|'publish_failed'|'not_found'|'expected_websocket'} with statuses 401/405/413/400/403/500/404/426; plus the outer-catch anomaly: any unhandled failure returns HTTP 200 with the EMPTY_SNAPSHOT shape (src/index.ts:810-816) — clients/scripts must not treat 200 alone as write success.
- Auth constants: SION_DIRECTOR_CODE='1234567890' hardcoded and public (src/index.ts:334) — director entry for hymns-4 only, never unlocks standard; real director codes (phone numbers, incl. legacy '12345678840' for ≤build-367 compat) live ONLY in the TRANSMITTER_CODES Worker secret (comma-separated); RELAY_DIRECTOR_TOKEN secret is baked into scripts/director tooling; FLEET_DASHBOARD_KEY secret ('coro-…' prefix per project memory) shared out-of-band with the director.
- Fleet check-in body (device→worker, OPEN endpoint): {deviceId:string≤64 (stable per-install uuid from localStorage/AsyncStorage), label:string≤60 (self-entered name), role:string≤60, surface:'web'|'native', nativeBuild?:number, webCached?:boolean, pagesCached?:number, totalPages?:number, homeScreen?:boolean, cacheVersion?:string≤40} → {ok:true,total:number} (src/index.ts:66-78,196-231). Native shell and web PWA must both emit this shape.
- Fleet read shape (gated): GET /fleet(.json) → {roster:[{id,name,role,phones:string[],director?:boolean}], devices:FleetDevice[] newest-first} (src/index.ts:234-239). Roster seed body: RosterPerson[] or {people:[…]} (src/index.ts:678-683); phones exist ONLY server-side + gated dashboard (never in repo, never sent by devices).
- Dashboard readiness constants that must track native releases: MIN_SYNC_BUILD=361 (src/index.ts:352, follower floor) and director-must-equal fleet-max-build logic (src/index.ts:381-382,410-412) — bumping native builds changes dashboard semantics with no code change here.
- /log body contract (device→worker, OPEN): JSON array | {entries:[…]} | single object; each entry stamped server-side with rx:unixSeconds; read-back shape {ok,count,entries} (src/index.ts:594-619,148-157). Used by devices to report Multipeer sync lifecycle for remote debugging.
- DO instance-name contract: sync rooms by user-visible name via getByName(room); fixed instances '__debug_log__' (src/index.ts:595) and '__fleet__' (src/index.ts:629) — both also reachable as /r/ rooms because the route regex permits underscores.
- test-client.html localStorage keys: sv_base, sv_token, sv_room (test-client.html:117-121); its follower logic (backoff 500ms→8s then 4s polling, stale guard seq<=lastSeq) mirrors — but may have drifted from — the real web follower implementation.
- Build/deploy contract: wrangler ^4.20, compatibility_date 2025-06-01 (DO RPC methods require modern runtime), SQLite-backed DO migration tag v1 (class rename/delete needs a new migration), deploy via `npx wrangler deploy`, secrets via `wrangler secret put RELAY_DIRECTOR_TOKEN|TRANSMITTER_CODES|FLEET_DASHBOARD_KEY`, local dev via .dev.vars.
- WebView bridge, web→native: window.ReactNativeWebView.postMessage(JSON) with envelope {channel:'signovivo-native', type, ...}; types handled by PdfReaderApp.tsx:550-697: 'bridge-ready' {page?,totalPages?,book?}, 'page-changed' {page,totalPages?,book?}, 'director-code' {code}, 'book-changed' {book, page?}, 'resync' {}, 'exit-director' {}, 'render-failed' {}. Channel constant NATIVE_BRIDGE_CHANNEL defined web/src/app.js:275.
- WebView bridge, native→web: injected JS calls window.__signoVivoReceiveNativeEvent(payload) (PdfReaderApp.tsx:264; web/src/app.js:1112). Payload types: {type:'role', role:'director'|'follower'|'none'}, {type:'bridge-state', available:bool}, {type:'sync-event', event:{type:'page',page,book} | {type:'state',status,role,message}}, {type:'set-book', book}, {type:'relay-auth-error', status:401|403}.
- Injected preload globals (injectedJavaScriptBeforeContentLoaded, PdfReaderApp.tsx:961-970): window.__SIGNO_VINO_NATIVE_FILE_MODE=true, window.__SIGNO_VINO_NATIVE_BUNDLE_VERSION='<buildNumber>', window.__SIGNO_VINO_INITIAL_BOOK='standard'|'hymns-4' — consumed at web/src/app.js:215,274,3822. (Note the VINO not VIVO spelling — both sides must keep the typo.)
- Relay publish: POST https://signovivo-sync.4j4982y8jp.workers.dev/r/alvernia-main/publish, header X-Director-Code: <digits>, body {v:1, page:int≥1, totalPages:int≥0, mode:'standard'|'nonStandard', bookId:'standard'|'hymns-4', seq:wall-clock-ms-monotonic, ts:unix-seconds} (directorRelaySync.js:74-121). Response semantics: 200 ok; 401 unknown/retired code; 403 Sión code aimed at private manual — both latch a one-shot relay-auth-error.
- Relay state/geo: GET https://signovivo-sync.4j4982y8jp.workers.dev/r/alvernia-main/state with response header X-Hymnal: 'standard'|'nonstandard' (lowercase! differs from mesh mode's camelCase 'nonStandard') → book selection (PdfReaderApp.tsx:895-898; worker sets it; web app.js:1283,3530 also reads it). Worker freshness window RELAY_LIVE_MAX_AGE_S=90 is what the 12s native relay heartbeat services (PdfReaderApp.tsx:327-330).
- Telemetry: POST https://signovivo-sync.4j4982y8jp.workers.dev/log with a JSON ARRAY of events; JS shape {t:ms, dev:<sv_devid 6-char>, role, build, event, ...} (PdfReaderApp.tsx:138-168); Swift shape adds src:'swift' and dev=<MCPeerID displayName> (DirectorSyncModule.swift:174-191). GET /log is the debugging timeline.
- Fleet dashboard: POST https://signovivo-sync.4j4982y8jp.workers.dev/fleet/checkin body {deviceId:<sv_devid>, surface:'native', nativeBuild:number, label:string(≤40), role:'Director'|''} (PdfReaderApp.tsx:173-192); shares the /fleet dashboard + seeded DO roster with signovivo.com; deviceId key sv_devid is shared with /log.
- Live takeover (2026-09-05): takeoverDirector(sessionCode) is the ONLY door for a CONNECTED follower to become director. It mints the token, sends {type:'director_announce', token} to connectedDirectorPeer over the still-open session, waits takeoverAnnounceFlushSeconds (0.4 s), then runs beginDirecting with that same token; the replaced director demotes via handleDirectorConflict (newer token wins — receivers have handled this since before build 472). JS (becomeDirector) no longer drops the follower link before starting. startDirector keeps rejecting a connected follower with DIRECTOR_TAKEOVER_REQUIRED. The deferred start is guarded on resetGeneration and rejects DIRECTOR_TAKEOVER_SUPERSEDED if a role change lands inside the flush window. MIXED FLEET: the announce is sent by the TAKER, so it protects only when the taker runs ≥481; a 472 taker still drop-then-starts and the replaced director learns through discovery (up to ~45 s). A director that loses its last connected follower re-browses at once (refreshBrowserOnly) so that path is shorter than the 25 s tick.
- BLE scanner newcomer rules (2026-09-05, BlePageBeacon.didDiscover): an advertiser never applied is not rendered until heard ≥ newAdvertiserGrace (1.0 s) with no contention; its floor is the seq it was FIRST heard at (must climb past it if it was ever on a contested air, accepted as-is otherwise); a never-applied advertiser reappearing after > contentionWindow of silence has that first-heard seq re-stamped to the last seq heard before the silence (the moment is kept), i.e. build 480's floor for a returning advertiser. Consequence: a director that appears within 4 s of another advertiser (every live takeover, to third followers) is followed over BLE from its first page TURN; the mesh carries its first page. Cause: bluetoothd re-emits a dead process's last advertisement for ~1 s when advertising restarts, and a rebooted follower has no floor for that nonce. Scanner-side only: 472 followers keep rendering the replayed ghost.
- Native module contract (JS ↔ Swift): NativeModules.DirectorSyncModule methods startDirector(sessionCode), takeoverDirector(sessionCode), startFollower(sessionCode), stop(), resetForAppReset(), sendPageUpdate(page,totalPages,mode,bookId), primePermissions(), refreshNearbyDiscovery(), requestCurrentSnapshot(), setIdleTimerDisabled(bool), getDeviceName(), requestDirectorTakeover(), approveDirectorTakeover(id), denyDirectorTakeover(id) (DirectorSyncModuleBridge.m; wrappers src/nearbyDirectorSync.js). Rejection codes JS must handle: DIRECTOR_TAKEOVER_REQUIRED, DIRECTOR_SESSION_INVALID, DIRECTOR_ROLE_INVALID, DIRECTOR_TAKEOVER_*.
- Native event contract: NativeEventEmitter event name 'DirectorSyncEvent' (DirectorSyncModule.swift:10; nearbyDirectorSync.js:103) with bodies: {type:'page',page,totalPages,mode,bookId,sessionCode}, {type:'state',role,sessionCode,status∈[idle,advertising,searching,connecting,connected,waiting-followers,resolving-conflict,self-directed],peerCount,directorCount,message}, {type:'error',code∈[DIRECTOR_CONFLICT,DIRECTOR_START_FAILED,FOLLOWER_START_FAILED,BUNDLE_PACK_FAILED,BUNDLE_SEND_FAILED,BUNDLE_RECEIVE_FAILED],message,...}, {type:'takeover-request',requestId,requesterName}, {type:'takeover-approved'/'takeover-denied',requestId}, {type:'bundleUpdated',version}, {type:'bundle-error',stage}, {type:'memoryWarning',role}.
- Multipeer wire protocol: Bonjour serviceType 'signovivo' (must match Info.plist NSBonjourServices _signovivo._tcp/_udp); discoveryInfo {session:'1234' normalized, role:'director'|'follower', hgen:'2' (absent = legacy ≤226 → inverted invite direction), token:<20-digit-µs>-<UUID> (director only)}; data payloads JSON ≤8KB {v:1, type:'page'|'hello'|'director_announce'|'takeover_request'|'takeover_approved'|'takeover_denied'|'bundle_offer'|'bundle_request', ...}; page payload carries bundleVersion:<CFBundleVersion string>. Fixed shared session code DIRECTOR_SESSION='1234' (PdfReaderApp.tsx:49).
- Peer bundle-transfer resource format: MCSession sendResource named 'webbundle-<ver>.pack'; archive = [4-byte BE UInt32 header length][header JSON {v:1,version,files:[{path,len}...]}][concatenated file bytes in manifest order] (packWebBundle 800-871 / installReceivedBundle 879-1049); installed to Documents/WebBundle which resolveBundleUri prefers over the shipped bundle.
- Director codes contract: SION_DIRECTOR_CODE '1234567890' valid only when currentBook==='hymns-4'; standardDirectorCodes[] from director-codes.json (committed empty; baked from gitignored director-codes.private.json by release.sh at archive time) valid only on 'standard'; SOFT_RESET_CODE '744668486'. The SAME entered code is sent as X-Director-Code, so the worker's TRANSMITTER_CODES secret must contain the same numbers with the same book scoping (native and worker must stay in lockstep).
- Book identity contract: canonical BookId ∈ {'standard','hymns-4'}; legacy mode ∈ {'standard','nonStandard'} (modeForBook PdfReaderApp.tsx:73); geo header value ∈ {'standard','nonstandard'} — three encodings of the same 2-book concept that converters (bookFromSync:75-78, geo mapping:897-898) must keep aligned.
- Build-number contract: version.json {baseVersion,buildNumber} is the single source; app.config.js reads it at config-resolve time; scripts/bump-build.mjs propagates to ios/SignoVivo.xcodeproj CURRENT_PROJECT_VERSION, ios/SignoVivo/Info.plist CFBundleVersion, app.json; PdfReaderApp BUILD_VERSION (line 43) rides telemetry/fleet/preload; Swift currentBundleVersion (CFBundleVersion) rides mesh page payloads and bundle_offer version comparison (numeric Int compare). web/build.mjs bakes the same number into the web badge; release.sh enforces lockstep.
- Web bundle location contract: release.sh copies web/dist → ios/WebBundle (gitignored, pbxproj blue-folder resource) → shipped at <Bundle>/WebBundle/index.html; peer updates land at <Documents>/WebBundle/index.html; PdfReaderApp resolveBundleUri prefers Documents (711-724); WebView loads it via file:// which REQUIRES the postinstall patch scripts/patch-rn-webview.js (RNCWebViewImpl.m host-length fix) to actually route through loadFileURL:allowingReadAccessToURL:.
- AsyncStorage keys shared across the shell's own subsystems (defined src/offlineBooks.ts:84-97 + PdfReaderApp): sv.book.active, sv.book.lastPage.<bookId>, sv.sync.lastRole, sv.sync.lastDirectorAt, director_access_code, sv_devid, sv_fleet_label, sv_fleet_skip, sv_bc.
- Song-index build contract: src/alverniaManual2SongIndex.js (song→page pairs incl. [371,371] 'Santo Español', note irregular [347,346]) is read as SOURCE TEXT by web/build.mjs:575 and inlined into the web app, validated by scripts/check-book-consistency.mjs and e2e/native-entrypoint.test.mjs:45 — the native file is the ground truth for the WEB jump-by-number feature.
- Build-time placeholder tokens replaced by web/build.mjs: __CACHE_VERSION__ (web/src/app.js:264 and web/src/sw.js:1), __RELAY_BASE__ (web/src/app.js:3074, default https://signovivo-sync.4j4982y8jp.workers.dev, overridable via env ALVERNIA_RELAY_BASE, build.mjs:46), __BUILD_NUMBER__ (web/src/app.js:267; app.js:3820 guards un-replaced token via `[0] !== "_"`).
- Native→web injected global: window.__SIGNO_VINO_NATIVE_BUNDLE_VERSION = String(version.json buildNumber) (PdfReaderApp.tsx:965 via injectedJavaScriptBeforeContentLoaded; consumed web/src/app.js:3819-3826 with precedence over baked BUILD_NUMBER). Also native→web event sink window.__signoVivoReceiveNativeEvent (pinned by native-entrypoint.test.mjs:24) and html[data-role] role-driven controls (per memory).
- Inlined HTML data contracts (build.mjs:814-821): <script id="books-data" type="application/json"> = {default:"hymns-4", books:{<id>:{label,totalPages}}}; <script id="pages-data"> = {totalPages:<default book>}. Web app must lazily fetch /books/<id>/pages.json and /books/<id>/search-index.json for everything else.
- dist/URL layout served by Pages AND bundled into ios/WebBundle (file://): /index.html /app.js /sw.js /styles.css /manifest.webmanifest /icon.png /icon-192.png /icon-512.png /books.json /books/<id>/pages/page-NNN.webp /books/<id>/{pages.json,search-index.json,song-titles.json,song-search-index.json} /_headers (Pages-only, and INERT — Pages rejects the two-splat rule; see the _headers entry above).
- Per-book manifest shapes (produced by build.mjs, consumed by web app + native): pages.json {totalPages, songIndex:[{song,page,title,themes,lyrics,key,solfege,complexity,intro}], themeIndex:[{id,label,emoji}] (empty [] for hymns-4)}; search-index.json {pages:[{page,text}]} (page = RENDERED page number); song-titles.json {"<song>":"Title"}; song-search-index.json {"<song>":"ocr blob"}. NOTE: prebuilt assets/standard/hymns-4-song-search-index.json is a LIST [{song,page,title,normalized,lyrics}] with ORIGINAL-book page numbers — web build reshapes it (build.mjs:707-738); native consumes the list shape directly.
- SW cache-name contract: signo-vivo-static-<cacheVersion> / signo-vivo-pages-<cacheVersion>, prefixes signo-vivo-static-/-pages-, PAGE_CACHES_TO_KEEP=2, CRITICAL_SHELL_ASSETS=["/","/index.html","/styles.css","/app.js"] (web/src/sw.js:1-19). cacheVersion format `<git-short-sha>-<8 hex>` (build.mjs:27).
- Relay HTTP/WS contract used by build's test suite and both clients: BASE https://signovivo-sync.4j4982y8jp.workers.dev; room alvernia-main; POST /r/alvernia-main/publish with header X-Director-Code:<digits> (native/transmitter; digits-only normalization server side, index.ts:741,763) OR Authorization: Bearer <RELAY_DIRECTOR_TOKEN secret>; GET /r/alvernia-main/state; WS wss://.../r/alvernia-main/subscribe (first message = snapshot; text "ping" → snapshot reply); GET /health (echoes ok, service:"signovivo-sync", v, requester geo+book per memory); OPTIONS preflight 204 with Access-Control-Allow-Headers: Authorization,Content-Type,X-Director-Code (index.ts:314).
- Relay payload/state shape: {v:1, page:number(clamped>=1, NaN coerced to 1), totalPages, seq:number (strictly-greater-wins; seq=0 always accepted; real native directors use Date.now() ms), ts:unix-seconds(server-set), mode:"standard"|"nonStandard", bookId:string}; rejection body {ignored:true}; success {ok:true, seq}. Client freshness window RELAY_LIVE_MAX_AGE_S=90 (web/src/app.js:3079).
- Director access codes: PUBLIC Sión code "1234567890" hardcoded identically in PdfReaderApp.tsx:55 (SION_DIRECTOR_CODE) and sync-worker/src/index.ts:334 — must never drift; valid only for hymns-4 book scope. STANDARD (Del Rio) codes = real phone numbers: native side baked from gitignored director-codes.private.json into tracked director-codes.json at archive time (release.sh:40-47; PdfReaderApp.tsx:37,56-60 normalizes to digits); server side = worker secret TRANSMITTER_CODES (comma-separated, index.ts:24,341) — two stores synced by hand. Legacy code "12345678840" (build-367 compat) was ROTATED OUT of the secret 2026-07-05 (A1 done — real code → 200, legacy → 401 verified); relay-sync.test.mjs now reads RELAY_TEST_CODE from env, not a hardcode. FLEET_DASHBOARD secret gates /fleet-dashboard?k=… (index.ts:25).
- Multipeer/native constants shared across layers and pinned by tests: DIRECTOR_SESSION="1234" (PdfReaderApp.tsx:49, permission-flow.test.mjs:123); Bonjour service types _signovivo._tcp/_signovivo._udp (Info.plist, pinned twice); SOFT_RESET_CODE="744668486" (PdfReaderApp.tsx:62); SecureStore/AsyncStorage key STORED_CODE_KEY="director_access_code" (PdfReaderApp.tsx:65); Multipeer payload fields {v:protocolVersion, page, totalPages, mode, bookId} + "hello"/"takeover_approved"/"takeover_denied" message types (nearby-sync-contract.test.mjs:20-59).
- version.json {baseVersion:"1.0.4", buildNumber:370} → app.json expo.version + expo.ios.buildNumber(string) + expo.android.versionCode(number); Info.plist CFBundleVersion + CFBundleShortVersionString; pbxproj CURRENT_PROJECT_VERSION + MARKETING_VERSION; web badge; native overlay; fleet check-in field nativeBuild (PdfReaderApp.tsx:182). Bump path: scripts/bump-build.mjs only.
- Deploy targets: Cloudflare Pages project "alvernia-reader", production branch "main" (release.sh:65 --branch main --commit-dirty=true; deploy:web omits branch → preview); Worker name "signovivo-sync" with DO binding SYNC_ROOM/class SyncRoom, vars ALLOWED_ORIGINS="*" (sync-worker/wrangler.jsonc); secrets RELAY_DIRECTOR_TOKEN, TRANSMITTER_CODES, fleet secret.
- Native shell filesystem contract: ios/WebBundle/ = verbatim cp -R of web/dist (release.sh:30, gitignored), loaded over file:// — depends on scripts/patch-rn-webview.js postinstall patching react-native-webview's host.length check so loadFileURL:allowingReadAccessToURL: is used.
- App Store submission identity: bundleId com.cazares.alvernia, ascAppId 6760515687, ascApiKeyId SF9J7DCBTMI1 + issuer 69a6de94-… + local .p8 path (eas.json:33-41); export plists ios/exportOptions.app-store.plist (release.sh/testflight scripts) and scripts/export-options.plist (submit-appstore.sh), both method=app-store, automatic signing; IPA artifact ~/Desktop/SignoVivo-<build>.ipa delivered via `open -a Transporter`; native archive log /tmp/release-native.log.
- Build-tool contracts (must exist on the build mac): pdftoppm/pdftotext/pdfinfo (poppler), cwebp, sips (macOS), tesseract (ocr-nonstandard-index.py only), xcodebuild, npx wrangler; LANG=en_US.UTF-8 required for pod-touching flows.
- Test-run contract: `npm run test:e2e` = node --test ./e2e/*.test.mjs includes the prod-mutating relay-sync file; e2e/eas-config.test.mjs additionally shells `npx expo config --json` (requires installed node_modules).
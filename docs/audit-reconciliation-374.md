# SignoVivo — Audit Reconciliation 370 → 374 (receipts)

> Companion to `app-hardening-plan.md`. Generated 2026-07-04 from the 14-agent reconciliation pass
> (delta-map → 9 per-dimension reconcilers → 3 new-surface finders → synthesizer, all Opus 4.8,
> reading real code at HEAD `39b60313`). This is the evidence trail: what the single-book refactor
> changed, how every original finding was re-triaged, and the full text of the 14 new findings.

**Legend:** SURVIVES = applies as-is · SURVIVES_MOVED = applies, line numbers moved · CHANGED =
still a bug but mechanism/fix differs · PARTIALLY_MOOT = part gone · MOOT = target code deleted.

---

## 1. What the refactor removed (build 370 → 374)

- Two-book registry / book switching — DELETED wholesale. In build 370 web/src/app.js had loadBookRegistry/FALLBACK_BOOK_REGISTRY, switchBook(), loadBook(), hydrateBookData(), bookSwitchGeneration, updateBookLabel(label) driven by a registry (old app.js ~lines 80-150, 474-576). All gone; app.js now hard-pins const BOOK_ID='standard' (app.js:155) and updateBookLabel just sets data-book (app.js current). PdfReaderApp lost the 'book-changed' message case (was PdfReaderApp ~line 608 in 370) and bookFromSync()/modeForBook branching.
- IP-geo book resolution — DELETED from the worker. sync-worker/src/index.ts build 370 had a ~90-line geo block (postal/city/country/lat/lon, Haversine kmFromDelRio, STANDARD_RADIUS_KM=45, isStandard OR-logic, cors['X-Hymnal']) at the old ~line 505-590. HEAD replaces it with a one-line comment + bare try (index.ts:499-508). The /health endpoint no longer echoes geo (index.ts:507-508).
- X-Hymnal header + its CORS exposure — DELETED. 'Access-Control-Expose-Headers: X-Hymnal' removed from corsHeaders (was index.ts ~line 313). Web side deleted bookFromHymnal(), relayGeoBook, relayGeoBookApplied, isDelRioAuthorized(), the whole X-Hymnal read in relayPollOnce (old app.js ~3480-3560), and native deleted its GEO_STATE_URL fetch useEffect (old PdfReaderApp ~lines 852-880).
- Sión public director code (SION_DIRECTOR_CODE='1234567890') — DELETED everywhere: worker (was index.ts ~line 333 const + book-scope enforcement in /publish ~line 690 and /unlock ~line 655), PdfReaderApp (was ~line 60), src/directorRelaySync.js comments. validTransmitterCodes() no longer seeds it (index.ts:327).
- Book-scoped director codes — DELETED. Build 370 PdfReaderApp.onDirectorCode gated by currentBook ('hymns-4'+Sión code vs 'standard'+STANDARD_DIRECTOR_CODES); now a single STANDARD_DIRECTOR_CODES check (PdfReaderApp.tsx:516).
- /unlock private-manual access path — GUTTED. Worker build 370 validated a code and returned hymnal standard/nonstandard with a brute-force-oracle comment (was index.ts ~line 647-700); now a no-op always-ok stub (index.ts:649-654). Web unlockStandard() + svStandardUnlock localStorage + standardUnlocked pin (old app.js ~3078-3120) DELETED.
- The public Sión FLOOR + geo-gate reveal machine — DELETED from app.js: PUBLIC_FLOOR_BOOK, floorBookOnly, precachePublicFloor(), floorPageFileName, resolveInitialBook() precedence, initialBookIsKnown, tryLiftGate() decode-guard, setGateOffline(), revealAfterGeoSettle()/liftGateBackstop() 8s/12s backstops, the online/visibility/5s periodic geo re-heal (old app.js ~9-60, 106-150, 303-360, 1059-1142). Replaced by a single setTimeout(liftGateNow,4000) (app.js:1147).
- hymns-4 book assets + build pipeline — DELETED: assets/hymns-4.pdf (16348 lines), assets/standard/hymns-4-{pages,song-titles,song-search-index}.json. web/build.mjs lost HYMNS4_TITLE_OVERRIDES, buildHymns4Manifests(), the buildBook mode param, and the second buildBook call; DEFAULT_BOOK flipped from 'hymns-4' to 'standard' (build.mjs). src/offlineBooks.ts lost the hymns-4 BOOKS entry + H4 imports + NON_STANDARD_BOOK_IDS (offlineBooks.ts:1-60).
- Auto-director restore on relaunch — DELETED from PdfReaderApp: DIRECTOR_RESTORE_WINDOW_MS, STORED_CODE_KEY, lastDirectorAt persistence, and the boot-time recentlyDirector→becomeDirector(storedCode) block (was ~line 750-770 in 370). Now boot ALWAYS calls becomeFollower() (PdfReaderApp.tsx:757). The stored director code is never persisted anymore.

## 2. What the refactor added

- Director-role confirmation dialog — a valid director code NEVER promotes silently. PdfReaderApp.onDirectorCode now always fires Alert.alert before becomeDirector (PdfReaderApp.tsx:539-547). Three-way title/body: liveDirector takeover warning ('⚠️ Ya hay un director activo' + destructive 'Tomar el control'), super-admin labeled prompt, or plain '¿Dirigir el coro?' (PdfReaderApp.tsx:527-546).
- No auto-director / always-follower boot — device ALWAYS boots as follower; director requires explicit code entry + confirm (PdfReaderApp.tsx:753-757).
- Super-admin code label — SUPER_ADMIN_CODES set (PdfReaderApp.tsx:61-67), a subset of standard codes baked from director-codes.private.json; only changes the confirm dialog wording (isSuperAdmin, PdfReaderApp.tsx:526). director-codes.json gained an empty superAdminCodes array (director-codes.json:4).
- liveDirector takeover heads-up — uses lastDirectorSnapshotRef.current to detect another live director and warn before takeover (PdfReaderApp.tsx:527-528).
- Native pinned to the standard manual — DEFAULT_BOOK:BookId='standard' (PdfReaderApp.tsx:70), isBookId only accepts 'standard' (PdfReaderApp.tsx:74), incoming mesh page forced book='standard' (PdfReaderApp.tsx ~763), boot startBook hard-set 'standard' (PdfReaderApp.tsx ~727). This is the original outage fix (commit f34d2bb0).
- Single public book served to everyone — web app.js const BOOK_ID='standard' + STANDARD_TOTAL_PAGES=371 (app.js:155-159); index.html first-paint img flipped to books/standard/pages/page-001.webp (index.html:54); worker returns standard to all with no auth/geo.
- Director role recorded as breadcrumb only — becomeDirector persists ONLY lastSyncRole, never the code or timestamp (PdfReaderApp.tsx:440-442), since there is no auto-restore.

## 3. Current file shapes

- **web/src/app.js** (3336 lines): The single biggest delta (~904 diff lines, net large deletion). Removed: the entire book-registry/switchBook/loadBook/hydrateBookData/bookSwitchGeneration multi-book layer; the geo-gate reveal state machine (tryLiftGate decode-guard, floorBookOnly, precachePublicFloor, revealAfterGeoSettle/liftGateBackstop 8s/12s backstops, online/visibility/5s geo re-heal); X-Hymnal geo (bookFromHymnal, relayGeoBook*, isDelRioAuthorized, unlockStandard, standardUnlocked); bookFromSnap privacy gate. Current landmarks: BOOK_ID/STANDARD_TOTAL_PAGES const block ~lines 152-159; liftGateNow/revealReader thin helpers ~lines 16-60; single-book coreAssets()/pagePadWidth()/pageFileName() ~lines 182-212; deferOfflinePrecache guarded-once (offlinePrecacheStarted) ~lines 262-300; applyRelaySnapshot now ignores snap.bookId ~lines 684-742 (comment 'kept on the wire for build-373 compat'); relayPollOnce stripped of geo ~lines 803-865; initReader single-book paint ~lines 871-1053; final backstop setTimeout(liftGateNow,4000) at the tail (app.js:1147). #geo-gate is hidden immediately.
- **sync-worker/src/index.ts** (710 lines): 148 diff lines, mostly deletions. Removed: SION_DIRECTOR_CODE const + seeding of validTransmitterCodes (now index.ts:327, no Sión), the ~90-line IP-geo/Haversine/X-Hymnal block from the fetch handler, X-Hymnal from CORS Expose-Headers + /health geo echo (now bare, index.ts:507-508), /unlock code validation (now no-op always-ok stub, index.ts:649-654), and Sión book-scoping in /publish (now any valid code may publish, index.ts:692). VESTIGIAL: Snapshot.bookId field still declared (index.ts:41), still in EMPTY_SNAPSHOT (index.ts:53), still stored/mirrored by DO publish() (index.ts:128) — but no route reads it for authz/routing anymore.
- **PdfReaderApp.tsx** (957 lines): 206 diff lines. Removed: SION_DIRECTOR_CODE, DEFAULT_BOOK='hymns-4'→'standard' (line 70), bookFromSync/modeForBook branching (modeForBook now always 'standard'), isBookId now only 'standard' (line 74), the 'book-changed' message case, GEO_STATE_URL + the IP-geo book-selection useEffect, DIRECTOR_RESTORE_WINDOW_MS/STORED_CODE_KEY/lastDirectorAt auto-restore, storedBookRef/activeBookId persistence. Added: SUPER_ADMIN_CODES set (lines 61-67); rewritten onDirectorCode with always-confirm Alert.alert (lines 499-549, becomeDirector at 545); boot always becomeFollower (lines 753-757); becomeDirector persists only lastSyncRole (lines 440-442); mesh 'page' event forces book='standard' (~line 763).
- **web/build.mjs** (698 lines): 158 diff lines removed. Deleted HYMNS4_TITLE_OVERRIDES (22-song map), buildHymns4Manifests(), the buildBook `mode` param and dispatch, the second buildBook({id:'hymns-4'}) call, and hymns4 from the books.json registry. DEFAULT_BOOK flipped 'hymns-4'→'standard' (single const now, ~line 640). books.json now has one book entry; inline #pages-data is the standard book's totalPages.
- **web/src/index.html** (375 lines): 11 diff lines. #geo-gate div kept but re-commented as a plain loader ('Legacy id geo-gate kept to avoid churn'); first-paint <img id=page-image> src changed from books/hymns-4/pages/page-02.webp to books/standard/pages/page-001.webp (line 54).
- **src/directorRelaySync.js** (129 lines): 18 diff lines. Comments de-Siónized; the publish payload now hard-codes bookId:'standard' (~line 117) 'kept for relay Snapshot backward-compat' rather than sourcing from per-book context. 403 handling comments trimmed (401-only is the real path now).
- **src/offlineBooks.ts** (92 lines): 29 diff lines. AppMode and BookId narrowed to just 'standard' (lines 8-9); H4 JSON imports + the hymns-4 BOOKS entry removed; getBook() now always returns BOOKS[0]; NON_STANDARD_BOOK_IDS export deleted.
- **web/src/sw.js** (0 lines): 17 diff lines, comment-only. Notes rewritten from multi-book (/books/<id>/) to single-book (/books/standard/); no behavioral change. isPageImageRequest still matches /pages/ generically.

**Current protocol:** "Relay protocol is UNCHANGED on the wire but bookId is now vestigial. PROTOCOL_VERSION=1 (index.ts:29). Snapshot shape published/mirrored by the DO is still {v,page,totalPages,mode,bookId,seq,ts} (index.ts:37-46). AUTH: /publish requires either Bearer RELAY_DIRECTOR_TOKEN or a valid X-Director-Code in validTransmitterCodes(env) — which is now purely env.TRANSMITTER_CODES (comma-split), NO Sión seed, fail-closed if unset (index.ts:327,666-671). Any valid code may publish; book-scoping is GONE (index.ts:692). SEQ: monotonic per director session, seq===0 means no director live yet (unchanged); web de-dupes/orders by seq and force-resyncs on load/reconnect/foreground/poll (app.js applyRelaySnapshot). bookId FIELD: still SENT — src/directorRelaySync.js hard-codes bookId:'standard' in the payload (~line 117), and PdfReaderApp/DO still carry it — but it is NO LONGER READ for any routing/authz decision anywhere: worker /publish ignores it, web applyRelaySnapshot explicitly ignores snap.bookId (app.js ~726 'kept on the wire for build-373 compat'), native forces book='standard' on incoming mesh pages. `mode` is likewise always 'standard' (PdfReaderApp modeForBook). X-Hymnal is fully removed (no longer sent or read). /unlock is a no-op always-{ok:true,hymnal:'standard'} stub (index.ts:649-654). /health no longer echoes geo. So: bookId + mode + /unlock are vestigial/half-wired; page+seq+ts+auth are the only live semantics."

**New/unaudited surface:** "Four newly-written / substantially-rewritten regions are UNAUDITED (all post-dating the 370 audit) and deserve fresh hunting:\n\n1. DIRECTOR-ROLE STATE MACHINE (PdfReaderApp.tsx:499-549, onDirectorCode). Brand-new always-confirm flow: an unknown code injects role:'none'; a known code fires Alert.alert with a liveDirector-aware / super-admin-aware prompt and only becomes director in the onPress (line 545). Hunt: the liveDirector heuristic reads lastDirectorSnapshotRef.current (line 527) — is it reliably set/cleared? Race between the async Alert onPress and a concurrent mesh role change (roleGenerationRef). Can two devices both confirm and thrash the mesh? The confirm dialog can be dismissed while a becomeDirector from a prior tap is mid-flight. becomeDirector at PdfReaderApp.tsx:397 and becomeFollower at :368.\n\n2. NO-AUTO-DIRECTOR BOOT (PdfReaderApp.tsx:753-757 + becomeDirector persistence at :440-442). Boot now unconditionally becomeFollower(); lastSyncRole is written but never read for restore. Hunt: does anything still read STORAGE_KEYS.lastDirectorAt/activeBookId that no longer gets written (stale-read)? exit-director path at PdfReaderApp.tsx:646-660.\n\n3. SINGLE-BOOK WEB BOOT + REVEAL (app.js:871-1053 initReader, plus the thin liftGateNow/revealReader at :16-60 and the lone setTimeout(liftGateNow,4000) at :1147). The elaborate decode-guarded/floor/backstop reveal machine is gone; reveal is now immediate. Hunt: does removing the decode guard reintroduce the 'página no encontrada' alt-text flash on a slow first image? Is 4000ms backstop still needed / can boot throw before revealReader at :1032? applyRelaySnapshot (app.js:684-742) now ignores book entirely — verify an out-of-range director page just clamps and can't mis-navigate.\n\n4. REWORKED WORKER FETCH HANDLER (sync-worker/src/index.ts:499-700). The geo block is gone; the outer try/catch guard, /unlock stub (:649-654), and /publish auth (:656-700) are the live surface. Hunt: /unlock now returns ok:true to ANY caller (was a code oracle) — any downstream that still trusts it? bookId still stored by DO publish() (:128) — dead field but still slices to 64 chars, harmless. offlineBooks.ts getBook() ignores its arg (:offlineBooks.ts) — any caller passing a non-standard id expecting a different book?"

---

## 4. Disposition ledger — all original findings re-triaged

### relay-protocol

- **[CHANGED / critical]** `relay-director-webview-reload-broadcasts-boot-page` → PdfReaderApp.tsx:566
  - CHANGED: Now only ONE trigger remains (not two): a WebView reload/content-process-termination while the device is STILL a live director (roleRef==='director' or explicitTransmitterRef set). On such a reload the web re-boots to page 2 (relay.hasDirector always false in-shell), fires bridge-ready{page:2}; PdfReaderApp.tsx:566 overwrites currentPageRef to 2 and :584 broadcasts page 2 to every mesh follower + relay. The app-crash-then-relaunch variant is GONE because there is no auto-director restore anymore — a relaunched app is a follower and never broadcasts. Note the follower-resync branch at PdfReaderApp.tsx:585-596 correctly re-drives a reloaded FOLLOWER to lastDirectorSnapshotRef, so the bug is director-only.
  - FIX NOW: Same as audit part (a), scoped to the still-live director reload only: in the bridge-ready case (PdfReaderApp.tsx:564-598), when roleRef.current==='director' OR explicitTransmitterRef.current, do NOT adopt msg.page at line 566; instead inject {type:'sync-event',event:{type:'page',page:currentPageRef.current,book:currentBookRef.current}} to re-drive the freshly-reloaded web to the director's real last page (mirror of the follower branch at 596), and only resume normal page-changed adoption after the web echoes that page back. Part (b) of the original fix (reading sv.book.lastPage into currentPageRef in the boot effect for the relaunch-restore path) is no longer needed for the director path since relaunch never auto-becomes director — but note currentPageRef still defaults to 1 (PdfReaderApp.tsx:97), so if a human DOES re-enter the director code immediately after a relaunch, becomeDirector's broadcastPage at :445 publishes page 1 until the web's bridge-ready arrives; reading lastPage into currentPageRef in the boot effect would still harden that manual-re-promote path.
  - The primary mechanism SURVIVES verbatim. bridge-ready still adopts the web's boot page: PdfReaderApp.tsx:566 `if (typeof msg.page === "number") currentPageRef.current = msg.page;` then PdfReaderApp.tsx:582-584 `if (roleRef.current === "director") { // The director's own page is authoritative — just re-broadcast it. broadcastPage(currentPageRef.current, currentBookRef.current); }`. So a WKWebView content-process termination / bundleUpdated remount on a LIVE director iPad still clobbers currentPageRef with the web's boot render and re-broadcasts it to mesh + relay. BUT the audit's part-(b) app-relaunch half is now MOOT: the 24h auto-director-restore was DELETED (commit 38132dd3). Boot is now unconditional `becomeFollower()` at PdfReaderApp.tsx:757 (comment: 'NO auto-director: a device ALWAYS boots as a follower'), so there is no restore-window path that auto-becomes director and broadcasts page 1/2 on relaunch — a relaunched director comes back as a plain follower and cannot broadcast until a human re-enters + confirms the code. Also the audit's 'web always boots DEFAULT_START_PAGE=2 because relay.hasDirector is always false in-shell' still holds (DEFAULT_START_PAGE render at app.js:3278). The persisted sv.book.lastPage.<book> is STILL write-only: written at PdfReaderApp.tsx:611-614, and the ONLY reader of the lastPagePrefix is offlineBooks.ts:89's clearAllBookState (delete-only) — confirmed no read-back exists.
- **[SURVIVES_MOVED / high]** `relay-seq-guard-blocks-staleness-and-takeover-on-ws-path` → web/src/app.js:3033
  - FIX NOW: Fix is unchanged in substance; update the line anchors: in app.js applyRelaySnapshot, move the hasPublished + relayIsFreshLive evaluation (currently 3044) ABOVE the seq guard (currently 3033) so a stale duplicate still runs the demote branch; and for the regression case track relay.lastAppliedTs and, on an un-forced push with snap.seq < relay.lastSeq but fresh snap.ts >= lastAppliedTs, reset relay.lastSeq and apply. Keep the plain same-seq/same-ts duplicate drop so heartbeat replies stay cheap.
  - SURVIVES verbatim, only line numbers moved (3407 -> 3033). The seq guard still returns BEFORE the freshness gate: app.js:3033 `if (!force && Number.isFinite(snap.seq) && snap.seq > 0 && snap.seq <= relay.lastSeq) return;` precedes the demote branch at app.js:3044 `if (!hasPublished || !relayIsFreshLive(snap)) { relay.hasDirector = false; ... return; }`. Failure A (dead director never demoted over a healthy WS because ping-reply snapshots carry seq===lastSeq and are dropped at 3033) is intact — WS messages still arrive un-forced at app.js:3159. Failure B (worker accepts ANY seq after 90s → a clock-slow handoff director publishes seqs below the old one and every WS push is dropped for N seconds) is intact: worker takeover self-heal still present at sync-worker/src/index.ts:118-120 `const snapshotStale = this.snapshot.seq === 0 || nowSec - this.snapshot.ts > RELAY_LIVE_MAX_AGE_S; if (!snapshotStale && incomingSeq > 0 && incomingSeq <= this.snapshot.seq) return {ok,ignored}`, and seqs are still wall-clock ms via directorRelaySync.js:56-59 `seqCounter = Math.max(seqCounter + 1, Date.now())`. The manual ⟳ reset (relay.lastSeq=-1) is now at app.js:3010. Nothing about the refactor touched this logic; the removed cross-book layer was never part of this finding.
- **[SURVIVES_MOVED / high]** `relay-e2e-tests-publish-to-production-room` → e2e/relay-sync.test.mjs:17
  - FIX NOW: Unchanged: point ROOM at a per-run throwaway room `process.env.RELAY_E2E_ROOM || ('e2e-' + Date.now().toString(36))` (the worker ROUTE regex still accepts any [A-Za-z0-9_-]{1,64} room), keep at most one read-only prod smoke test (GET /health, GET /state), and drop the hardcoded 12345678840 in favor of an env TEST code. Line anchors: BASE/ROOM/CODE now at e2e/relay-sync.test.mjs:16-21.
  - SURVIVES. The suite still targets the PRODUCTION room with the still-valid legacy code and publishes ~15 real page flips. e2e/relay-sync.test.mjs:16-21 `const BASE = "https://signovivo-sync.4j4982y8jp.workers.dev"; const ROOM = "alvernia-main"; const PUB = ...; const CODE = "12345678840";`. Live publishes remain throughout, e.g. line 111 `publish({ v: 1, page: 42, totalPages: 371, seq, mode: "standard", bookId: "alvernia" })` and lines 120/129/141/151/159. package.json:17 still `"test:e2e": "node --test ./e2e/*.test.mjs"` with no room override. Miguel's own memory (feedback_never_run_full_e2e) independently confirms this suite flips live followers' pages. Note the payload's bookId:'alvernia' now resolves harmlessly to the single standard book (worker ignores bookId for routing), but each publish still stamps a fresh ts so the room reads LIVE for 90s and every signovivo.com follower force-follows the test pages — the fire-drill risk is unchanged.
- **[MOOT / n/a]** `relay-sion-code-cross-book-publish-into-live-room` → deleted
  - MOOT — the entire Sión-code + book-scoping machinery this finding targets was DELETED in the two-book refactor (commit 318d6f39). There is no SION_DIRECTOR_CODE anywhere in sync-worker/src/index.ts (grep: no match), and validTransmitterCodes(env) at index.ts:327 no longer seeds any public Sión code — it is purely env.TRANSMITTER_CODES. The old block `if (codeOk && !tokenOk && code === SION_DIRECTOR_CODE) { if (String(body.bookId) !== "hymns-4") return json({error:"sion_code_book_scoped"},403) }` is gone; the current publish handler explicitly documents the removal at sync-worker/src/index.ts:692 `// Single book: any valid transmitter code may publish page numbers. No book-scoping.` and just calls stub.publish(body) (:695). There is no hymns-4 book to switch to (assets/hymns-4.pdf deleted, app.js has no switchBook/bookFromSnap), so a 'cross-book flip' is impossible. The residual concern — 'any valid code may publish into the shared room' — still technically exists but is a DIFFERENT finding (relay-no-transmitter-identity) and no longer involves a book scope or a repo-printed public code. The audit's EVIDENCE lines (index.ts:793-799, app.js:3377-3378) no longer exist.
- **[SURVIVES_MOVED / high]** `relay-no-transmitter-identity-two-publishers-ping-pong` → src/directorRelaySync.js:56
  - FIX NOW: Unchanged in substance, line anchors updated: mint a txId (uuid) in setRelayPublishCode (directorRelaySync.js, near the code-setter) and include it in every payload (directorRelaySync.js:113-121); in SyncRoom.publish (sync-worker/src/index.ts:~115-135) store lastTxId and, while the snapshot is FRESH (nowSec - ts <= RELAY_LIVE_MAX_AGE_S), reject a publish from a different txId with 409 {error:'transmitter_conflict'} unless it carries an explicit takeover flag set by becomeDirector; surface the 409 through the existing latched relayAuthErrorHandler (directorRelaySync.js:30-32,89-92, extend from 401/403 to 409) so the losing device warns 'otro transmisor está activo'.
  - SURVIVES. The relay's only ordering primitive is still the wall-clock-ms seq, there is still no per-transmitter session id, and the worker still arbitrates purely on seq. directorRelaySync.js:56-59 `const nextSeq = () => { ... seqCounter = Math.max(seqCounter + 1, Date.now()); return seqCounter; };` — pure wall clock, device-agnostic. The payload at directorRelaySync.js:113-121 carries {v,page,totalPages,mode,bookId:'standard',seq,ts} with NO txId/session id. Worker still accepts any strictly-greater seq while live and returns {ok:true,seq} to both publishers (sync-worker/src/index.ts:120-135), so an interleaved-away publisher sees no error and the relay-auth latch (directorRelaySync.js:89-92, now 401/403 only) never fires. Two authorized publishers (e.g. a forgotten foregrounded transmitter-only iPhone on its 12s heartbeat + the real director) still ping-pong every signovivo.com follower with no arbitration. The refactor made this MORE reachable, not less: book-scoping is gone (index.ts:692), so ANY valid TRANSMITTER_CODES code can publish into the single shared room. The mesh still has token arbitration (unchanged) but it never reaches transmitter-only/out-of-range devices.
- **[MOOT / n/a]** `relay-ws-message-interleaved-apply-during-book-switch` → deleted
  - MOOT — the race depended entirely on applyRelaySnapshot awaiting a cross-book switchBook, which no longer exists. In current app.js, applyRelaySnapshot (app.js:3022-3075) has NO switchBook call and NO await on any book load — it is now synchronous-until-render single-book code. The header comment at app.js:3026-3028 states it outright: `// Single-book app: snap.bookId (kept on the wire for build-373 compat) is effectively always "standard", so there is no book to switch — the director's page renders straight against the one book. renderPage clamps any out-of-range page into range.` grep confirms switchBook/loadBook/state.currentBook are gone from app.js entirely. The WS message handler at app.js:3159 still calls applyRelaySnapshot un-serialized, but with no awaited switch inside there is no window for msg2 to interleave a half-applied switch and render against a stale totalPages. An out-of-range director page just clamps via renderPage (app.js:3071), which is the benign behavior the audit's fix aimed for. The audit's EVIDENCE (switchBook at 3378, state.currentBook=bookId at 1289, loadBook at 1291) no longer exists.
- **[MOOT / n/a]** `relay-unauthorized-follower-renders-wrong-song-live` → deleted
  - MOOT — this finding is entirely a two-book + geo-privacy artifact, all deleted. The app is now single-book and FULLY PUBLIC: everyone is served the same 'standard' Alvernia manual with no geo authorization. bookFromSnap, isDelRioAuthorized, and the `if (book === "standard" && !isDelRioAuthorized()) return null` privacy gate are GONE from app.js (grep: no matches). There is no hymns-4 book to mis-render a standard page number against — a follower and the director are always on the same single book, so a standard page N always renders standard page N. The audit's EVIDENCE lines (app.js:3357 privacy null, 3393 out-of-range skip) no longer exist. applyRelaySnapshot (app.js:3022-3075) applies snap.page directly against the one book and renderPage clamps out-of-range values (app.js:3071). No 'wrong Sión song under a green pill' is possible.
- **[SURVIVES_MOVED / medium]** `relay-polling-stopped-before-ws-opens-blind-windows` → web/src/app.js:3109
  - FIX NOW: Unchanged: remove stopRelayPolling() from connectRelay entry (app.js:3109) and instead stop polling inside the 'open' handler right after relayPollOnce(true) (app.js:3133), so /state polling continues uninterrupted until a socket is actually live. startRelayPolling is self-cleaning (app.js:3100 stops any prior timer) so no double-timer risk.
  - SURVIVES verbatim, lines moved (3585 -> 3109). connectRelay still calls stopRelayPolling() at entry: app.js:3108-3109 `relay.manualClose = false; stopRelayPolling();`. The zombie-CONNECTING guard is still a 6s timer that only restarts polling if the socket is still readyState 0 when it fires: app.js:3124-3127 `let connectTimer = setTimeout(() => { connectTimer = 0; if (ws.readyState === 0) { try { ws.close(); } catch {} startRelayPolling(); } }, 6000);`. The 'open' handler (app.js:3129-3158) does NOT call stopRelayPolling — but because polling was already killed at entry (3109), there is a dead window from connectRelay entry until 'open' or the 6s timer. The close handler still schedules a backoff reconnect (app.js:3181-3183) and only re-arms /state polling once backoff hits the 8000ms cap (app.js:3185), reproducing the repeating ~6s blind windows on WS-hostile networks. Untouched by the refactor.
- **[SURVIVES_MOVED / medium]** `relay-follower-clock-skew-defeats-freshness` → web/src/app.js:3018
  - FIX NOW: Unchanged: (a) worker — add `now: Math.floor(Date.now()/1000)` to the /state JSON response (sync-worker/src/index.ts getState/state handler, ~640-646); (b) web — in relayPollOnce (app.js:3083-3097) compute and store clockOffsetS = serverNow - Date.now()/1000, and change relayIsFreshLive (app.js:3018) to use (Date.now()/1000 + clockOffsetS) - snap.ts. Default offset 0 preserves current behavior before the first poll.
  - SURVIVES, lines moved (3333 -> 3018). relayIsFreshLive still compares the follower's raw wall clock to the server ts with no offset calibration: app.js:3018-3020 `const relayIsFreshLive = (snap) => snap && Number.isFinite(snap.seq) && snap.seq > 0 && (!snap.ts || (Date.now() / 1000) - snap.ts <= RELAY_LIVE_MAX_AGE_S);`. RELAY_LIVE_MAX_AGE_S=90 at app.js:2766. There is no server-time anchor anywhere client-side (grep for clockOffset/serverNow: none), and the worker /state response still exposes no `now` field (grep `now:` in index.ts: no match). A device whose clock is >90s fast evaluates every fresh snapshot as stale → applyRelaySnapshot's demote branch (app.js:3044) clears hasDirector on every push/poll → the device silently never follows. Inverse skew keeps a dead director 'live' longer. Untouched by the refactor.
- **[MOOT / low]** `relay-fleet-webcached-inflated-across-books` → web/src/app.js:2802
  - CHANGED: The audited cross-book path is deleted. Any residual over-report would now require a full-cached OLD cacheVersion coexisting with a partial NEW one (max-across-versions at app.js:2797-2802) — a separate stale-version concern, not the two-book contamination this finding described.
  - MOOT as described (the CROSS-BOOK inflation is gone). The finding's mechanism required TWO books sharing one pages cache: the always-precached hymns-4 Sión floor (51 pages) padding the count so a partially-cached standard manual self-reports fully cached. That precache is DELETED: grep for precachePublicFloor / floorBook / hymns-4 / PUBLIC_FLOOR in app.js returns NOTHING — single book only. hymns-4.pdf and its manifests were removed from assets. So the cache can only ever contain standard pages, and countCachedPageImages's unfiltered `keys.filter((r) => r.url.includes("/pages/")).length` (app.js:2802) can no longer mix a second book's pages into the count. The webCached self-report at app.js:2831 `webCached || (totalPages > 0 && pagesCached >= totalPages)` is therefore accurate for the single book. Residual nit (NOT the audited bug): the count still isn't book-id-scoped and takes max across cache versions, so a mid-upgrade device with a full old-version cache + partial new-version could still over-report — but that is a stale-cache-version concern, a different mechanism from the deleted cross-book inflation, and there is only one book so no cross-book contamination remains. The audit's EVIDENCE (precachePublicFloor guaranteeing 51 hymns-4 pages, app.js:3106-3121) no longer exists.
- **[CHANGED / low]** `relay-transmitter-only-role-lost-on-relaunch` → PdfReaderApp.tsx:405
  - CHANGED: The role loss on relaunch is now BY DESIGN and applies to every role, not just transmitter-only: after the no-auto-director change, a relaunched app (mesh or no-mesh) always boots as follower and never resumes publishing until a human re-enters + confirms the code (onDirectorCode Alert.alert at PdfReaderApp.tsx:539-547). The transmitter-only case is no longer a special stale-read bug — the mesh-effect bootstrap gate (`if (!syncAvailable) return` at PdfReaderApp.tsx:744) is moot because there is no restore path behind it to reach anymore. The residual real-world impact (a forgotten/relaunched transmitter iPhone silently stops publishing → followers demote after 90s with no banner) still exists but is now the accepted trade-off of the always-ask design.
  - FIX NOW: Do NOT apply the audit's persist-and-auto-restore fix — it contradicts the intentional no-auto-director design (commit 38132dd3, PdfReaderApp.tsx:753-757). If the silent-stop-on-relaunch UX is still deemed a problem, the design-consistent remedy is a NON-auto path: on boot, if a lastSyncRole==='director'/'transmitter' breadcrumb exists (PdfReaderApp.tsx:442), surface a one-tap 're-take director?' prompt (reusing the onDirectorCode Alert.alert confirm at :539-547) rather than silently re-publishing — keeping 'always ask, always' intact while alerting the operator that publishing stopped. A pure credential-persist auto-restore is off the table.
  - The observable behavior SURVIVES (a transmitter-only device that relaunches comes back as a plain follower and stops publishing) but the audit's FIX now DIRECTLY CONTRADICTS an intentional design change, so it cannot be applied as written. The transmitter-only branch still writes none of the restore keys: PdfReaderApp.tsx:405-412 `if (!syncAvailable) { explicitTransmitterRef.current = true; roleRef.current = "off"; injectEvent({type:"role",role:"director"}); broadcastPage(...); startDirectorHeartbeat(); return; }` — no AsyncStorage writes. AND, critically, auto-restore of ANY director/transmitter role was DELIBERATELY removed for all roles in commit 38132dd3: boot now unconditionally `becomeFollower()` at PdfReaderApp.tsx:751-757 with the comment 'NO auto-director: a device ALWAYS boots as a follower ... We never silently restore a stale director role'. DIRECTOR_RESTORE_WINDOW_MS / STORED_CODE_KEY / lastDirectorAt are gone (grep: no matches; STORAGE_KEYS now only has lastSyncRole + lastPagePrefix). becomeDirector persists ONLY a breadcrumb: PdfReaderApp.tsx:440-442 'We deliberately do NOT persist the director code or a timestamp — there is no auto-restore, so a credential must never sit in storage.' So the audit's proposed fix (persist STORED_CODE_KEY + auto-restore within DIRECTOR_RESTORE_WINDOW_MS) is now the OPPOSITE of the shipped intent and would re-introduce the exact auto-promote-a-stale-director behavior Miguel removed.
- **[MOOT / n/a]** `relay-unlock-swallows-failed-switchbook` → deleted
  - MOOT — the entire unlock-then-switchBook flow was deleted. unlockStandard, svStandardUnlock, standardUnlocked, relayGeoBookApplied, relayGeoBook, and switchBook are all GONE from app.js (grep: no matches). The /unlock endpoint itself is now a no-op always-ok stub with no code check and no book switch: sync-worker/src/index.ts:649-653 `if (action === "unlock") { // The app is now single-book and fully public — there is nothing to unlock. ... return json({ ok: true, hymnal: "standard" }, 200, cors); }`. There is no second book to fail switching to and no geo pin to freeze, so 'reports unlocked while still showing hymns-4' is impossible. The audit's EVIDENCE (app.js:3498-3506) no longer exists.
- **[MOOT / n/a]** `relay-browsing-user-book-switched-mid-browse` → deleted
  - MOOT — depends on a director cross-book switchBook ripping the book out from under a browsing follower, and there is no cross-book switch anymore. app.js has no switchBook (grep: no match); applyRelaySnapshot (app.js:3022-3075) never switches books. The browsing short-circuit still exists and is CORRECT for the single-book world: app.js:3060 `if (relay.browsing) { renderRelayPill(); revealReader(); return; }` — a browsing follower keeps their page on every push (there is no book to change), and goLive (app.js:2982-2990) renders relay.livePage against the one book. The audit's EVIDENCE (app.js:3377 cross-book switchBook before the browsing guard at 3434, startPage render + history wipe at 1302-1305, book-less livePage) no longer exists. The remaining same-book anti-strand force-follow behavior is intact and was never the bug.

### web-reader

- **[MOOT / n/a]** `web-reader-snapshot-during-switchbook-renders-old-totalpages` → web/src/app.js:3022 (applyRelaySnapshot, but the switchBook interleave it depended on is deleted)
  - The entire bug depended on switchBook() flipping state.currentBook synchronously before an awaited loadBook, so a second snapshot could render against the OLD book's totalPages. The two-book system was deleted wholesale: `git show 309a9afa:web/src/app.js | grep -c switchBook` = 6; at HEAD = 0. applyRelaySnapshot (app.js:3022) now has NO book-switch branch at all — the comment at app.js:3026-3028 states 'Single-book app: snap.bookId (kept on the wire for build-373 compat) is effectively always "standard", so there is no book to switch — the director's page renders straight against the one book. renderPage clamps any out-of-range page into range.' There is no await between the seq guard (line 3033) and renderPage (line 3071), so no cross-book interleave window exists. The wrong-totalPages/stranded-follower failure mode is gone with the two-book system.
- **[CHANGED / high]** `web-reader-unguarded-localstorage-kills-module-white-gate-forever` → web/src/app.js:331 and web/src/app.js:339
  - CHANGED: The throw at app.js:331 (or :339) during module evaluation aborts the rest of the script, so the module-tail `setTimeout(liftGateNow, 4000)` at app.js:3336 is never reached/scheduled, initReader never runs, and liftGateNow is never called from any path — the user is stranded behind the loading screen with no relay, no fleet check-in, and no error text, exactly as in build 370 but now via the 4s backstop rather than the deleted 8s/12s geo backstops.
  - FIX NOW: Same fix, updated line references: in web/src/app.js wrap both reads — `let hapticEnabled = true; try { hapticEnabled = localStorage.getItem(HAPTIC_PREF_KEY) !== "off"; } catch {}` at line 331, and `let tipDismissed = false; try { tipDismissed = localStorage.getItem(TIP_KEY) === "dismissed"; } catch {}` at line 339 (use tipDismissed for the classList toggles at 340-341). Also guard sessionStorage.getItem at app.js:2001 (and removeItem at :2006). A small safeStorageGet helper prevents regressions.
  - Both unguarded top-level localStorage reads SURVIVE verbatim, only moved: `let hapticEnabled = localStorage.getItem(HAPTIC_PREF_KEY) !== "off";` (app.js:331, was :436) and `if (localStorage.getItem(TIP_KEY) === "dismissed") {` (app.js:339, was :444) — both bare, no try/catch, executed at module evaluation. In a cookie-blocked browser these still throw SecurityError during module eval. The secondary unguarded read the finding mentioned also survives: sessionStorage.getItem(SW_RELOAD_FLAG) inside the controllerchange handler (app.js:2001, was :2314). What CHANGED is the failure geometry: in build 370 the throw prevented the 8s/12s geo-gate backstops (setTimeout(revealAfterGeoSettle,8000)/liftGateBackstop,12000) from being scheduled. Those backstops are DELETED (0 refs at HEAD). The new anti-trap is `setTimeout(liftGateNow, 4000)` at app.js:3336 — but it is ALSO a module-top-level statement AFTER lines 331/339, so a throw at 331 STILL prevents it from ever being scheduled. Net effect is identical: permanent blank screen, zero recovery, for a cookie-blocked congregation phone.
- **[MOOT / n/a]** `web-reader-persisted-standard-boot-reveals-hardcoded-hymns4-img` → web/src/index.html:54 (img now points at the correct single book)
  - The wrong-book flash required the hardcoded first-paint <img> to point at a DIFFERENT book than the one that gets resolved+revealed. In build 370 index.html hardcoded `src="books/hymns-4/pages/page-02.webp"` while a persisted-standard device resolved to the standard book — decode of the stale hymns-4 img lifted the gate onto the wrong book. At HEAD the img is `<img id="page-image" src="books/standard/pages/page-001.webp" ...>` (index.html:54) and BOOK_ID is hard-pinned to 'standard' (app.js:155) — first-paint img and the only book are the same, so there is no wrong book to flash. Additionally the whole decode-guarded reveal machine (tryLiftGate, precachePublicFloor, floorBookOnly, revealAfterGeoSettle) is deleted; reveal is now a plain revealReader()/liftGateNow (app.js:16, 3280) with no book-aware decode gate to fool. The persisted-standard-boot and the in-flight-snapshot variants both evaporate — there is only one book.
- **[SURVIVES_MOVED / medium]** `web-reader-live-img-error-retry-bypasses-requestid-guard` → web/src/app.js:21
  - FIX NOW: Unchanged fix, at app.js:24: before assigning, bail if the img moved on — `if ((pageImage.currentSrc || pageImage.src).split('?')[0] !== base) return;` and/or snapshot state.pageLoadRequest at error time and bail if it advanced. Keep the retry cap. Unify the retry token with pageFileUrl's '?reload='.
  - The top-level #page-image error handler SURVIVES verbatim, moved from :53-57 to app.js:20-25: `pageImage.addEventListener("error", () => { if (pageImgRetries++ >= 4) return; const base = (pageImage.currentSrc || pageImage.src).split("?")[0]; window.setTimeout(() => { pageImage.src = \`${base}?retry=${pageImgRetries}\`; }, 350 * pageImgRetries); });`. Still captures `base` at error time and re-commits it up to 1.4s later with NO consultation of state.pageLoadRequest and no check that pageImage.src still equals base. The downstream freeze the finding relied on also survives: applyRelaySnapshot at app.js:3071 still guards `if (state.currentPage !== snap.page) renderPage(...)`, so once a stale retry reverts the visible image to page A while state.currentPage=B, a same-page heartbeat won't re-render B — the follower shows the wrong hymn under a green pill until the director's next move. The '?retry=' vs pageFileUrl '?reload=' token divergence noted in the finding is also unchanged. The renderPage requestId guard it bypasses is likewise intact. Single-book refactor did not touch this path.
- **[SURVIVES_MOVED / medium]** `web-reader-theme-substring-shadows-text-search-santo` → web/src/app.js:1280 (searchByTheme substring) + web/src/app.js:1880-1884 (unconditional early return)
  - FIX NOW: Unchanged fix at app.js:1273-1286/1880-1884: prefer option (b) — run searchPages regardless and render theme results ABOVE text-search results (concatenate) so a theme hit never hides title/lyric hits; or require exact/word-boundary theme match (normLabel === norm, or word-split .some(w => w === norm) for norm.length>=4). Keep the emoji exact-match path.
  - Fully SURVIVES; only line numbers moved. searchByTheme (app.js:1273-1286, was :1590) still does substring match with first-match-wins: `if (normLabel.includes(norm) || normId.includes(norm) || norm === theme.emoji)` at app.js:1280. handleSearchInput (app.js:1863, was :2193) still gives it absolute precedence with an unconditional early return: `const themeResult = searchByTheme(query); if (themeResult) { renderThemeResults(...); return; }` at app.js:1880-1884 — searchPages never runs when a theme substring-matches. The shadowing data is intact: web/build.mjs:362-363 still defines `id: "espiritu_santo", label: "Espíritu Santo"` in the standard book, so query 'santo' → normalizeText('espiritu santo').includes('santo') → returns the Espíritu Santo theme's songs and hides all title/lyric 'Santo/Sanctus' matches (incl. Canto 371 'Santo Español', which has empty themes and cannot appear in that list). Single-book refactor left search untouched.
- **[SURVIVES_MOVED / medium]** `web-reader-browse-result-click-skips-relay-browsing-mode` → web/src/app.js:2498-2513 (searchResults click) vs web/src/app.js:1124-1129 (goToDraftSong browse block)
  - FIX NOW: Unchanged fix: extract goToDraftSong's post-jump relay block (now app.js:1124-1129) into a markIntentionalBrowse(targetPage) helper and call it from the searchResults click handler (app.js:2505 area) after renderPage(pageNum). Leave raw swipes/arrow-keys on peek-then-pull-home per the documented design.
  - Fully SURVIVES; moved. The searchResults click handler (app.js:2498-2513, was :2811-2825) still navigates with NO relay handling: `renderPage(pageNum); const songForPage = state.songIndex.find(...); if (songForPage) addToRecientes(songForPage.song); switchDrawerMode("browse");` — no relay.browsing/following/showGoLiveBar. goToDraftSong STILL sets browse mode on the numpad path: app.js:1124-1129 `if (relay.hasDirector && relay.livePage != null && targetPage !== relay.livePage) { relay.browsing = true; relay.following = false; showGoLiveBar(); renderRelayPill(); }`. The yank-back the finding relies on also survives verbatim: applyRelaySnapshot at app.js:3060 respects `if (relay.browsing) { renderRelayPill(); revealReader(); return; }` but line 3062-3071 otherwise re-renders the director's new page over a browsing follower who never got flagged. Same inconsistency, single-book unaffected.
- **[CHANGED / medium]** `web-reader-initreader-pagesjson-fetch-no-ok-no-retry` → web/src/app.js:3229-3231 (manifest acquisition) + web/src/app.js:3327-3331 (boot catch)
  - CHANGED: On a persisted/cold boot where #pages-data is absent (or a partial deploy serves an HTML error body), the fetch at app.js:3231 throws out of r.json(); initReader rejects; the catch at app.js:3327 shows 'No se pudo cargar Signo Vivo.' and reveals — but startRelayFollow (app.js:3292), deferOfflinePrecache (:3291), scheduleFleetCheckin (:3293), and renderActiveTab (:3296) are all AFTER the throw and never run. Because the geo re-heal poller was deleted in the refactor, no relay snapshots arrive at all, so the iPad shows an error overlay + cover with no follow and no self-recovery until manual reload.
  - FIX NOW: In web/src/app.js initReader: wrap manifest acquisition (app.js:3229-3231) in try/catch with (a) a response.ok check on the fetch branch, (b) fallback `state.totalPages = STANDARD_TOTAL_PAGES` on failure (NOT the deleted bookRegistry), and (c) a background retry of the pages.json fetch. Critically, hoist startRelayFollow/scheduleFleetCheckin/renderActiveTab above the fetch or into a try/finally so a manifest failure can never skip the relay follow — that regression is now worse than in build 370 since the geo re-heal safety net is gone.
  - Still applies but the mechanism and blast radius changed with the single-book refactor. The non-inline fetch STILL has no .ok check and no retry: `const manifest = inlinedPages ? JSON.parse(inlinedPages.textContent) : await fetch(resolveAppPath(\`/books/${BOOK_ID}/pages.json\`), { cache: "no-store" }).then((r) => r.json());` (app.js:3229-3231). A 404/HTML body still makes r.json() throw, initReader rejects, and the boot catch (app.js:3327-3331) only sets loading text + revealReader() — it STILL skips startRelayFollow, deferOfflinePrecache, scheduleFleetCheckin, renderActiveTab. HOWEVER two things changed: (1) the totalPages=1 clamp-forever half is much less likely — the primary boot path is now the INLINE #pages-data (app.js:3228-3230), and even the fetch fallback now has a hardcoded fallback `state.totalPages = STANDARD_TOTAL_PAGES;` (=371) at app.js:3238 for a NaN/missing manifestTotal (app.js:3234-3239). So a malformed-but-parseable manifest no longer sticks totalPages at 1; only a fetch/JSON THROW (network drop, non-JSON error body) does — and that throw bypasses the 3234-3239 fallback entirely, landing in the boot catch. (2) There is no bookRegistry to fall back to anymore, so the finding's suggested 'fallback to registry totalPages' fix is moot; the correct fallback is STANDARD_TOTAL_PAGES. Also the module-level geo re-heal that kept relay snapshots flowing is deleted — after a boot-catch throw, startRelayFollow never runs, so unlike build 370 NO relay snapshots arrive at all (relayPollOnce is only wired via startRelayFollow/initReader), making the dead-session worse, not better.
- **[PARTIALLY_MOOT / low]** `web-reader-loadsearchindex-stale-book-race-and-no-ok-check` → web/src/app.js:1153-1155
  - CHANGED: Only the response.ok gap remains: on a partial deploy, GET /books/standard/search-index.json returns a 200/404 HTML body; response.json() throws; the catch leaves state.searchIndexPages=[]; search shows no results until a tab re-activation re-fetches (which will keep failing until the deploy heals). No cross-book corruption is possible anymore.
  - FIX NOW: In loadSearchIndex (app.js:1152-1158) add `if (!response.ok) throw new Error('search-index ' + response.status);` before response.json(). The book/generation-guard portion of the original fix is no longer needed (single-book, constant BOOK_ID).
  - The stale-book-race half is MOOT: loadSearchIndex no longer captures a mutable state.currentBook in the URL — it uses the constant `BOOK_ID` (app.js:1153: `fetch(resolveAppPath(\`/books/${BOOK_ID}/search-index.json\`), ...)`). There is no switchBook and no hydrateBookData to clear searchIndexPages mid-fetch, so an in-flight fetch can never install the 'wrong book's' index — grep confirms 0 refs to switchBook/hydrateBookData at HEAD. The no-response.ok-check half PARTIALLY SURVIVES: app.js:1153-1155 still does `const response = await fetch(...); const data = await response.json(); state.searchIndexPages = data.pages || [];` with no `if (!response.ok)` guard, so a 404 HTML error body (partial deploy) still throws into the catch (app.js:1156-1158) and leaves searchIndexPages=[]. Recovery is also unchanged: activateTab retries only when the array is empty (app.js:2261 `if (isBuscar && !state.searchIndexPages.length) loadSearchIndex();`). Note an inline #search-data fast path was added (app.js:1140-1151) which is the common case and is try/guarded, further shrinking the exposed surface to the network-fallback branch only.
- **[SURVIVES_MOVED / low]** `web-reader-window-keydown-ignores-editable-targets` → web/src/app.js:2737-2749
  - FIX NOW: Unchanged fix at app.js:2737 (top of the window keydown handler, but after the songJumpOpen digit block at 2739): `const t = event.target; if (t && (t.tagName === 'INPUT' || t.tagName === 'TEXTAREA' || t.tagName === 'SELECT' || t.isContentEditable)) return;`. Also stopPropagation in the searchInput Escape branch.
  - Fully SURVIVES; moved from :3050-3062 to app.js:2737-2749. The window keydown handler still dispatches arrows with no event.target/isComposing check: `if (event.key === "Escape" && state.drawerOpen) { closeDrawer(); return; } if (event.key === "ArrowRight") turnSong(1); if (event.key === "ArrowLeft") turnSong(-1);` (app.js:2746-2748). turnSong still defaults keepOverlay=false → renderPage+clearDraft+closeDrawer (app.js:1890). The searchInput keydown handler still handles Escape/Enter/ArrowDown without stopPropagation. So arrows while typing in the search input or fleet-picker name field still turn the song and slam the drawer; Escape still double-fires. Untouched by the single-book refactor.
- **[MOOT / n/a]** `web-reader-hydratebookdata-keeps-numeric-prefetch-sets` → deleted (hydrateBookData removed; no book-switch reset point exists)
  - The entire bug is about a cross-book-switch stale prefetch: hydrateBookData cleared the URL-keyed prefetchedPageUrls but not the number-keyed state.prefetchedPages/prefetchingPages, so page numbers warmed in hymns-4 were skipped in standard. With the two-book system deleted there is no book switch and no hydrateBookData (`grep -c hydrateBookData web/src/app.js` = 0). The number-keyed sets (app.js:108-109) and prefetchSongPage's dedupe (app.js:661) still exist and are NEVER cleared anywhere — but that is now CORRECT behavior: a page warmed for the single 'standard' book stays validly warmed for the session; there is no other book whose identically-numbered pages could collide. No adjacent-song-prefetch loss can occur. MOOT.
- **[SURVIVES_MOVED / low]** `web-reader-rendersongitem-innerhtml-unescaped-ocr-titles` → web/src/app.js:2075-2080
  - FIX NOW: Unchanged fix at app.js:2067-2081: rebuild renderSongItem with createElement/textContent (mirror the safe makeSongButton pattern), or reuse esc() on title/song/key. Optionally add a build.mjs validation rejecting titles containing [&<>].
  - Fully SURVIVES; moved from :2385-2393 to app.js:2067-2081. renderSongItem still builds DOM via innerHTML with unescaped fields: `const keyBadge = song.key ? \`<span class="search-result-key">${song.key}</span>\` : ""; btn.innerHTML = \`...<span class="search-result-num">${song.song}</span> <span class="search-result-title">${song.title || \`Canción ${song.song}\`}</span> ${keyBadge}\`;` (app.js:2072-2080). No esc(). It's used by every browse tab (renderGroupedSongs/Recientes/Tono at app.js:2096,2125,2144,2170,2177,2200,2208). build.mjs still falls back to raw extractTitle OCR output when no override exists, so this remains a latent (not live — current shipped standard data has no &<>) unescaped trust boundary. The single-book refactor did not change the sink; the standard book is now the ONLY book feeding it, but the same OCR fallback pipeline supplies its titles.
- **[CHANGED / low]** `web-reader-draft-cap-blocks-legacy-11-digit-code` → web/src/app.js:1068 (10-digit cap) + web/src/app.js:623 (Number() strip) + web/src/app.js:1102-1116 (code routing)
  - CHANGED: On the NATIVE shell, entering the 11-digit legacy code 12345678840 on the WebView numpad truncates at app.js:1068 to 1234567880 (10 digits), then goToDraftSong posts that truncated string as the director-code to the native bridge (app.js:1108) — the code is rejected as invalid. On plain web the same truncation now merely yields 'Código no válido' (app.js:1113) with no unlock attempt, so the web blast radius is reduced to a confusing-but-inert error.
  - FIX NOW: In web/src/app.js raise the appendDigit cap (app.js:1068) to 11 (or 12) so the legacy code survives the numpad on the native path; and in goToDraftSong branch on the RAW draft (`const raw = state.songDraft.trim(); if (/^\d{5,}$/.test(raw)) { ...use raw as code... }`) before normalizeSongDraftNumber so leading zeros/exact digits reach postNativeBridge. The 'remove from web unlock' half is already done (unlockStandard deleted). Alternatively retire 12345678840 from TRANSMITTER_CODES and document 10 digits as the contract.
  - Both defects SURVIVE but the WEB-side consequence changed. appendDigit still hard-caps at 10: `if (state.songDraft.length >= 10) return;` (app.js:1068, was :1374). normalizeSongDraftNumber still Number()-parses the draft, stripping leading zeros: `const parsed = Number(trimmed);` (app.js:623). So the 11-digit legacy transmitter code 12345678840 is still truncated to 10 digits on the numpad, and a future leading-zero code is still corrupted. What CHANGED: unlockStandard and the web /unlock POST are DELETED. goToDraftSong (app.js:1096-1116) now routes a 5+ digit code ONLY to native via `postNativeBridge({ type: "director-code", code })` (app.js:1108) when hasNativeBridge/NATIVE_FILE_MODE; on plain WEB it just `flashSongDisplay("Código no válido", "err")` (app.js:1113) because 'the app is fully public and single-book — there is nothing to unlock'. So the truncation/leading-zero corruption is now HARMLESS on web (no code is honored there at all) but STILL BITES on the native shell: a director/sacristan typing the 11-digit legacy code on the native numpad loses the 11th digit before it reaches the director-code bridge. The Sión 1234567890 mention in the finding is moot (deleted).
- **[MOOT / n/a]** `web-reader-gate-caption-pingpong-and-false-offline` → deleted (revealAfterGeoSettle / liftGateBackstop / setGateOffline / floorBookOnly all removed)
  - The bug lived entirely inside the geo-gate reveal state machine, which is DELETED. `git show 309a9afa:web/src/app.js | grep -c 'floorBookOnly|revealAfterGeoSettle|liftGateBackstop|setGateOffline'` = 24; at HEAD = 0. There is no 8s revealAfterGeoSettle loop, no 12s liftGateBackstop loop, no setGateOffline caption toggling, no floorBookOnly, and no 'Sin conexión — reintentando…' caption in app.js at all. Reveal is now a single immediate liftGateNow (app.js:16) plus one belt-and-suspenders `setTimeout(liftGateNow, 4000)` (app.js:3336). No two competing 1.5s loops exist, so the false-offline caption and the caption ping-pong cannot occur. MOOT.
- **[SURVIVES_MOVED / low]** `web-reader-fullscreen-fab-noop-on-ios-pwa` → web/src/app.js:1917-1919 (dead pseudo-fullscreen branch) + web/src/app.js:162-163 / :1055 (fab still advertised)
  - FIX NOW: Unchanged fix: in updateFullscreenButton (app.js:1052-1062) drop canOfferPseudoFullscreen from the show condition — hide the fab unless `nativeFullscreenSupported && !NATIVE_FILE_MODE` — and delete the dead pseudo-fullscreen branch in toggleFullscreen (app.js:1917-1919). Keep the fab on desktop/Android where requestFullscreen works.
  - Fully SURVIVES; moved. canOfferPseudoFullscreen still forces supportsFullscreen true on an iOS standalone PWA: `const canOfferPseudoFullscreen = isIOS && isStandaloneApp; const supportsFullscreen = nativeFullscreenSupported || canOfferPseudoFullscreen;` (app.js:162-163, was :260-261). updateFullscreenButton still shows the fab whenever supportsFullscreen && !NATIVE_FILE_MODE (app.js:1055). toggleFullscreen's pseudo-fullscreen branch is still a dead no-op: `if (canOfferPseudoFullscreen) { ... return; }` (app.js:1917-1919, was :2230-2233). So a home-screen iPad PWA — the exact parish fleet config (isStandaloneApp is also what the fleet check-in reports as homeScreen at app.js:2834) — still shows a prominent ⛶ fab whose tap does nothing. Untouched by the single-book refactor.

### native-swift

- **[SURVIVES_MOVED / high]** `native-swift-peer-bundle-unauthenticated-exec` → ios/SignoVivo/DirectorSyncModule.swift:717 (handleBundleOffer); PdfReaderApp.tsx:923-926 (WebView flags), 48 (DIRECTOR_SESSION)
  - The refactor did NOT touch DirectorSyncModule.swift at all (git diff 309a9afa..HEAD shows 0 changes to the Swift file). handleBundleOffer still auto-requests any higher version with no auth: 'guard offered > mine else { return }; beginBundleTransfer(); ... type: "bundle_request"' (swift:717-729). MCSession still 'encryptionPreference: .none' (swift:125,1060,1278) and certificateHandler(true) unconditional (swift:1884). WebView still originWhitelist={["*"]} allowFileAccess allowFileAccessFromFileURLs allowUniversalAccessFromFileURLs (PdfReaderApp.tsx:923-926). DIRECTOR_SESSION='1234' still the public constant (PdfReaderApp.tsx:48). resolveBundleUri still prefers Documents/WebBundle forever (PdfReaderApp.tsx:713). The vector is fully intact; only the line numbers for the JS-side WebView flags moved (983-986 -> 923-926, 49 -> 48).
- **[SURVIVES_MOVED / high]** `native-swift-relay-split-brain-no-tiebreak` → src/directorRelaySync.js:56-59 (nextSeq), 112-123 (payload)
  - directorRelaySync.js changed 18 lines but ONLY de-Sionized comments and hard-coded bookId:'standard' — the split-brain mechanism is untouched. nextSeq is still pure wall-clock with no device id: 'seqCounter = Math.max(seqCounter + 1, Date.now()); return seqCounter;' (lines 56-59). The publish payload {v,page,totalPages,mode,bookId,seq,ts} (lines 112-123) still carries NO transmitter identity. Worker /publish now accepts ANY valid code (book-scoping removed) and still resolves by last-write seq, so two concurrent publishers still flap the web congregation. Relay heartbeat still gated only on local role (PdfReaderApp.tsx:351: 'roleRef.current !== "director" && !explicitTransmitterRef.current'), nothing coordinates across devices. NOTE: the specific trigger the finding named (24h auto-restore re-minting a second director) is now MOOT because auto-restore was deleted — but the finding's own body also names 'two directors briefly out of mesh range' and 'a transmitter-only device', which remain live, and a NEW live trigger exists: two people can both enter a valid code and both confirm the always-ask dialog (onDirectorCode/becomeDirector) with no relay tiebreak. Core defect and fix survive; only the prior:HIGH#2 line ref (57) maps to 56-59.
- **[SURVIVES_MOVED / high]** `native-swift-stale-documents-bundle-masks-update` → PdfReaderApp.tsx:713 (resolveBundleUri existence check), 905 (badge inject)
  - resolveBundleUri still prefers Documents/WebBundle by EXISTENCE only, no version compare: 'const info = await FileSystem.getInfoAsync(docIndex); if (info.exists) return docIndex;' (PdfReaderApp.tsx:712-713). Swift installReceivedBundle (swift:879) is unchanged and still only ever writes Documents/WebBundle; no native-build-change cleanup path exists. Badge still sourced from the native inject: 'window.__SIGNO_VINO_NATIVE_BUNDLE_VERSION = ${JSON.stringify(BUILD_VERSION)}' (PdfReaderApp.tsx:905), so a stale Documents bundle still self-masks. Line refs moved 711->707/713, 965->905. Fully applies.
- **[SURVIVES_MOVED / high]** `native-swift-webview-patch-silent-noop` → scripts/patch-rn-webview.js:45-46
  - The refactor did NOT touch scripts/patch-rn-webview.js (0 changes in git diff 309a9afa..HEAD). The silent no-op survives verbatim: 'console.warn("[patch-rn-webview] Pattern not found — react-native-webview may have changed. Skipping."); process.exit(0);' (lines 45-46), with 'const original = "if (request.URL.host) {";' (line 34). npm install still succeeds and a blank-app archive can still ship. Line moved 44 -> 45/46. Fully applies; entirely orthogonal to the two-book/geo refactor.
- **[SURVIVES_MOVED / high]** `native-swift-render-failed-sentinel-promote` → PdfReaderApp.tsx:687 (sentinel set), 445 & 410 (becomeDirector broadcastPage), src/directorRelaySync.js:114 & swift:662 (clamp to 1)
  - The -1 sentinel path is intact: 'if (roleRef.current === "follower") { currentPageRef.current = -1; }' (PdfReaderApp.tsx:686-687). becomeDirector still immediately broadcastPage(currentPageRef.current) with no sentinel re-check on BOTH paths: transmitter-only path line 410 and mesh-director path line 445. The clamps that turn -1 into page 1 both survive: directorRelaySync.js:114 'page: Math.max(1, Number(page) || 1)' and swift:662 'currentPageNumber = max(1, page.intValue)'. The exact live-handoff trigger (grab follower B, enter director code while A is gone, B's ref still -1) is unchanged and in fact MORE reachable now: the new always-ask flow means becomeDirector is still the promotion entry, and bridge-ready director re-broadcast (referenced as 569-571 in the finding) is now at line 584. Line refs moved 690->687, 449/412->445/410. Fully applies.
- **[SURVIVES / high]** `native-swift-dbglog-device-name-pii` → ios/SignoVivo/DirectorSyncModule.swift:177
  - Swift file untouched by the refactor. dbgLog still POSTs the real device name to the public /log endpoint: '"dev": localPeerID?.displayName ?? "?"' (swift:177), where displayName derives from UIDevice.current.name (swift:1054-1056). Still fired on found/lost/session/invite/watchdog throughout (swift:1630,1660,1698,1572,1276). The /log URL is unchanged (swift:184). Same line number, fully applies.
- **[SURVIVES_MOVED / medium]** `native-swift-onerror-no-recovery` → PdfReaderApp.tsx:929 (onError) vs 930-935 (onContentProcessDidTerminate recovery)
  - Unchanged behavior: 'onError={() => breadcrumb("webview-error")}' (line 929) still only breadcrumbs, while onContentProcessDidTerminate does the full recovery — 'webReadyRef.current = false; pendingInjectRef.current = []; webViewRef.current?.reload();' (lines 930-935). A failed first file:// load still leaves a permanent blank WebView with no retry. Line moved 989 -> 929. Fully applies.
- **[PARTIALLY_MOOT / low]** `native-swift-page-changed-clamp-stale-total` → PdfReaderApp.tsx:603-608
  - CHANGED: The residual theoretical defect: the clamp still reads the OLD totalPagesRef before adopting msg.totalPages, so the FIRST page-changed after totalPagesRef changes (e.g. bridge-ready set a wrong/zero total, or a bundle that reports a different totalPages than a prior render) is clamped against the prior value. But within a single 371-page book this is a no-op — totalPages is stable — so it is no longer a live production hazard, only a latent ordering smell.
  - FIX NOW: If retained purely as hardening: update totalPagesRef from msg.totalPages FIRST, then clamp against the new value (or clamp against Math.max(totalPagesRef.current, Number(msg.totalPages)||0)). The 'also carry totalPages in book-changed' half of the original fix is moot — that handler no longer exists.
  - The clamp-before-update ordering STILL EXISTS: 'let page = Math.max(1, Number(msg.page) || currentPageRef.current); if (totalPagesRef.current > 0) page = Math.min(page, totalPagesRef.current); currentPageRef.current = page; if (typeof msg.totalPages === "number" && msg.totalPages > 0) { totalPagesRef.current = msg.totalPages; }' (lines 603-608). HOWEVER the finding's actual TRIGGER is gone: it depended on the 'book-changed' handler updating currentBookRef/currentPageRef but NOT totalPagesRef, leaving a stale total across a hymns-4(51pp)->standard(371pp) switch. The 'book-changed' message case has been DELETED entirely (grep for 'book-changed' in PdfReaderApp.tsx returns only comment fragments; the switch has no 'book-changed' case), and the app is now single-book (modeForBook always 'standard', incoming mesh page forces book='standard' at line 765). With one book of fixed size 371, totalPagesRef is only ever set from that book's own page-changed/bridge-ready, so there is no cross-book stale-total window. The mis-clamp-on-first-turn-after-switch scenario cannot occur anymore.
- **[CHANGED / medium]** `native-swift-zombie-director-on-storage-failure` → PdfReaderApp.tsx:439-463
  - CHANGED: Trigger surface is smaller: a single setItem of a 'director' string is far less likely to throw than the old 3-key multiSet, but a full/corrupt AsyncStorage still rejects it. Sequence: startNearbyDirector succeeds -> roleRef='director' (439) -> await setItem throws -> catch -> wasFollower=false branch injects role:'none' (461) -> broadcastPage/startDirectorHeartbeat (445-446) never ran -> mesh advertises director, web shows 'código incorrecto', no heartbeat, followers get no snapshot. Note the generation guard at 443 ('if (myGen !== roleGenerationRef.current) return') sits BETWEEN the await and the broadcast, so even a non-throwing-but-superseded case skips the broadcast — but that path is intentional.
  - FIX NOW: Wrap the single setItem in its own try/catch that logs but does not abort, so injectEvent('director'), broadcastPage, and startDirectorHeartbeat at 444-446 always run once startNearbyDirector succeeds. On a genuine director-start failure, resetNearbyDirectorSync so no role-'director' advertiser is left with no heartbeat. (Same intent as the original fix; just applied to setItem instead of multiSet.)
  - Still a real bug, but the persistence call changed shape. Old code awaited AsyncStorage.multiSet([...]) (storing role + code + timestamp) at 442-446; the new code deliberately persists ONLY the role via a single setItem: 'roleRef.current = "director"; await AsyncStorage.setItem(STORAGE_KEYS.lastSyncRole, "director"); ... broadcastPage(...); startDirectorHeartbeat();' (lines 439-446). The zombie ordering hazard persists: roleRef is set to 'director' (439) and startNearbyDirector already ran (430), then the throwing await is at line 442, and broadcastPage/startDirectorHeartbeat are at 445-446 AFTER it. On a fresh numpad entry (wasFollower=false) a setItem rejection lands in the catch at 452 and hits the else branch 'injectEvent({ type: "role", role: "none" })' (461) — leaving roleRef='director' + advertiser running but with NO heartbeat and NO page broadcast (Swift still refuses to snapshot a nil-page director). Same silent-zombie outcome.
- **[SURVIVES_MOVED / medium]** `native-swift-bridge-ready-unclamped-total` → PdfReaderApp.tsx:566-567
  - FIX NOW: Validate in bridge-ready like page-changed: adopt msg.page only if finite and >=1, adopt msg.totalPages only if a positive integer; otherwise keep prior values. Since there is one fixed book (371pp), a cleaner fix is to derive totalPages from the known standard-book constant rather than trusting the web bundle at all.
  - bridge-ready still adopts msg.page and msg.totalPages with only a typeof check, no range/finite guard: 'if (typeof msg.page === "number") currentPageRef.current = msg.page; if (typeof msg.totalPages === "number") totalPagesRef.current = msg.totalPages;' (lines 566-567), in contrast to the page-changed clamp at 603-604. A tampered/peer-pushed bundle reporting totalPages=0 disables the page-changed clamp (the '> 0' guard skips) letting a wild page propagate to mesh followers + relay; a bogus small value caps legit turns. This is arguably MORE relevant now given the still-live unauthenticated peer-bundle-exec vector (finding #1) that can supply exactly such a malicious bundle. Line moved 553-554 -> 566-567. Fully applies (the 'trust a per-book registry' half of the fix is now trivially just the single standard book's total).
- **[MOOT / n/a]** `native-swift-24h-restore-remints-stale-director` → deleted
  - The entire 24h auto-director-restore was DELETED in the refactor. Build 370 had 'const recentlyDirector = lastRole === "director" && Date.now() - lastAt < DIRECTOR_RESTORE_WINDOW_MS' then 'await becomeDirector(storedCode)' (370 PdfReaderApp.tsx:771-777) with DIRECTOR_RESTORE_WINDOW_MS=24h (370:64) and STORED_CODE_KEY. Grep for DIRECTOR_RESTORE_WINDOW / recentlyDirector / STORED_CODE_KEY in current PdfReaderApp.tsx returns NOTHING. The bootstrap now unconditionally boots as follower: 'if (!didBootstrapRef.current) { didBootstrapRef.current = true; ... becomeFollower(); }' (lines 751-757), with an explicit comment 'NO auto-director: a device ALWAYS boots as a follower.' becomeDirector now persists ONLY lastSyncRole as a breadcrumb, never the code or timestamp (line 442, comment at 440-441). A stale director can no longer be re-minted on relaunch — this exact split-brain trigger is eliminated. (The residual relay-no-tiebreak defect it fed into lives on under finding native-swift-relay-split-brain, via other triggers.)
- **[MOOT / n/a]** `native-swift-geo-overrides-chosen-book` → deleted
  - The geo book-selection effect and storedBookRef were DELETED wholesale. Build 370 had GEO_STATE_URL (370:46), storedBookRef (370:103,611,738,885), and the async geo effect whose post-await guard re-checked lastDirectorSnapshotRef but not storedBookRef (370:899-901). Grep for GEO_STATE_URL / storedBookRef / geoBook in current PdfReaderApp.tsx returns NOTHING. The app is single-book: boot hard-sets 'const startBook: BookId = "standard"' (line 728), modeForBook always returns 'standard' (line 77), isBookId only accepts 'standard' (line 74), and incoming mesh pages force book='standard' (line 765). There is no geo fetch, no book choice, and no storedBookRef to race — the asymmetric-guard override cannot occur. The finding's own body already flagged it was 'largely mitigated... book is geo-locked'; the refactor removed the machinery entirely.

### offline-pwa

- **[SURVIVES_MOVED / high]** `offline-pwa-cacheversion-misses-book-content` → web/build.mjs:22
  - FIX NOW: Same fix, minus the deleted hymns-4 inputs: compute cacheVersion AFTER the (now single) buildBook runs, hashing dist/books.json + dist/books/standard/pages.json + dist/books/standard/search-index.json + a digest of rendered page files (or hash assets/alvernia_manual_2.pdf + src/alverniaManual2SongIndex.js) + PDF_RENDER_DPI/WEBP_QUALITY. Move the __CACHE_VERSION__ token replacement (build.mjs:49,59) to after buildBook at line 655. Keep hashing the 5 shell files too.
  - The refactor did NOT touch cacheVersion. build.mjs:10-28 still computes cacheVersion = gitShortSha + sha256 over ONLY the 5 shell files (build.mjs:22: `for (const f of ["app.js", "sw.js", "styles.css", "index.html", "manifest.webmanifest"])`) plus build.mjs itself (line 26). Book inputs (assets/alvernia_manual_2.pdf, src/alverniaManual2SongIndex.js) and render knobs PDF_RENDER_DPI (line 91)/WEBP_QUALITY (line 92) are still NOT hashed. The false comment survives verbatim at build.mjs:16-20 ('guarantees the cache busts whenever the content changes'). cacheVersion is still computed at line 10, BEFORE buildBook runs at line 638 (so hashing built outputs would still require moving the token replacement). The concrete failure survives: applyRelaySnapshot (app.js:3071) renders snap.page through renderPage→clampPage (line 223/954), which clamps against a STALE state.totalPages, misrouting a jump to a newly-added song. The hymns-4 half of the finding (assets/hymns-4.pdf, hymns-4-*.json) is deleted, but the standard-book half is fully intact — the fire-drill risk is unchanged since the app is now single-book and that ONE book is exactly the one being served live at Mass.
- **[SURVIVES_MOVED / high]** `offline-pwa-immutable-page-bytes-stale-forever` → web/src/sw.js:184
  - FIX NOW: Same content-addressable-directory fix, simplified for one book: build.mjs emits pages under books/standard/pages-<8-hex content hash>/page-NNN.webp, publishes the hash in books.json + inline #pages-data, and app.js pageFileName() reads it. Update _headers glob to /books/*/pages-*/* and extractCachedPageNumber regex (app.js:479). floorPageFileName no longer exists, so only pageFileName needs updating.
  - The self-poisoning old-cache-first + 1-year-immutable + cross-cache-seeding mechanism is fully intact. sw.js page handler still consults its own PAGE_CACHE (line 178), then falls back across previous-version page caches via matchAnyPageCache (line 184) BEFORE the network fetch (line 187) — even online. build.mjs:695 still writes `/books/*/pages/*  Cache-Control: public, max-age=31536000, immutable`. The refill is still self-poisoning: app.js cacheSinglePage (line 510) `fetch(url, { cache: "no-store" })` from an SW-controlled page is intercepted by the same handler, which serves OLD-cache bytes and cache.put()s them into the NEW PAGE_CACHE (line 192). The immutability assumption comment survives at sw.js:6-13. This is single-book now, but the standard manual is exactly the book that gets revised in place (extractTitle 'Rev' stripping) — an in-place chord fix on a page still never reaches returning followers. Fully survives; hymns-4 is irrelevant to the mechanism.
- **[CHANGED / high]** `offline-pwa-fleet-webcached-false-green` → web/src/app.js:2831
  - CHANGED: Two surviving false-green paths post-refactor: (1) sticky OFFLINE_READY_KEY==='ready' survives CacheStorage eviction (unchanged from 370); (2) countCachedPageImages returns max across page caches, so a retained full OLD-version cache masks a partial NEW-version precache. The cross-BOOK inflation is gone (single book, no floor), but stale-cache inflation persists.
  - FIX NOW: Compute webCached live and per-version in fleetCheckin: reuse getCachedPageSet/extractCachedPageNumber (already book-scoped to BOOK_ID at app.js:476-492) against the CURRENT PAGE_CACHE only (or the union, but not a cross-cache max), and set webCached = (currentPageSet.size >= totalPages). Drop the sticky OFFLINE_READY_KEY from the check-in. Replace countCachedPageImages' `max` over `keys.filter(url.includes('/pages/'))` with the book-scoped set count.
  - Sticky-key leg SURVIVES; cross-book-counting leg is MOOT. fleetCheckin (app.js:2811-2849) still computes webCached = `localStorage.getItem(OFFLINE_READY_KEY) === 'ready'` (line 2822) `|| (totalPages > 0 && pagesCached >= totalPages)` (line 2831). OFFLINE_READY_KEY is still set once per cacheVersion at app.js:544 and never re-verified — isOfflineBundleReady (line 579) still has ZERO callers, so the sticky false-green after CacheStorage eviction is fully live. The specific '320 standard + 51 floor = 371' cross-book inflation is MOOT: precachePublicFloor is deleted and there is only one book. HOWEVER countCachedPageImages (app.js:2793-2808) STILL takes `max` across ALL page caches and counts EVERY '/pages/' entry (line 2802) with no scoping — and old full page caches are retained (PAGE_CACHES_TO_KEEP=2, sw.js:14). So a partial NEW-version precache (e.g. 320/371) plus a retained FULL old-version cache still yields max=371 → false-green. Note getCachedPageSet/extractCachedPageNumber (app.js:476-492) ARE now book-scoped to BOOK_ID, but countCachedPageImages does NOT use them — it uses the crude keyword filter, so the count is still unreliable.
- **[SURVIVES_MOVED / medium]** `offline-pwa-partial-install-wedges-updates` → web/src/sw.js:135
  - FIX NOW: Same fix: in the SKIP_WAITING handler (sw.js:135), when isShellCached() is false and !isFirstInstall(), re-run the CORE_ASSETS allSettled precache into STATIC_CACHE, then call skipWaitingIfShellReady() again.
  - Unchanged by the refactor (delta map: sw.js changes were comment-only). Install runs the allSettled precache once then `.then(() => skipWaitingIfShellReady())` (sw.js:83). The SKIP_WAITING message handler (sw.js:131-137) still ONLY re-runs skipWaitingIfShellReady() (line 135) — it never re-attempts the CORE_ASSETS precache: `if (event.data?.type === "SKIP_WAITING") { event.waitUntil(skipWaitingIfShellReady()); }`. app.js still posts SKIP_WAITING (worker.postMessage at app.js:1937), so the client-side retry hook exists but the SW never refills the cache. A partial shell-less install stays WAITING until the next byte-changed sw.js deploy. Fully survives.
- **[SURVIVES_MOVED / medium]** `offline-pwa-page-cache-eviction-by-recency` → web/src/sw.js:108
  - FIX NOW: Same: in activate (sw.js:101-127), count cache.keys().length per page cache and additionally retain the single largest cache if not already kept (cap at 3), or only delete an old cache once the union of retained caches covers it.
  - Unchanged. activate still keeps caches by pure recency: sw.js:108 `const pageCachesToKeep = new Set([PAGE_CACHE, ...pageCaches.slice(-PAGE_CACHES_TO_KEEP)])` (PAGE_CACHES_TO_KEEP=2, line 14), deleting everything else (line 115). Newness still != completeness — an interrupted midweek refill can leave the immediately-previous cache partial, and activate can delete the older FULL cache. The refill still only runs while the tab is open, ≥4s after gate-lift (deferOfflinePrecache SETTLE_MS=4000, app.js:562-576). matchAnyPageCache (sw.js:152) still serves any surviving cache; absent pages still need the network (sw.js:187). Book-agnostic mechanism, fully survives.
- **[SURVIVES_MOVED / medium]** `offline-pwa-query-string-offline-navigation-fails` → web/src/sw.js:219
  - FIX NOW: Same: in the SWR branch (sw.js:219) match with { ignoreSearch: true }, normalize the put key to bare pathname (mirror the page handler's Request at sw.js:191), and add a navigate-mode fallback returning caches.match('/index.html', { ignoreSearch: true }) when cache+network both fail.
  - Unchanged. The SWR branch still matches query-SENSITIVE: sw.js:219 `caches.match(event.request).then((cached) => cached || revalidate.then((response) => response || fetch(event.request)))` — no { ignoreSearch: true }, and no navigate-mode fallback to /index.html anywhere. NETWORK_FIRST_PATHS is keyed on requestUrl.pathname (sw.js:205), so '/?fbclid=...' enters this branch and misses the bare '/' precached at sw.js:23-24. The redundant put of query-variant duplicates persists (sw.js:210 `cache.put(event.request, ...)`). An offline navigation with a click-ID param still yields the browser offline error page despite a fully-cached app. Fully survives.
- **[SURVIVES_MOVED / medium]** `offline-pwa-unguarded-localstorage-kills-boot` → web/src/app.js:331
  - FIX NOW: Same: `let hapticEnabled = true; try { hapticEnabled = localStorage.getItem(HAPTIC_PREF_KEY) !== "off"; } catch {}` at app.js:331; guard the TIP_KEY read at 339; wrap the OFFLINE_READY_KEY setItem at 544 in try/catch. A safeStorage helper prevents recurrence.
  - Survives, still severe. app.js:331 `let hapticEnabled = localStorage.getItem(HAPTIC_PREF_KEY) !== "off";` and app.js:339 `if (localStorage.getItem(TIP_KEY) === "dismissed")` are still unguarded at module-eval time, and app.js:544 `localStorage.setItem(OFFLINE_READY_KEY, "ready")` is an unguarded write inside ensureOfflineBundle (before writeOfflineMetadata/fleetCheckin at 545-550). A SecurityError from block-all-cookies still throws during module evaluation. The white-gate consequence SURVIVES with a twist: the anti-trap backstop `setTimeout(liftGateNow, 4000)` is now at the module TAIL (app.js:3336) — a throw at line 331 aborts the module long before that setTimeout is scheduled, so the eternal white geo-gate (index.html:44, still shipped) is exactly as reachable as in build 370. The finding is fully live.
- **[MOOT / n/a]** `offline-pwa-persisted-standard-reveal-flash` → deleted
  - MOOT — the entire wrong-book-flash mechanism was deleted by the 318d6f39/39b60313 refactor. The finding depended on index.html hardcoding `books/hymns-4/pages/page-02.webp` while a returning standard device resolved to 'standard'; the reveal (tryLiftGate decode-guard) would lift on the hymns-4 floor image. In build 374: (1) index.html:54 first-paint img is now `src="books/standard/pages/page-001.webp"` — the ONLY book; (2) there is no book resolution (BOOK_ID='standard' const, app.js:155), no floor, no precachePublicFloor, no initialBookIsKnown/resolveInitialBook; (3) tryLiftGate/decode-guard is deleted — reveal is now immediate via liftGateNow (app.js:11-16) called synchronously in initReader (app.js:3280) and a 4s backstop (app.js:3336). There is no second book to flash to. The 'never flash the wrong manual' invariant is trivially satisfied because only one manual exists.
- **[SURVIVES_MOVED / medium]** `offline-pwa-mid-mass-deploy-force-reload` → web/src/app.js:1999
  - FIX NOW: Same: in the controllerchange handler (app.js:1999), if relayIsFreshLive() && relay.following, set a pendingReloadForUpdate flag instead of reloading; execute the reload on the hasDirector=false transition in applyRelaySnapshot (app.js:3022), on visibilitychange→hidden, or after a hard cap. Keep instant reloads for idle tabs.
  - Survives (line moved from 2320 to 1999). The controllerchange handler (app.js:1999-2008) still unconditionally `window.location.reload()` (line 2007); the only guards are hasReloadedForUpdate (line 2000) and flagged/hadControllerAtBoot (line 2004) — there is NO 'am I actively following a live director?' check. The auto-update pipeline is intact: registration.update() polling (refreshRegistration at app.js:1958, online-hook at 1959) and app.js posts SKIP_WAITING (app.js:1937). The relay-liveness signals needed for the fix still exist: relayIsFreshLive (app.js:3018) and relay.following (app.js:2983/3008/3069). A deploy mid-Mass still blanks every online follower to the (now single-book) loader and re-boots. The consequence is milder than 370 (no geo re-resolution — reveal is immediate), but the mid-hymn blank + relay re-join still happens. Survives.
- **[PARTIALLY_MOOT / low]** `offline-pwa-precache-no-in-session-retry` → web/src/app.js:567
  - CHANGED: Only the ensureOfflineBundle abort-on-first-failure path remains: cacheSinglePage throws → deferOfflinePrecache catch resets offlinePrecacheStarted (app.js:567-570) but no online-event/timer ever re-enters the kick. The floor-precache silent-holes sub-case no longer exists.
  - FIX NOW: Add a bounded in-session retry for ensureOfflineBundle failure: on the deferOfflinePrecache catch (app.js:567), schedule a re-kick on window 'online' + a 60s backoff timer (max ~5 attempts) that re-runs the kick path. Optionally make cacheSinglePage tolerate isolated 404s (skip+record) instead of aborting the remaining pages. Drop the precachePublicFloor guidance — that code is gone.
  - ensureOfflineBundle half SURVIVES; precachePublicFloor half is MOOT. cacheSinglePage still throws on the first !response.ok page (app.js:511-513), rejecting the whole bundle; deferOfflinePrecache's catch sets offlinePrecacheStarted=false 'to let a later trigger retry' (app.js:568) — but deferOfflinePrecache is called exactly ONCE (app.js:3291) and nothing re-kicks it: the two window 'online' listeners (app.js:1959 SW-refresh, app.js:3205 relay-reconnect) don't touch the precache, and there is no timer/backoff. So one transient failure still strands a partial bundle for the whole session. The precachePublicFloor half of the finding (per-page failures swallowed by `catch {}`, publicFloorPrecacheStarted stuck true) is MOOT — precachePublicFloor and the floor concept are deleted. Combined with the surviving fleet-webcached false-green, the partial bundle is still invisible on the dashboard.
- **[SURVIVES_MOVED / low]** `offline-pwa-global-cache-match-version-mix` → web/src/sw.js:59
  - FIX NOW: Same: scope shell lookups to STATIC_CACHE — `const cache = await caches.open(STATIC_CACHE); const cached = await cache.match(event.request, { ignoreSearch: true })` in both the SWR (sw.js:219) and cache-first (sw.js:227) branches, falling back to global caches.match only when the network is also dead. Leave the page-image branch's cross-version fallback as-is.
  - Unchanged. Both the SWR branch (sw.js:219) and the generic cache-first branch (sw.js:227) still use GLOBAL caches.match (not scoped to STATIC_CACHE), scanning oldest-first. activate still RETAINS the old static cache when the new shell is incomplete (sw.js:116-119, `key !== STATIC_CACHE && shellReady`). isFirstInstall (sw.js:59-62) still reads 'no older static cache' as first install, so an offline-partial install can activate after eviction. In the retained-old-cache state a client can still be served OLD index.html (old inline #pages-data/totalPages) alongside NEW app.js — version-skewed shell. Book-agnostic mechanism, fully survives.
- **[SURVIVES_MOVED / low]** `offline-pwa-dead-offline-gate-ui` → web/src/app.js:446
  - FIX NOW: Same two-part fix: (1) delete the dead #offline-gate DOM (index.html:60-70), setOfflineGateState + its 2 call sites, LOADING_PHRASES/start/stopLoadingPhrases, formatVerifiedAt; (2) repurpose isOfflineBundleReady (app.js:579) as an on-device status line ('Offline listo: 371/371' vs 'incompleto: 320/371') using the book-scoped count from the fleet-webcached fix. Note: no floor/hymns-4 to account for now — the union collapses to one book.
  - Survives. setOfflineGateState (app.js:446-474) is still only ever called with { visible: false } — now at app.js:2290 and 3270 (one of the three original sites was removed but zero pass a truthy visible), so the entire #offline-gate dialog (index.html:60-70), spinner/progress/ready/retry buttons, LOADING_PHRASES, showAdminNote/canTestOffline, formatVerifiedAt remain unreachable. isOfflineBundleReady (app.js:579) — the only real cache-vs-totalPages verifier — still has ZERO callers. The dead #offline-admin-note 'No entregues este iPad hasta ver Offline listo' still ships (index.html:67) and #offline-test-button (index.html:70) is still referenced nowhere. Still ~200 lines of dead UI + no on-device offline-readiness signal. Survives.
- **[SURVIVES_MOVED / low]** `offline-pwa-unawaited-sw-async` → web/src/sw.js:128
  - FIX NOW: Same: move claim inside the activate waitUntil body — `await self.clients.claim();` after the cache cleanup (sw.js:125). In the page fetch handler, capture the put promise and `event.waitUntil(putPromise.catch(() => {}))` so the SW stays alive until the write lands.
  - Unchanged. self.clients.claim() at sw.js:128 still runs OUTSIDE event.waitUntil — the activate waitUntil closes at sw.js:127 and claim() is the next statement, its promise dropped. The page handler's cache.put (sw.js:192, `cache.put(cacheKey, response.clone())`) is still not tied to the event lifetime via event.waitUntil, so an iOS SW kill after respondWith settles but before the put persists silently drops that page from the offline bundle. Both are the exact unguarded-async class. Book-agnostic, fully survives.

### security-privacy

- **[PARTIALLY_MOOT / high]** `secpriv-committed-live-transmitter-code` → e2e/relay-sync.test.mjs:21
  - CHANGED: A committed code (12345678840) that is still in the live TRANSMITTER_CODES secret is a working /publish credential: POST /r/alvernia-main/publish with X-Director-Code:12345678840 drives every signovivo.com follower to any page (page+seq are the only live semantics now; book/mode/unlock are vestigial). The private-manual angle is gone (public single book, /unlock is a no-op).
  - FIX NOW: Rotate 12345678840 OUT of the TRANSMITTER_CODES secret NOW (still the load-bearing action). Change the e2e suite to a dedicated env-injected test-only code (process.env.SV_TEST_CODE) against a throwaway room, never alvernia-main. Drop the '/unlock reads the private manual' language from the writeup — that path is now a no-op. Longer term move to opaque per-director tokens.
  - The committed constant SURVIVES verbatim: e2e/relay-sync.test.mjs:21 `const CODE = "12345678840";`, and the suite still publishes it LIVE to the prod room (ROOM="alvernia-main" at :17; publish() asserts 200/ok at :111-114). Worker auth is UNCHANGED in mechanism: index.ts:665-668 `const code = (...X-Director-Code...).replace(/[^0-9]/g,''); const codeOk = code.length>0 && validCodes.has(code)`. BUT two of the finding's three legs changed. (1) validTransmitterCodes no longer unions SION_DIRECTOR_CODE — it is now PURELY env.TRANSMITTER_CODES, comma-split, fail-closed (index.ts:327-336). So whether 12345678840 is live depends ENTIRELY on the secret (the audit's own read-only caveat). (2) The book-scoping the finding leaned on is GONE: index.ts:692 comment 'Single book: any valid transmitter code may publish page numbers. No book-scoping.' — so the 'NOT book-scoped, can publish any bookId' distinction the finding drew between 12345678840 and the Sión code is moot; ALL codes are now unscoped, and bookId is vestigial (still stored at index.ts:128 but read by no route). (3) The '/unlock to render the private manual from any network' half is FULLY MOOT: /unlock is now a no-op always-ok stub (index.ts:649-654 `return json({ ok: true, hymnal: 'standard' }, 200, cors)`) and the manual is public single-book. NET: the Mass-hijack-via-publish leg SURVIVES (if the code is still in TRANSMITTER_CODES it can drive the live congregation to any page); the private-manual-read leg is MOOT; grep confirms 12345678840 now appears ONLY in the e2e test (the stale worker comment at old index.ts:22 was removed in the refactor).
- **[CHANGED / high]** `secpriv-publish-flood-no-ratelimit` → sync-worker/src/index.ts:656
  - CHANGED: No rate limit + seq=0 bypass still let any holder of a live transmitter code flood /r/alvernia-main/publish and drag every web follower to attacker-chosen pages of the single standard manual mid-hymn (each seq=0 packet gets a fresh higher server seq so it also clears the follower's seq guard at app.js:3033). The cross-BOOK dimension is gone — it is now a page-flood within one book, not a book-switch flap.
  - FIX NOW: Rate-limiting (per-IP + per-code token bucket in the DO, or a Cloudflare Rate Limiting rule on the route) is still the load-bearing fix. Bind publish to an authenticated director session to stop interleave. DROP the 'per-book rooms' / 'reject bookId mismatch' recommendations entirely — there is one book and one room now; bookId is vestigial (index.ts:128), so a room→book binding is meaningless. Requiring monotonic seq even at seq=0 (reject seq<=current unless snapshot is stale) would blunt the seq=0 flood specifically.
  - The core rate-limiting gap SURVIVES: still zero rate limiting / lockout / per-code throttle anywhere in index.ts, and /publish auth is unchanged (index.ts:656-671). The seq=0 bypass ALSO survives verbatim: DO publish() guard at index.ts:120 only fires `if (!snapshotStale && incomingSeq > 0 && incomingSeq <= this.snapshot.seq)`, and :129 assigns `incomingSeq > 0 ? incomingSeq : this.snapshot.seq + 1` — a seq=0/omitted publish always writes (e2e still proves this). BUT the finding's headline harm — the hymns-4 cross-book FLAP ('20 publishes/sec of {bookId:hymns-4,page:51}' yanking standard followers via switchBook) — is FULLY MOOT. The refactor deleted the whole two-book layer: web applyRelaySnapshot (index.ts... app.js:3022-3075) NO LONGER calls switchBook/bookFromSnap; it comments 'snap.bookId (kept on the wire for build-373 compat) is effectively always standard' (app.js:3026) and just renders the page via renderPage → clampPage (app.js:954,223). The enabling public Sión code (1234567890) is deleted everywhere. So the residual attack is NOT a book-flap; it is a same-book page-flood: an attacker with a live code (see the committed-code finding) spamming seq=0 publishes holds/thrashes the congregation on attacker-chosen PAGES of the one book, faster than the director's heartbeat.
- **[MOOT / n/a]** `secpriv-unlock-brute-force-oracle` → sync-worker/src/index.ts:649
  - The entire /unlock code-oracle was GUTTED. index.ts:649-654: `if (action === 'unlock') { ... return json({ ok: true, hymnal: 'standard' }, 200, cors); }` — a trivial always-ok stub. The comment states 'The app is now single-book and fully public — there is nothing to unlock ... kept as a trivial always-ok so old clients that still call it don't error. No geo, no code check needed.' There is no longer any code comparison, no 200/403 yes/no, no `code !== SION_DIRECTOR_CODE && validTransmitterCodes(env).has(code)` at all — so the brute-force ORACLE the finding targets no longer exists (unlock returns ok:true to everyone regardless of code, revealing nothing about validity). The residual 'low-entropy numeric codes on the unthrottled /publish endpoint' smell is real but that is now covered by secpriv-publish-flood/secpriv-committed-code, not by /unlock. The deletion also makes the /unlock brute-force a non-issue by construction: there is nothing to brute-force.
- **[SURVIVES / high]** `secpriv-privacy-policy-materially-false` → docs/privacy-policy.html:22
  - FIX NOW: Rewrite the policy to disclose device-id + optional self-entered display name + sync/diagnostic telemetry to Cloudflare. REMOVE the coarse-geo/location claim from the disclosure — the app no longer derives or uses IP geo (that leg is deleted). Also delete the stale 'Tu ciudad / himnario de tu comunidad / Llavero' line: there is no per-community book selection and city is not stored in Keychain. Fix or drop the 'no internet' and 'no third-party services' claims (Cloudflare relay is core). Re-file the App Store privacy questionnaire to match.
  - The privacy policy is UNCHANGED by the refactor (git shows no touch to docs/privacy-policy.html in the 370→374 delta) and remains materially false. Current text: :22 'SignoVivo no recopila, almacena ni transmite ningún dato personal. La aplicación no requiere cuenta de usuario, registro ni conexión a internet.'; :27 'Tu ciudad ... almacenada en el Llavero seguro de iOS.'; :34 'No se transmite ningún dato fuera de tu red local.'; :38 'no utiliza servicios de análisis, publicidad, rastreo ni ninguna librería de terceros'. Still contradicted by the live code: PdfReaderApp POSTs deviceId + self-entered label + nativeBuild to /fleet/checkin (AsyncStorage sv_fleet_label at :207-227, checkin fetch still present); the app depends on the Cloudflare relay for sync (so 'no internet' is false); state is in AsyncStorage not Keychain (no SecureStore import). The SKEPTIC's correction still holds and is now STRONGER: the geo/IP-derived-location leg is GONE (worker no longer reads request.cf geo — index.ts:499-508 is the single-book comment + bare try, /health at :507-508 returns only {ok,service,v}), so the disclosure-gap list shrinks to device-id + self-entered display name + diagnostic telemetry to Cloudflare (no coarse geo anymore). Also note :27's 'Tu ciudad (para cargar el himnario de tu comunidad)' is now describing a feature that no longer exists (single book, no geo) — an additional inaccuracy the rewrite should drop.
- **[SURVIVES_MOVED / low]** `secpriv-log-unauthenticated-pii-leak` → sync-worker/src/index.ts:515
  - FIX NOW: Gate GET and DELETE /log behind FLEET_DASHBOARD_KEY / the bearer token (like /fleet, index.ts:573-587). Add a per-entry AND total byte cap in appendLog (index.ts:146-155) to stop a single oversized POST permanently bricking the buffer — this is the higher-value fix. Swapping the Swift dev field to sv_devid remains nice-to-have (it is already a generic model name).
  - The /log handler SURVIVES essentially unchanged, moved from ~:594 to index.ts:515-540. POST(appendLog :531), DELETE(clearLog :535), and GET(readLog dump :538-539) all still have NO auth check and NO content-length guard (contrast /fleet/checkin's 16KB cap at :557 and /publish's 64KB at :676). appendLog (:146-155) still slices to 200 per-call and rings to the last 600 entries but caps NO bytes — so the SKEPTIC's real robustness bug survives: a single oversized POST can push the 'dbglog' storage value toward the DO value-size limit → put throws → the outer catch (:703-707) masks it as a 200, bricking the diagnostic buffer until a DELETE. The SKEPTIC's severity call (low, not high) still holds: the name-harvest premise was already refuted (Swift /log dev field is a generic model name; JS sends the anonymous sv_devid; real names only go to the FLEET_DASHBOARD_KEY-gated /fleet). The refactor did not touch this endpoint's auth or capping.
- **[SURVIVES_MOVED / high]** `secpriv-release-pii-swap-window` → scripts/release.sh:41
  - FIX NOW: Register `trap restore_codes EXIT INT TERM` immediately after the swap (:43) so the empty file is always restored on crash/interrupt. Harden restore_codes to be idempotent + existence-guarded so the EXIT trap doesn't itself fail under set -e on the success path: `restore_codes() { [ -n "$RESTORE_CODES" ] && [ -f director-codes.committed.bak ] && mv -f director-codes.committed.bak director-codes.json; RESTORE_CODES=""; }`. Add a pre-commit hook blocking a non-empty standardDirectorCodes as the durable backstop. Note: the private file now also carries superAdminCodes, so the swap injects those too — the same trap protects both.
  - SURVIVES verbatim; only the exact line moved slightly (now :41-42). scripts/release.sh:41-42 still `cp director-codes.json director-codes.committed.bak; cp director-codes.private.json director-codes.json`, and restore is still only on the explicit paths: restore_codes at :48 `restore_codes() { [ -n "$RESTORE_CODES" ] && mv -f director-codes.committed.bak director-codes.json; }`, invoked only at :52 (archive-fail), :56 (export-fail), and :59 (success). grep confirms there is STILL NO `trap` anywhere in release.sh. Under set -euo pipefail a Ctrl-C during the ~10-min xcodebuild archive kills bash before any restore runs, leaving real phone numbers in the TRACKED director-codes.json (.gitignore excludes only .private.json and .committed.bak). director-codes.json is still tracked and committed-empty (director-codes.json:1-5 has empty standardDirectorCodes; the refactor added an empty superAdminCodes array at :4 but standardDirectorCodes stays []). A subsequent commit in that or a concurrent tab still bakes PII into public history — irreversible. The refactor did not touch this hazard.
- **[SURVIVES_MOVED / medium]** `secpriv-fleet-key-in-url-logged` → sync-worker/src/index.ts:575
  - FIX NOW: Unchanged from the audit: prefer the X-Fleet-Key header, move the dashboard behind a short-lived signed cookie set from a one-time ?k= exchange, strip ?k= from the meta-refresh (index.ts:459), and consider Cloudflare Access in front of /fleet*.
  - SURVIVES; moved from ~:654 to index.ts:575. Still `const k = url.searchParams.get('k') || request.headers.get('X-Fleet-Key') || '';` (:575), `const keyOk = Boolean(env.FLEET_DASHBOARD_KEY) && k === env.FLEET_DASHBOARD_KEY;` (:577), the dashboard's 20s meta-refresh still re-embeds ?k= (renderFleetDashboard at :459 `<meta http-equiv="refresh" content="20${kq ? ';url=/fleet-dashboard' + kq : ''}">`), and renderFleetDashboard is still called with the raw k (:611). wrangler.jsonc:8 still `"observability": { "enabled": true }`, so Workers Logs capture the full request URL incl. ?k=. getFleet still returns the roster WITH phones (:232-237). The whole /fleet subsystem is untouched by the refactor. Medium stands.
- **[SURVIVES_MOVED / medium]** `secpriv-fleet-checkin-open-abuse` → sync-worker/src/index.ts:553
  - FIX NOW: Unchanged: rate-limit /fleet/checkin, cap the nativeBuild that feeds `latest` to a sane ceiling (current shipped build + small margin — note current shipped is 374, so a cap near ~400 is reasonable) rather than trusting up to 1,000,000, and raise the 300-device ring cap only alongside rate-limiting.
  - SURVIVES; moved. /fleet/checkin is still OPEN with only a 16KB cap (index.ts:553-566, cap at :557). checkin() still ring-caps to the 300 newest-by-ts (:222-228 `.sort((a,b)=>b.ts-a.ts).slice(0,300)`), so 300 fresh fake deviceIds still evict every real device. nativeBuild is still accepted up to 1,000,000 (:212 `Math.min(Number(o.nativeBuild)||0, 1000000)`). The dashboard's latest/build-flag logic (renderFleetDashboard, not shown but the finding's :381-382/:411-412 region) is untouched by the refactor, so a single nativeBuild:1000000 check-in still inflates `latest` and flags the real director's row, and label spoofing still flips readiness. Defeats the pre-Mass readiness tool, not Mass — medium holds. The refactor removed the surface parameter's book relevance but the abuse vectors are book-independent.
- **[MOOT / n/a]** `secpriv-standard-manual-not-actually-gated` → web/build.mjs (single-book) / index.ts:499
  - MOOT: the finding's entire premise — that the manual is DESCRIBED as PRIVATE/access-controlled while being served publicly — no longer exists, because the app is now INTENTIONALLY single-book and fully public. The 'PRIVATE to Del Rio' worker comment the finding quoted (old index.ts:512-513) was DELETED; index.ts:499-501 now states 'Single-book, fully-public app: everyone gets the standard manual. No IP-geo, no book selection.' and the /unlock stub at :650 says 'The app is now single-book and fully public — there is nothing to unlock.' The client-side 'gate' the finding targeted is deleted: isDelRioAuthorized and the localStorage svGeoBook check are GONE from app.js (grep for isDelRioAuthorized/svGeoBook returns nothing), X-Hymnal is fully removed from the worker (no Expose-Headers, /health no longer echoes geo). So there is no longer any 'claimed access control that doesn't exist' to correct — the docs/comments were fixed by the refactor to correctly call the manual public. The manual is still world-readable static Pages assets (unchanged), but that is now the intended, accurately-documented design, not a false invariant.
- **[MOOT / n/a]** `secpriv-director-code-plaintext-asyncstorage` → PdfReaderApp.tsx:442 (STORED_CODE_KEY deleted)
  - MOOT: the plaintext credential is no longer persisted at all. The refactor (commit 38132dd3, no-auto-director) DELETED STORED_CODE_KEY / 'director_access_code' and the AsyncStorage.multiSet write of the code. grep for STORED_CODE_KEY and director_access_code in PdfReaderApp.tsx now returns NOTHING. becomeDirector (PdfReaderApp.tsx:436-442) now explicitly comments 'We deliberately do NOT persist the director code or a timestamp — there is no auto-restore, so a credential must never sit in storage.' and only writes `AsyncStorage.setItem(STORAGE_KEYS.lastSyncRole, 'director')` (:442) — a role breadcrumb, not the code. The DIRECTOR_RESTORE_WINDOW_MS 24h auto-restore that made the code linger is gone; boot now always becomeFollower(). So the exact vulnerability (a real phone-number code sitting in cleartext AsyncStorage for the restore window) no longer exists. The SecureStore migration the finding proposed is now unnecessary for this key because the key is never written; the only residual is the now-vestigial expo-secure-store dependency, which is a cleanup nit, not this security finding.
- **[MOOT / n/a]** `secpriv-health-geo-oracle` → sync-worker/src/index.ts:507
  - MOOT: the geo echo was DELETED. /health (and /) now return ONLY a bare service ping — index.ts:507-508: `if (url.pathname === '/' || url.pathname === '/health') { return json({ ok: true, service: 'signovivo-sync', v: PROTOCOL_VERSION }, 200, cors); }`. There is no city/region/postal/country/lat/lon/kmFromDelRio/hymnal in the response anymore (the whole ~90-line request.cf geo block + Haversine kmFromDelRio was removed in the refactor). X-Hymnal is gone from CORS (corsHeaders at index.ts:301-316 has NO Access-Control-Expose-Headers at all). So the cross-site geolocation/Del-Rio-membership oracle the finding described cannot fire — there is nothing sensitive to read from /health. CORS is still '*' (:302-308) but the response body carries no geo, so the finding's specific leak is eliminated.
- **[MOOT / n/a]** `secpriv-skeptic-new-1-cross-book-flap` → web/src/app.js:3022 (switchBook path deleted)
  - MOOT: this SKEPTIC-NEW reliability item is entirely about the single shared room letting a LEGITIMATE hymns-4/Sión director yank standard followers via bookFromSnap→switchBook('hymns-4'). Every named symbol is deleted. app.js applyRelaySnapshot (:3022-3075) no longer references bookFromSnap or switchBook; it comments 'snap.bookId (kept on the wire for build-373 compat) is effectively always standard, so there is no book to switch' (:3026-3028) and just renders the page via renderPage→clampPage (:3071,954,223). grep confirms switchBook, bookFromSnap, and isDelRioAuthorized no longer exist in app.js. There is no hymns-4 book, no Sión director, and RELAY_ROOM='alvernia-main' now carries exactly one book. An out-of-range director page just clamps and cannot mis-navigate to another book. The cross-book-coherence concern is architecturally impossible in build 374.
- **[SURVIVES / high]** `secpriv-skeptic-new-2-e2e-prod-room-footgun` → e2e/relay-sync.test.mjs:17
  - FIX NOW: Point the suite at a throwaway room (not alvernia-main) and inject a test-only code via env (process.env.SV_TEST_CODE), same fix as secpriv-committed-live-transmitter-code. Additionally delete or rewrite the now-meaningless bookId:'hymns-4'/mode:'nonStandard' assertions (e2e:185-190) since book/mode are vestigial fields. Default the suite to a local `wrangler dev` harness with an env-gated random-room live smoke.
  - SURVIVES: the e2e suite still publishes to the PROD room with mode:'standard' pages. e2e/relay-sync.test.mjs:17 `const ROOM = 'alvernia-main'`, and it still fires standard-mode publishes to real pages — :111 page:42, :120 page:77-range loop, :129 page:50, and more, all with mode:'standard'. The refactor made this WORSE-simpler, not safer: since the app is now single-book and applyRelaySnapshot ignores snap.bookId and renders any published page against the one live book (app.js:3022-3075, no book gate at all), running `node --test e2e/relay-sync.test.mjs` during Mass drives EVERY follower straight to those pages — there is no longer even a book mismatch that might spare non-standard followers. The system's own memory (feedback_never_run_full_e2e) and this reconciliation's hard safety rules confirm the suite is still a live-prod footgun. Note :187 still publishes bookId:'hymns-4'/mode:'nonStandard' and :190 asserts the DO stores it — harmless now (bookId is vestigial, stored at index.ts:128 but read by nothing) but the standard-mode page publishes are the live hazard.

### build-release

- **[SURVIVES / critical]** `build-release-director-codes-swap-no-trap` → scripts/release.sh:40
  - release.sh is byte-identical to build 370 (`git diff 309a9afa..HEAD -- scripts/release.sh` is EMPTY). The PII swap survives verbatim: release.sh:41-42 `cp director-codes.json director-codes.committed.bak; cp director-codes.private.json director-codes.json`, restore_codes defined at :48, called only on the inline failure/success branches (:52,:56,:59). `grep -n trap scripts/release.sh` still returns nothing — no EXIT/INT/TERM handler. A Ctrl-C/sleep/crash during the xcodebuild at :50-57 still leaves the tracked director-codes.json holding real phone numbers. The refactor did NOT touch the release pipeline at all, so this critical PII-leak path is fully intact.
- **[SURVIVES / critical]** `build-release-e2e-suite-mutates-prod-relay` → e2e/relay-sync.test.mjs:17
  - The test file is unchanged: relay-sync.test.mjs:17 `const ROOM = 'alvernia-main'`, :18 `PUB = ${BASE}/r/${ROOM}/publish`, :21 `const CODE = '12345678840'`. package.json:17 still `"test:e2e": "node --test ./e2e/*.test.mjs"` (includes the live file unconditionally). The worker STILL accepts this publish: validTransmitterCodes(env) at index.ts:327 reads env.TRANSMITTER_CODES and 12345678840 is the documented residual code (memory project_director_codes_are_phone_numbers_security_debt). So `npm run test:e2e` still fires real page-flip publishes into the production room mid-Mass — the exact worst-case. NOTE: line 187 still publishes `bookId:'hymns-4'` and line 190 asserts `snap.bookId==='hymns-4'`; the worker no longer routes on bookId but the DO still stores it verbatim (index.ts:128 vestigial field), so that assertion likely still passes AND the harmful publish still lands. The Mass-breaking danger is unchanged; only the (irrelevant) two-book flavor of the payload is now vestigial.
- **[SURVIVES / high]** `build-release-restore-codes-set-e-abort` → scripts/release.sh:48
  - Unchanged (empty diff). release.sh:16 `set -euo pipefail`; :39 `RESTORE_CODES=""`; :46 missing-private-file warning leaves it empty; :48 `restore_codes() { [ -n "$RESTORE_CODES" ] && mv -f director-codes.committed.bak director-codes.json; }`; :59 bare `restore_codes`. On a fresh worktree (private file absent — as in THIS checkout) the `[ -n ]` test returns 1, and under set -e the :59 call still kills the script AFTER the 10-minute archive but BEFORE the IPA copy (:60) and web deploy (:65). Fully intact.
- **[CHANGED / high]** `build-release-cacheversion-hash-gap-frozen-manifests` → web/build.mjs:22
  - FIX NOW: Same fix, but the enumerated hash inputs must drop the deleted files: hymns-4.pdf and assets/standard/hymns-4-{pages,song-titles,song-search-index}.json are GONE (verified: `ls assets/hymns-4.pdf` → No such file). Now hash: src/alverniaManual2SongIndex.js, version.json, assets/alvernia_manual_2.pdf, assets/icon.png, and the ALVERNIA_PDF_RENDER_DPI/ALVERNIA_PDF_WEBP_QUALITY strings. The stale-while-revalidate-in-sw.js alternative is unchanged and now simpler (single /books/standard/ path).
  - The cacheVersion IIFE is UNCHANGED: build.mjs:22 still `for (const f of ["app.js", "sw.js", "styles.css", "index.html", "manifest.webmanifest"])` + build.mjs itself at :26 — book data (PDF, alverniaManual2SongIndex.js, version.json) still unhashed. sw.js still serves /books/*/{pages,search-index}.json cache-first (final handler ~sw.js:170-190) with the shell-only NETWORK_FIRST paths. release.sh:65 still deploys `--commit-dirty=true`. The correctness hole (dirty book-data-only deploy never reaches returning followers) is fully intact. The BUG survives as-is; only the FIX text must change because two inputs it named no longer exist.
- **[SURVIVES / high]** `build-release-deploy-web-npm-script-trap` → package.json:15
  - package.json is unchanged (empty diff). :15 still `"deploy:web": "node ./web/build.mjs && npx wrangler pages deploy web/dist --project-name alvernia-reader"` — no --branch main, no bump, no WebBundle sync — vs release.sh:65 which pins `--branch main --commit-dirty=true`. Running it from a dev worktree still yields a silent PREVIEW deploy (Pages prod=main only per memory). The single-book refactor didn't touch this; the side-door trap is fully live. Still coupled to finding #10 (footprint test red because this script was added without updating the allowlist).
- **[SURVIVES / high]** `build-release-alt-archive-scripts-bypass-invariants` → scripts/testflight-upload.sh:23
  - All three scripts still exist and are unchanged (`git diff 309a9afa..HEAD` empty for all three; ls confirms scripts/testflight-upload.sh, testflight-upload-transporter.sh, submit-appstore.sh, export-options.plist present). They still archive directly with no `node web/build.mjs`, no `cp -R web/dist ios/WebBundle`, no director-codes swap, no bump — producing an IPA with a stale WebBundle and (because committed director-codes.json is still the empty shape) a DISABLED standard-director entry. submit-appstore.sh still points at scripts/export-options.plist vs release.sh's ios/exportOptions.app-store.plist. Fully intact; if anything the single-book change makes the 'director can't authenticate' failure mode even more central since standard is now the ONLY book.
- **[SURVIVES / medium]** `build-release-bump-first-no-rollback` → scripts/release.sh:22
  - Unchanged. release.sh:22 `node scripts/bump-build.mjs` is still step 1 (before any tool/auth/private-file preflight); the only `git checkout --` in the script is for ios/Podfile.lock (:52/:56/:58) — version.json/pbxproj/Info.plist/app.json are never reverted on failure. bump-build.mjs still writes buildNumber+1 unconditionally. Retry-double-bump hazard fully intact.
- **[CHANGED / medium]** `build-release-immutable-page-urls-vs-changed-bytes` → web/build.mjs:91
  - FIX NOW: Same content-fingerprint-in-URL fix, but drop 'assets/hymns-4.pdf' from the fingerprint inputs (it's deleted). Fingerprint = short hash of (alvernia_manual_2.pdf bytes + DPI + quality) folded into /books/standard/pages/<rev>/page-NNN.webp. With only one book the stopgap (pin DPI/quality as constants + a REV constant) is even simpler.
  - The mechanism survives intact: build.mjs:91-92 still env-tunable `PDF_RENDER_DPI = parsePositiveInt(process.env.ALVERNIA_PDF_RENDER_DPI, 115)` and `WEBP_QUALITY = ...ALVERNIA_PDF_WEBP_QUALITY, 60`; build.mjs:693-695 still writes `_headers` with `/books/*/pages/* Cache-Control: public, max-age=31536000, immutable`; the :689-691 comment still falsely claims filenames are 'content-stable'. sw.js still keeps old page caches (PAGE_CACHES_TO_KEEP=2 at sw.js:14, matchAnyPageCache fallback at :152). Changing DPI/quality or editing a PDF page still ships a permanent old/new mix at immutable URLs. The BUG is unchanged; the FIX text must drop the dead hymns-4 asset.
- **[PARTIALLY_MOOT / medium]** `build-release-hymns4-songindex-range-unchecked` → web/build.mjs:548
  - CHANGED: The two-book framing collapses to one: only the standard manual's regex-scraped songIndex remains unvalidated. Injecting a `[song, page]` pair whose page > rendered pageFiles.length (or a non-increasing song) into src/alverniaManual2SongIndex.js produces a pages.json songIndex entry that jumps to a clamped/wrong page at Mass, with a fully green build.
  - FIX NOW: In buildStandardManifests, after building songIndex (~build.mjs:548-573) and before writing pages.json (:623), throw unless every entry has Number.isInteger(page) && 1 <= page <= pageFiles.length (and optionally strictly-increasing song). Drop all hymns-4 / buildHymns4Manifests references and the 'extend check-book-consistency for hymns-4-pages.json' clause — those files no longer exist.
  - The HEADLINE target — buildHymns4Manifests — is DELETED (git diff shows -146 lines from build.mjs; `grep -n hymns4 web/build.mjs` finds nothing; the second buildBook call and hymns-4-pages.json source are gone, and `ls assets/standard/hymns-4-pages.json` → No such file). So the hymns-4-specific half is MOOT. BUT the finding's own FIX explicitly also flagged the standard book's regex-scraped songIndex ('Mirror the same validation in buildStandardManifests… the regex /\[(\d+),\s*(\d+)\]/g at build.mjs:577'). That code SURVIVES: current build.mjs:548 `for (const match of songIndexSource.matchAll(/\[(\d+),\s*(\d+)\]/g)) { songIndex.push({ song: Number(match[1]), page: Number(match[2]) }); }` reading src/alverniaManual2SongIndex.js, then :623 writes `pages.json` with that songIndex and NO page-range check (only `throw`s in build.mjs are for sips/pdftoppm/cwebp/pdftotext failures — grep confirms no `page <= pageFiles.length` guard). An out-of-range or non-monotonic pair in alverniaManual2SongIndex.js still ships green as a bad jump target — the build-325 bug class, now only for the standard (only) book.
- **[SURVIVES / medium]** `build-release-footprint-test-red-at-head` → e2e/repo-minimal-footprint.test.mjs:15
  - Both files unchanged (empty diffs). package.json still has `deploy:web` at :15 (12 script keys); the footprint allowlist still lacks it (`grep -n deploy:web e2e/repo-minimal-footprint.test.mjs` → no match). So the exact-allowlist assertion still fails on a clean tree → `npm run test:e2e` is still red at HEAD, training everyone to ignore red. Refactor didn't touch it. Still coupled to finding #5.
- **[CHANGED / medium]** `build-release-check-book-consistency-soft-skip-and-coverage` → scripts/check-book-consistency.mjs:29
  - CHANGED: Post-refactor there is only one book, so the 'validates only standard, hymns-4 unguarded' complaint is moot — but the soft-exit-0-on-missing-poppler, the release.sh-never-invokes-it, and the native-vs-web dual-source-of-truth (offlineBooks.ts still imports assets/standard/pages.json) all remain live for the single standard book.
  - FIX NOW: Keep fix parts (1) missing-tool path exit 1 with CHECK_BOOK_SOFT=1 escape, and (2) call check-book-consistency as a release.sh preflight before bump. DROP part (3)'s hymns-4-pages.json clause; retain only the cross-check of assets/standard/pages.json totalPages against pdfinfo and against what web/build.mjs would render.
  - check-book-consistency.mjs is unchanged (empty diff): :29-32 still `if (info.status !== 0) { console.warn(...skipping...); process.exit(0); }` (soft-skip when pdfinfo/poppler missing) and the same for unparsable output. release.sh still never invokes it (`grep check-book scripts/release.sh` → NONE); only preios/preios:mpad do (package.json:8,10). offlineBooks.ts:4 still `import STANDARD_PAGES_JSON from '../assets/standard/pages.json'` — the checked file is still a live native input AND still a second source of truth vs web/build.mjs's independently-rendered totalPages. Those three sub-problems all SURVIVE. Only the 'guards only the standard book / extend to hymns-4' sub-point is MOOT (hymns-4 gone), which actually SIMPLIFIES the coverage story — standard is now the whole app, so a soft-skipped guard on a fresh Mac still ships unreachable final songs on Sunday.
- **[SURVIVES / medium]** `build-release-rm-rf-build-concurrent-tabs` → scripts/release.sh:49
  - All four scripts unchanged. release.sh:49 `rm -rf build` (no lock), testflight-upload.sh:24 and testflight-upload-transporter.sh:23 same, submit-appstore.sh archives into $REPO_ROOT/build. No mkdir/lockfile guard exists (grep 'lock' matches only Podfile.lock). Concurrent-tab archive clobber (§4 multi-tab standing pattern) fully intact.
- **[SURVIVES / medium]** `build-release-podfile-lock-copy-defeats-guard` → scripts/release.sh:36
  - Unchanged. release.sh:36 still `cp ios/Pods/Manifest.lock ios/Podfile.lock  # pod-guard workaround (Ruby 4.0.1 pod install is broken)` with restores at :52/:56/:58. The blind cp still neutralizes the [CP] Check Pods Manifest.lock consistency phase on every archive, hiding genuine Pod drift (memory reference_build_env_pod_ruby4_drift documents real standing drift) until runtime on the parish iPads. Fully intact.
- **[SURVIVES / low]** `build-release-bump-silent-regex-skip-dead-block` → scripts/bump-build.mjs:34
  - bump-build.mjs unchanged (empty diff). :34 `const out = src.replace(/CURRENT_PROJECT_VERSION = \d+;/g, ...); if (out !== src) ...` — silent-on-no-match, no throw. :43 Info.plist CFBundleVersion replace, no /g, single-key assumption. :20-21 the offlineWebBundle sync block (`offlineBundlePath = path.join(root,'src','offlineWebBundle.js'); if (fs.existsSync(...))`) is still present and still dead — `src/offlineWebBundle.js` does not exist (unchanged by refactor). release.sh:21 echo still advertises offlineWebBundle. Format-drift silent-skip hazard fully intact; and it now hides inside the still-red suite (finding #10).
- **[SURVIVES / low]** `build-release-dist-wipe-before-tool-preflight` → web/build.mjs:41
  - Survives. build.mjs:41 `fs.rmSync(distDir, { recursive: true, force: true })` still runs before any external tool (sips :70, pdftoppm :102-107, cwebp :123-127, pdftotext :561), which are still reactive throws (status !== 0) with no upfront `which`/`--version` preflight (grep confirms none). release.sh:27 still `node web/build.mjs >/dev/null`. On a machine missing a brew tool, the wipe still destroys the last good dist before a late failure, silently. Line moved trivially within the same region; behavior identical.
- **[SURVIVES / low]** `build-release-preios-hooks-bump-on-every-local-run` → package.json:8
  - package.json unchanged. :8 `"preios": "node scripts/check-book-consistency.mjs && node scripts/bump-build.mjs"` and :10 same for preios:mpad — npm still auto-runs these before ios/ios:mpad, and bump-build.mjs still writes 4 tracked files unconditionally. Casual local runs still advance the release counter and dirty version.json/app.json/Info.plist/pbxproj, feeding both the drift-looking gap and the multi-tab dirty-tree confusion, plus `--commit-dirty=true` baking dirty version files into prod. Fully intact.

### test-suite

- **[SURVIVES / critical]** `test-suite-relay-sync-mutates-production-room` → e2e/relay-sync.test.mjs:16-21
  - The relay-sync test file was NOT touched by the 370->374 refactor (git diff 309a9afa..HEAD -- e2e/relay-sync.test.mjs is empty). Current lines unchanged: line 16 `const BASE = "https://signovivo-sync.4j4982y8jp.workers.dev"`, line 17 `const ROOM = "alvernia-main"`, line 21 `const CODE = "12345678840"`, line 45 top-level `await get(STATE)` fires a network read at module load. README.md:17 still instructs `npm run test:e2e`. The worker still accepts the committed code (validTransmitterCodes reads env.TRANSMITTER_CODES, index.ts:327, and the legacy code remains in the deployed secret per project memory) and the stale-room takeover bypass is intact at index.ts:118-122 (`snapshotStale ... accepts a lower seq`), so the rewind hazard is unchanged. The ONLY sub-detail that shifted: line 187 `bookId: "hymns-4"` and line 111 `bookId: "alvernia"` are now VESTIGIAL — the worker no longer routes/authorizes on bookId (index.ts:692 'any valid transmitter code may publish; no book-scoping') and web ignores snap.bookId — but every follower still force-follows the published PAGE numbers, so the live-Mass page-flip danger is identical. The seq-flakiness (`final.seq === t+3` at line 238, `freshSeq()` seeding off the live room at line 45-47) is also unchanged.
- **[SURVIVES / high]** `test-suite-repo-footprint-red-at-head` → e2e/repo-minimal-footprint.test.mjs:15
  - package.json:15 still carries `"deploy:web": "node ./web/build.mjs && npx wrangler pages deploy web/dist --project-name alvernia-reader"` (no --branch main, no bump). The scripts allowlist assertion at repo-minimal-footprint.test.mjs:15 was not updated by the refactor and does NOT include deploy:web (current package.json scripts: start, preios, ios, preios:mpad, ios:mpad, ios:local, android, build:web, deploy:web, typecheck, test:e2e, postinstall). So the deepStrictEqual still fails => the whole suite is still red at HEAD, masking regressions. Untouched by the single-book refactor.
- **[CHANGED / high]** `test-suite-sync-worker-zero-tests-security-boundaries` → sync-worker/src/index.ts (no test dir; boundaries at :107-136 seq, :577 fleet key, :663-671 publish auth)
  - CHANGED: The finding as written names a Sión /unlock oracle, /publish Sión book-scoping, and a geo X-Hymnal table that no longer exist in the single-book worker. What remains untested and Mass-critical: publish auth (401 on bad/missing code; bearer path), seq sanitization + the stale-room takeover bypass (a poisoned or wrongly-scaled seq must self-heal after 90s, and a fresh room must reject a lower seq), the fleet /fleet.json FLEET_DASHBOARD_KEY gate that guards choir phone numbers, and the 413 oversized-body guard.
  - FIX NOW: Create sync-worker/test/relay.test.ts on vitest + @cloudflare/vitest-pool-workers with a REVISED matrix (drop the Sión/unlock/X-Hymnal cases entirely): (1) /publish auth: no/garbage code -> 401; valid TRANSMITTER_CODES entry -> 200; bearer RELAY_DIRECTOR_TOKEN -> 200; fail-closed when env.TRANSMITTER_CODES unset. (2) /unlock now returns 200 {ok:true,hymnal:'standard'} to ANY caller — pin that it is a harmless stub (regression-catch if someone re-adds an auth path). (3) SyncRoom.publish: seq strictly-greater guard while fresh (equal/lower ignored:true), seq=0 always accepted, Infinity/NaN/negative/now+61s collapse to 0, stale room (vi.setSystemTime past 90s) accepts a LOWER seq (takeover bypass), page clamp [1,100000], mode/bookId .slice(0,64) (bookId is dead-but-stored). (4) /fleet.json without key -> 401, with FLEET_DASHBOARD_KEY -> roster; oversized checkin -> capping. (5) publish 413 on content-length > 64KB. (6) /log ring buffer caps at 600. Per-test random room names.
  - The worker STILL has zero tests (sync-worker/ has only src/index.ts, wrangler.jsonc, package.json with dev/deploy/tail/typecheck, tsconfig, test-client.html — no test dir, no test script), so the finding's spine survives. But THREE of the six boundaries the finding enumerated are now MOOT because the refactor deleted their code: (a) /unlock Sión-code rejection is GONE — /unlock is a no-op `return json({ ok: true, hymnal: "standard" }, 200, cors)` at index.ts:649-654, there is no `code !== SION_DIRECTOR_CODE` clause left to protect; (b) /publish book-scoping is GONE — index.ts:692 comment 'Single book: any valid transmitter code may publish page numbers. No book-scoping' and there is no `sion_code_book_scoped` 403; (f) the geo X-Hymnal decision table is GONE — no Haversine/kmFromDelRio/STANDARD_RADIUS_KM anywhere. The boundaries that SURVIVE and still deserve worker tests: (d) publish seq sanitization at index.ts:107-110 (Infinity/NaN/negative/now+60s collapse to 0); (e) stale-room takeover bypass at index.ts:118-122; publish auth at index.ts:663-671 (bearer OR X-Director-Code in validTransmitterCodes, fail-closed if unset); the fleet PII gate at index.ts:577 (`keyOk = Boolean(env.FLEET_DASHBOARD_KEY) && k === env.FLEET_DASHBOARD_KEY`); body-size 413 at :675-677; the DO ring buffer cap 600 at :152. Severity stays HIGH: fleet PII exposure and a seq-poison lockout during Mass are still catastrophic and unguarded.
- **[CHANGED / high]** `test-suite-appjs-sync-core-zero-tests` → web/src/app.js (no tests; seq guard, RELAY_LIVE_MAX_AGE_S:2766/3020, clampPage:223)
  - CHANGED: The Stage-1 extraction target no longer includes bookFromSnap's book-resolution table or the standard/hymns-4/legacy-alvernia mapping (that code is deleted). The pure functions worth extracting to web/src/relay-core.js are now just: relayIsFreshLive (freshness + missing-ts=>fresh contract), the seq-guard predicate, clampPage, and single-book pageFileName/pagePadWidth. applyRelaySnapshot now ignores bookId entirely, so the 'out-of-range director page just clamps and can't mis-navigate' assertion becomes the key behavioral test (no cross-book mis-nav possible anymore).
  - FIX NOW: Stage 1: extract relayIsFreshLive, the seq-guard predicate, clampPage, and single-book pageFileName/pagePadWidth into web/src/relay-core.js; web/test/relay-core.test.mjs asserts: seq drop/apply/force matrix; freshness true at now-89s, false at now-91s, TRUE when ts missing; clampPage NaN/0/overflow -> in-range; pad width for 371 pages -> 3. DELETE the deleted-book assertions (bookId 'standard' unauthorized -> null, 'hymns-4' -> hymns-4, legacy 'alvernia' mode fallback) — that resolver no longer exists. Stage 2 jsdom harness: director push renders; stale (91s) clears the pill; restarted-director lower-seq + reset (lastSeq=-1) recovers; out-of-range director page clamps in-range.
  - app.js STILL has zero unit tests (only relay-sync.test.mjs:380 greps it), so the finding survives. But the specific covered-logic list shifted with the refactor: bookFromSnap privacy resolver is GONE (app.js no longer resolves per-book — applyRelaySnapshot 'ignores snap.bookId, kept on the wire for build-373 compat'), unlockStandard/switchBook is GONE (no unlockStandard, no book layer), so the audited 'restarted-director seq trap at 3407 / applyRelaySnapshot interleave clamping against a stale book totalPages / unlockStandard swallowed switchBook' bug classes are partly MOOT. What SURVIVES and is still untested: the seq guard (now inside applyRelaySnapshot), relayIsFreshLive at app.js:3020 (`!snap.ts || (Date.now()/1000)-snap.ts <= RELAY_LIVE_MAX_AGE_S`), RELAY_LIVE_MAX_AGE_S=90 at app.js:2766, clampPage at app.js:223 (pageNumber clamp), pageFileName/pagePadWidth (now single-book), reconnect/backoff/heartbeat. Line numbers all moved (file is now 3336 lines vs the audit's 3928).
- **[PARTIALLY_MOOT / medium]** `test-suite-eas-config-banned-pathway-stale-assets` → e2e/eas-config.test.mjs:54-59
  - FIX NOW: Same split-and-mostly-delete as audited, MINUS the hymns-4.pdf pin (that asset is gone). New e2e/app-config.test.mjs derives its asset list from app.json (parse expo.icon='./assets/03_icon_1024x1024.png' and expo.splash.image='./assets/04_splash_1668x2388.png', assert THOSE exist), plus assets/icon.png (comment: required by web/build.mjs:56) and assets/alvernia_manual_2.pdf. DELETE the two EAS-profile tests (lines 35,38) and the resolved-expo-config shell-out (line 20+) as banned-pathway pins; keep app-identity (name/slug/bundleIdentifier). Then delete eas-config.test.mjs.
  - The core defect SURVIVES: eas-config.test.mjs:54-59 requiredPaths still lists ['assets/icon.png','assets/adaptive-icon.png','assets/splash.png','assets/favicon.png','assets/alvernia_manual_2.pdf'] while app.json:12-14 references `./assets/03_icon_1024x1024.png` and `./assets/04_splash_1668x2388.png` — the inverse-coverage gap (real icon/splash unpinned) is exactly as audited. The two EAS-profile pins (line 35 appVersionSource, line 38 production channel) and the `npx expo config --json` shell-out (line 20) are all unchanged and still enforce the banned EAS pathway. What is now MOOT: the audit's FIX told the author to add `assets/hymns-4.pdf` to the corrected asset test ('both PDFs feed the web build; hymns-4.pdf is currently pinned by NO test') — hymns-4.pdf was DELETED by the refactor (ls assets/hymns-4.pdf -> No such file; git diff removed the hymns-4 offline tests). So the hymns-4.pdf half of the fix must be dropped; only alvernia_manual_2.pdf + assets/icon.png (for web/build.mjs:56) + the app.json-derived icon/splash paths should be pinned.
- **[SURVIVES_MOVED / medium]** `test-suite-vacuous-staleness-90s-contract-unpinned` → e2e/relay-sync.test.mjs:379-382 (assertion); app.js:2766 + :398; sync-worker/src/index.ts:34
  - FIX NOW: Unchanged from the audit: replace the vacuous grep with a drift-pin that regex-extracts `const RELAY_LIVE_MAX_AGE_S = (\d+)` from BOTH web/src/app.js (:2766) and sync-worker/src/index.ts (:34) and asserts both matched AND equal; add the behavioral boundary tests in the worker-vitest and web relay-core harnesses. Delete the `|| appJs.includes("90")` fallback.
  - The test is unchanged: relay-sync.test.mjs:380 still asserts `appJs.includes("RELAY_LIVE_MAX_AGE_S") || appJs.includes("90")`. The vacuous fallback is still satisfiable forever — app.js:398 still contains `}, 90);` (an unrelated timer), so even deleting RELAY_LIVE_MAX_AGE_S keeps it green. The genuine cross-file numeric contract still exists and is still unpinned by value: web/src/app.js:2766 `const RELAY_LIVE_MAX_AGE_S = 90` must equal sync-worker/src/index.ts:34 `const RELAY_LIVE_MAX_AGE_S = 90`. Only the line numbers moved (audit cited app.js:3079/index.ts:36); the split-brain-during-Mass failure mode is unchanged.
- **[PARTIALLY_MOOT / medium]** `test-suite-build-manifest-generation-untested` → web/build.mjs:546-548 (standard songIndex regex-scrape), :602-623
  - CHANGED: The build now has ONE book. The 'out-of-range songIndex ships silently' risk is now only the standard-book regex-scrape path (build.mjs:546-548 + the search-index range check at the standard buildBook). There is no second book to validate.
  - FIX NOW: New pure-JSON e2e/book-manifest-integrity.test.mjs (drop the hymns-4 assertions entirely): for assets/standard/pages.json assert every songIndex[].page is an integer in [1,totalPages] and songs are unique; parse src/alverniaManual2SongIndex.js with the SAME `/\[(\d+),\s*(\d+)\]/g` regex build.mjs:548 uses and assert max page <= pages.json totalPages (catches the build-325 class at test time without pdfinfo). Also add scripts/check-book-consistency.mjs to release.sh before the build step. (Drop the build.mjs buildHymns4Manifests hardening — that function is gone.)
  - The hymns-4 half of this finding is fully MOOT: buildHymns4Manifests() was deleted (git diff shows -158 lines from build.mjs; grep finds no buildHymns4/hymns-4 in build.mjs), assets/standard/hymns-4-pages.json is gone (ls -> No such file), and the hymns-4 offline-integrity tests were removed. So sub-problem (a) 'hymns-4 songIndex passed through with no page validation' no longer exists. The STANDARD-book half SURVIVES: build.mjs:546-548 still regex-scrapes the index from source — `for (const match of songIndexSource.matchAll(/\[(\d+),\s*(\d+)\]/g))` reading src/alverniaManual2SongIndex.js — so a commented-out pair still becomes a shipped song entry (sub-problem b), and scripts/check-book-consistency.mjs still runs only via preios (package.json:8) not release.sh, still covers only the standard book, and still exits 0 with a warning when pdfinfo is missing (line 30). This IS the song-370/build-325 fire-drill class and it remains unguarded at test time.
- **[SURVIVES / medium]** `test-suite-sw-cache-lifecycle-untested` → web/src/sw.js:14, :18, :48
  - FIX NOW: Unchanged from the audit: web/test/sw.test.mjs on node --test with an in-memory Map-backed CacheStorage mock; assert activate keeps the 2 newest page caches + deletes the oldest, the isShellCached deferral, first-install activates on partial cache, and previous-version page-cache fallback. Use /books/standard/pages/page-001.webp fixtures (single-book paths now).
  - sw.js still has zero tests (no test file references it). The audited policies are all intact: sw.js:14 `const PAGE_CACHES_TO_KEEP = 2`, sw.js:18 `const CRITICAL_SHELL_ASSETS = ["/", "/index.html", "/styles.css", "/app.js"]`, sw.js:48 isShellCached activation gate, PAGE_CACHES_TO_KEEP-driven cache pruning at :94-107. The refactor only rewrote sw.js COMMENTS from multi-book to single-book (git diff was comment-only, 17 lines); isPageImageRequest still matches `/books/standard/pages/page-NNN.webp` generically (:139). The white-pages-mid-Mass failure mode from a naive 'delete all old caches' cleanup is unchanged. The empty `current_lines:0` in the delta map's file_shapes entry is a mis-note — the file exists and is populated.
- **[SURVIVES / low]** `test-suite-nearby-sync-regex-pins-classification` → e2e/nearby-sync-contract.test.mjs:153-158, :213-218, :22-28
  - The Multipeer mesh is DEVICE-GATED and was NOT touched by the single-book refactor. nearby-sync-contract.test.mjs is unchanged: the implementation-shape pins the audit wants deleted are all still there — line 153 `discoveryRefreshInterval: TimeInterval = 25`, line 217 `refreshFn.includes("autoreleasepool")` — and the load-bearing wire-contract pins survive at lines 22-28 (`String(context.bookId`, the .d.ts mode/bookId type, `"bookId": bookId` in bridge+Swift). The mesh source still carries these: src/nearbyDirectorSync.js:47 `String(context.bookId || "")`, DirectorSyncModule.swift:138/678 `"bookId": ...`. NOTE the bookId/mode wire fields are now VESTIGIAL end-to-end (native forces book='standard' on incoming mesh pages, mode always 'standard'), but the pins and the classification finding are unaffected — the file still mixes contract pins with shape pins exactly as audited.
- **[SURVIVES / low]** `test-suite-permission-flow-dead-takeover-pin` → e2e/permission-flow.test.mjs:38-51
  - Unchanged. permission-flow.test.mjs:39-50 still pins requestDirectorTakeover / approveDirectorTakeover / denyDirectorTakeover across JS/bridge/Swift. Verified the dead-code state persists: src/nearbyDirectorSync.js:75 still `export const requestDirectorTakeover` and :83 approveDirectorTakeover (both exported), Swift still has @objc(requestDirectorTakeover:) at DirectorSyncModule.swift:435. PdfReaderApp.tsx imports and calls ONLY denyDirectorTakeover (line 27 import, line 818 `denyDirectorTakeover(String(event.requestId)).catch(...)`); requestDirectorTakeover/approveDirectorTakeover are called by nothing. The refactor was single-book/geo — it did not touch the takeover plumbing, so this dead-behavior pin still blocks deleting the dead exports and its name still lies.
- **[SURVIVES / low]** `test-suite-song55-test-name-lies-about-clamping` → e2e/native-entrypoint.test.mjs:44-45
  - Unchanged. native-entrypoint.test.mjs:44 still `test("song index resolves correctly — song 55 → page 55, out-of-range clamps", ...)` and line 45 reads src/alverniaManual2SongIndex.js as raw text (the `assert.match(source, /\[55,\s*55\]/)` grep). The target file src/alverniaManual2SongIndex.js still exists (3907 bytes, unchanged by the single-book refactor — it was always the standard manual's index). The test still resolves nothing and clamps nothing; clampPage still lives in app.js:223 with zero coverage. Note this finding (id ...song55...) and the DIMENSION-header duplicate at the top of the file (line 775, same title) are the SAME issue reported twice in the raw doc; both point at native-entrypoint.test.mjs:44.
- **[SURVIVES / low]** `test-suite-bump-build-lockstep-untested-directly` → scripts/bump-build.mjs:43-46, :20-27
  - Unchanged by the refactor. bump-build.mjs still does silent-on-no-match regex edits: line 43 `.replace(/(<key>CFBundleVersion<\/key>\s*<string>)\d+(<\/string>)/m, ...)` then line 46 `if (out !== src) fs.writeFileSync(...)` — a non-matching pattern is still indistinguishable from an up-to-date file, no throw. The dead src/offlineWebBundle.js sync block still runs at lines 20-27. The lockstep invariant is still only pinned by permission-flow.test.mjs:89-97 (tests current repo state, not run by release.sh between bump and archive). The App-Store-duplicate-build-number failure mode is unchanged.
- **[CHANGED / low]** `test-suite-readme-instructs-prod-mutation-and-lies` → README.md:3, :7-8, :17, :30
  - CHANGED: The audit's rewrite spec still applies for the dangerous parts (drop test:e2e from quick-start, fix the 'no nearby sync' lie, fix dead worktree paths). The architecture-description sentence must now reflect build 374: a SINGLE public 'standard' Alvernia manual (371pp), no geo/two-book/Sión, served to everyone; the WKWebView shell over the web bundle; the Cloudflare relay; the device-gated Swift Multipeer mesh (still present).
  - FIX NOW: Rewrite README.md (~30 lines): current single-book architecture in three sentences (single public 'standard' Alvernia manual built by web/build.mjs, bundled file:// into PdfReaderApp.tsx WKWebView shell; Cloudflare Worker relay in sync-worker/; device-gated Swift Multipeer mesh — which DOES exist, so keep the local-network permission keys); repo-relative paths only (remove the alvernia-reader-dev-reader-prep links); Quick start = `npm ci && npm run typecheck` plus the offline test command with a warning that the relay e2e tests must never target the production room; point releases at scripts/release.sh. Land in the same PR as the relay-sync rewrite.
  - README.md is unchanged by the refactor, so the finding SURVIVES with its line numbers intact: line 17 still `npm run test:e2e` (the prod-mutating suite, per finding #1); line 30 still 'The shipped iOS app no longer includes nearby sync or local-network permissions' (contradicted by Info.plist NSBonjour keys + DirectorSyncModule.swift, pinned by permission-flow.test.mjs); lines 7-8 still link the deleted worktree `/Users/cazares/src/alvernia-reader-dev-reader-prep/...`. What CHANGED with build 374: line 3's 'Single-purpose ... reader for the bundled Alvernia PDF' is now actually CLOSER to true — the app IS single-book (one standard Alvernia manual) — so the rewrite should describe the CURRENT single-public-book architecture (no two-book, no geo, no Sión), whereas the audit's summary was written when the app was two-book. The README is now stale in a NEW direction: any prose the rewrite adds about books must say single public 'standard' book served to everyone, not the multi-book model the audit-era README/plan assumed.

### perf

- **[SURVIVES_MOVED / high]** `perf-live-img-retry-overwrites-new-page` → web/src/app.js:21
  - FIX NOW: Fix unchanged. In the error handler capture `const reqAtError = state.pageLoadRequest` and `base`; in the setTimeout bail unless `state.pageLoadRequest === reqAtError && (pageImage.getAttribute('src')||'').split('?')[0] === base`. Reset pageImgRetries when renderPage commits a new src (app.js:981). Only the line number in the FIX text moves (renderPage guard now app.js:973, commit now app.js:981).
  - The live <img> error-retry is unchanged, just moved up from app.js:56 to the module-level block at app.js:20-25 (it now sits right below the new single-book liftGateNow helper). Verbatim current code: `let pageImgRetries = 0; pageImage.addEventListener("error", () => { if (pageImgRetries++ >= 4) return; const base = (pageImage.currentSrc || pageImage.src).split("?")[0]; window.setTimeout(() => { pageImage.src = `${base}?retry=${pageImgRetries}`; }, 350 * pageImgRetries); });` — still NO state.pageLoadRequest / src-match guard in the timer callback, so a retry captured at error time can re-commit an old page over a newer one. renderPage's own guard is now at app.js:973 (`if (requestId !== state.pageLoadRequest) return;`) and protects only its own path, exactly as the finding described. The refactor didn't touch this; the wrong-page-under-green-pill failure is fully intact and single-book makes it MORE reachable (every follower is on this one book).
- **[SURVIVES_MOVED / high]** `perf-timeout-commit-hides-loading-indicator` → web/src/app.js:983
  - FIX NOW: Fix unchanged: on `loadState === 'timeout'` skip hideLoadingIndicator (keep the loading state active) and hide it from the live img's load listener when `pageImage.dataset.page === String(state.currentPage)`. Only line refs update: load-listener is now the module block app.js:19-26 (which currently only does retry, not indicator-hide), hideLoadingIndicator app.js:401.
  - preloadImage still resolves 'timeout' after PAGE_IMAGE_LOAD_TIMEOUT_MS (app.js:935 `window.setTimeout(() => finish(resolve, "timeout"), timeoutMs)`), and renderPage still treats timeout identically to ready. Current code app.js:980-1000: `state.currentPage = nextPage; pageImage.src = nextPageUrl; ... if (loadState === "timeout") { console.warn("La carga de la página tardó demasiado", nextPage); }` then unconditionally `postNativeBridge({type:"page-changed"...})`, `renderStatus();` and `hideLoadingIndicator();` (app.js:986-1000). The timeout branch is warn-only; nothing keeps the loading overlay up. hideLoadingIndicator is at app.js:401. Old page shown under new-song title with a green pill and no spinner — behavior identical to build 370; refactor only shifted line numbers (1200→983/1000).
- **[PARTIALLY_MOOT / medium]** `perf-boot-precache-contention-with-live-turns` → web/src/app.js:532
  - CHANGED: Now a single 4-worker pool (max 4 concurrent precache fetches, not 8) over the 371-page standard book, still un-yielded to live page turns. The 'two pools running concurrently' clause and the '3.0 MB hymns-4 floor pool at gateLifted+5s' clause no longer apply — precachePublicFloor is gone.
  - FIX NOW: Drop precache concurrency 4→2 (app.js:532); before each cacheSinglePage iteration yield while a foreground load is pending (track an in-flight flag set in renderPage); set fetch `priority:'low'` in cacheSinglePage. DROP fix step (3) about chaining precachePublicFloor after ensureOfflineBundle — there is no floor pool anymore.
  - HALF of this finding is MOOT: precachePublicFloor — the SECOND 4-worker hymns-4 (3.0 MB) pool that ran concurrently, giving up to 8 parallel fetches — is DELETED wholesale with the two-book system (grep for precachePublicFloor in web/src/app.js: zero hits). What SURVIVES: ensureOfflineBundle still runs a 4-worker pool over all missing pages with no yield to foreground loads. Current code app.js:532 `const concurrency = Math.min(4, Math.max(1, missingPages.length));` then workers loop `await cacheSinglePage(cache, pageNumber)` (app.js:533-541); deferOfflinePrecache still SETTLE_MS=4000 + gate poll (app.js:558-577); still nothing checks for an in-flight foreground renderPage/preloadImage. So the cold-boot-during-Mass contention is real but the worst-case parallelism is halved (4, not 8) and the standard book is now the ONLY 25.1 MB download.
- **[MOOT / n/a]** `perf-persisted-standard-boot-wrong-book-reveal` → web/src/app.js:54 (index.html:54) / reveal at app.js:3280
  - The whole mechanism is deleted. (1) The first-paint <img> src is no longer the hardcoded hymns-4 page — index.html:54 now ships `src="books/standard/pages/page-001.webp"` (the correct, only book). (2) tryLiftGate with its decode-guard is GONE; reveal is now the trivial `liftGateNow` (app.js:11-16) which just adds `.is-hidden` — no `pageImage.decode()`, no book/src check, nothing that could reveal a foreign book's bitmap. (3) initialBookIsKnown / bookRegistry.default no longer exist (single book, BOOK_ID='standard' hard-pinned). initReader now renders `renderPage(DEFAULT_START_PAGE)` (app.js:3278, standard) then `revealReader()` (app.js:3280). Since the pre-painted img and the render target are BOTH the standard book, there is no other book to flash to — the wrong-manual-flash invariant this finding protected is structurally guaranteed now. The svGeoBook localStorage key the finding keyed on is also gone (no geo).
- **[SURVIVES / medium]** `perf-hidden-drawer-todas-rebuilds` → web/src/app.js:2154
  - CHANGED: Same jank, minus the book-switch trigger. Rebuilds happen twice at boot (app.js:3260, 3296) and on every drawer open (app.js:2247) — never on book switch anymore.
  - FIX NOW: Fix substantially unchanged: (1) early-return in renderActiveTab when the drawer is closed and tab isn't 'buscar', set a dirty flag consumed by openDrawer/activateTab; (2) build into a DocumentFragment, append once; (3) cache the built 'todas' DOM and reuse until songIndex changes (invalidate in hydrateSongIndex, app.js:3245). Drop the 'invalidate in hydrateBookData' clause — hydrateBookData no longer exists.
  - Fully survives, only one call-site (loadBook) removed. renderActiveTab still defaults to renderTabTodas (app.js:2212-2221) which loops the song list appending a <button> per song via renderSongItem (app.js:2154-2210). renderSongItem (app.js:2067-2082) still does `btn.innerHTML = ...` then `container.appendChild(btn)` per item — no DocumentFragment, no cache (grep DocumentFragment: zero hits). Still rebuilt: (a) at boot via hydrateSongIndex→renderActiveTab (app.js:3260), (b) again at initReader tail renderActiveTab (app.js:3296), and (d) on EVERY drawer open — openDrawer (app.js:1031-1040) ends with `switchDrawerMode("browse")` and switchDrawerMode (app.js:2239-2247) `if (isBrowse) renderActiveTab();`. The drawer is still transform-translated off-screen (not display:none), so every rebuild lays out. Only call-site (c) — inside loadBook (old app.js:1271) — is gone with the multi-book layer, so it no longer rebuilds 'on every book switch' (that clause of the title is moot); boot-twice + drawer-open still hold. Song count is now standard-only (~315), same magnitude as audited.
- **[SURVIVES_MOVED / medium]** `perf-mid-mass-deploy-fleet-reload` → web/src/app.js:1999
  - FIX NOW: Fix unchanged in substance: gate requestServiceWorkerActivation (app.js:1932) and the controllerchange handler (app.js:1999) on `relay.hasDirector && relay.following && document.visibilityState === 'visible'` — defer (park the waiting worker, retry on visibility-hidden / hasDirector false / after N min). Line refs move (2245→1932, 2312→1999).
  - Fully survives, just moved. The adaptive update poll still fires `refreshRegistration()` while visible (app.js:1967-1982), updatefound→statechange 'installed'→requestServiceWorkerActivation posts SKIP_WAITING (app.js:1947-1956, 1932-1938), and the controllerchange handler still does `hasReloadedForUpdate = true; ...; window.location.reload();` (app.js:1999-2007) with NO check of relay.hasDirector / relay.following / visibilityState. sw.js still honors SKIP_WAITING. So a deploy still auto-reloads every online follower within ≤60s mid-hymn — unchanged by the refactor (the reload aftermath is now cheaper per device: no geo poll, no book resolve, just reveal+relay reconnect — but the reload herd itself is identical).
- **[SURVIVES_MOVED / medium]** `perf-no-screen-wake-lock-web-followers` → web/src/app.js (applyRelaySnapshot ~3060 / startRelayFollow)
  - FIX NOW: Fix unchanged: on relay.hasDirector becoming true, `navigator.wakeLock?.request('screen')` wrapped in try/catch, retain the sentinel on `relay`, re-request on visibilitychange→visible, release when hasDirector goes false. Hook point is applyRelaySnapshot (now ~app.js:3060, was cited 3428) and startRelayFollow.
  - Fully survives. grep -n 'wakeLock|keepAwake|keep-awake|WakeLock' over web/src/app.js AND web/src/index.html → ZERO matches (confirmed this run). There is still no navigator.wakeLock anywhere, so a signovivo.com follower iPad/phone still auto-locks ~2 min into Mass and loses the live feed until manually woken. The refactor didn't add a wake lock. LOC 3665 (startRelayFollow, the visibilitychange recovery path the finding cited) shifted; hasDirector transition is now handled in applyRelaySnapshot around app.js:3060 (`if (relay.browsing) { renderRelayPill(); revealReader(); return; }` / revealReader at 3074).
- **[PARTIALLY_MOOT / low]** `perf-offline-poll-storm-battery` → web/src/app.js:3100
  - CHANGED: On a relay-down follower: only two overlapping loops now — the 4s forced /state poll (app.js:3100, 6s abort) and the ≤8s WS reconnect (app.js:3184). The 5s geo re-heal loop is gone. The svGeoBook/relayGeoBookApplied preconditions in the finding no longer exist.
  - FIX NOW: Add failure-aware decay: track consecutive relayPollOnce failures, stretch the 4s poll (4s→15s→30s, reset on success / online / visibility), add an in-flight guard in relayPollOnce (app.js:3083). Drop the 'reHealGeo 5s' clause entirely. Keep instant-recovery triggers.
  - ONE of the three loops is MOOT: the 5s geo re-heal (`setInterval(() => { if (relayGeoBookApplied) return; relayPollOnce(true); }, 5000)`) is DELETED with the geo system — grep for relayGeoBookApplied / reHealGeo / '5000' geo loop in app.js: zero hits; app.js:5 comment confirms 'no geo resolution'. What SURVIVES: (1) startRelayPolling's 4s forced relayPollOnce (app.js:3100 `relay.pollTimer = setInterval(() => relayPollOnce(true), 4000)`), still with a 6s AbortController per tick (app.js:3088) and STILL no in-flight dedupe (relayPollOnce app.js:3083-3097 has no pending-guard), and (2) the WS reconnect backoff capped at 8000ms (app.js:3184 `relay.backoff = Math.min(relay.backoff * 2, 8000)`). Plus the ≤60s SW poll (separate finding). So the storm is now a DUO not a trio — meaningfully lighter but still ~2 failed attempts every 4-5s with no failure-based decay.
- **[SURVIVES_MOVED / low]** `perf-sw-update-poll-mass-overhead` → web/src/app.js:1971
  - FIX NOW: Fix unchanged: raise the idle cap (5-10 min) once `relay.hasDirector` is true, keeping the 3s-after-foreground fast path. One-line cadence change in scheduleUpdateCheck (app.js:1967-1973) reading relay.hasDirector. Line ref moves 2284→1971.
  - Fully survives, moved. The adaptive check still backs off only to a 60s cap: app.js:1967-1973 `scheduleUpdateCheck` → `if (document.visibilityState === "visible") refreshRegistration(); swUpdateDelay = Math.min(Math.round(swUpdateDelay * 1.5), 60000);` and re-schedules; refreshRegistration = `registration.update()` (app.js:1958). Registration is still created with `{ updateViaCache: "none" }` (app.js:2011) so each update() is a full network fetch of sw.js. Over a 3h Mass this is still ~180 forced fetches + a radio wake/min, delivering no value mid-Mass. Unchanged by the refactor. (sw.js grew from ~11.75 KB dist claim; still ~single-book but the per-fetch cost order is the same.)
- **[CHANGED / low]** `perf-page-cache-double-retention-disk` → web/src/sw.js:14
  - CHANGED: Same double-retention behavior, single book only: ~25.1 MB per cache → ~50 MB steady (2 versions), ~75 MB transient during the third-cache refill, plus 1-year HTTP-cache <img> copies. The 'both books' / '+3.0 MB hymns-4' clauses are moot.
  - FIX NOW: Fix unchanged: after ensureOfflineBundle writes OFFLINE_READY_KEY (app.js:544), postMessage the SW a PRUNE_OLD_PAGE_CACHES message deleting page caches other than the current PAGE_CACHE (mirror the activate cleanup at sw.js:105-115). Keep 2-version retention until that signal. Only the byte figures in the writeup change.
  - Survives with reduced magnitude. sw.js still has `const PAGE_CACHES_TO_KEEP = 2;` (sw.js:14) and the activate cleanup keeps `new Set([PAGE_CACHE, ...pageCaches.slice(-PAGE_CACHES_TO_KEEP)])` (sw.js:105-115) — two full old page caches survive alongside the filling new one, still with NO app.js-side PRUNE message after ensureOfflineBundle completes (grep PRUNE in sw.js: zero hits). BUT the size math changed: hymns-4 (3.0 MB) is deleted, so a full cache is now ONLY the 25.1 MB standard book, not 28.1 MB. Steady state is ~50 MB (2×25.1) not ~56 MB, transient ~75 MB not ~84 MB during refill. The disk-pressure/Cache-Storage-eviction concern remains, just ~11% smaller.
- **[MOOT / n/a]** `perf-book-switch-stale-prefetch-sets` → deleted (hydrateBookData) — prefetch sets still at app.js:108-109
  - MOOT: the bug required a mid-Mass book switch, which is now impossible. hydrateBookData is DELETED (grep hydrateBookData / switchBook / loadBook in web/src/app.js: zero hits) along with the whole multi-book layer. The prefetch Sets themselves still exist — state.prefetchedPages / state.prefetchingPages (app.js:108-109), number-keyed, and prefetchSongPage still early-returns on `state.prefetchedPages.has(pageNumber) || state.prefetchingPages.has(pageNumber)` (app.js:661). But with exactly ONE book (BOOK_ID='standard', never changes), page numbers never collide across books and the Sets never need clearing — the 'stale old-book entries suppress new-book warming' failure cannot occur. Nothing to fix.
- **[SURVIVES_MOVED / low]** `perf-search-per-keystroke-renormalization` → web/src/app.js:1171
  - FIX NOW: Fix unchanged: precompute `entry._norm = normalizeText(entry.text)` once in loadSearchIndex (app.js:1146/1155) and build a page→song Map once per hydrate so the keystroke path is includes()-only; add a ~120ms debounce to handleSearchInput (app.js:1863, immediate on Enter). Line refs move (loadSearchIndex 1452→1139, searchPages 1494→1171, input listener 2746→2433).
  - Fully survives, moved 1498→1171. searchInput input listener still has NO debounce: app.js:2433 `searchInput.addEventListener("input", handleSearchInput);` (handleSearchInput at app.js:1863). searchPages (app.js:1171-1189) still calls `normalizeText(entry.text)` for every entry on every keystroke (app.js:1185) and `getSongForPage(entry.page)` per matching entry (app.js:1187), where getSongForPage is an O(songIndex) linear scan (app.js:1161-1168). loadSearchIndex (app.js:1139-1159) still does NOT precompute a normalized `_norm` — it just assigns `data.pages` raw. normalizeText (app.js:251) still does NFD + combining-mark strip + toLowerCase per call. The index is now standard-only (the same ~354-entry big book that was always the standard manual), so the ~134 K-char re-transform per keystroke is unchanged.
- **[MOOT / n/a]** `perf-gate-caption-flicker-dueling-backstops` → deleted (was app.js:3878 revealAfterGeoSettle / 3899 liftGateBackstop)
  - MOOT: both dueling loops are DELETED with the geo-gate reveal machine. grep in web/src/app.js for revealAfterGeoSettle / liftGateBackstop / setGateOffline / floorBookOnly / mustHoldForOffline → ZERO matches. The reveal path is now a single immediate `liftGateNow` (app.js:11-16) that just adds `.is-hidden` — no 8s revealAfterGeoSettle loop, no 12s liftGateBackstop loop, no setGateOffline caption toggling, no floorBookOnly, no 5s geo re-heal. The only remaining timer is one belt-and-suspenders `setTimeout(liftGateNow, 4000)` (app.js:3336) which fires ONCE and is idempotent (gateLifted guard) — it cannot flicker anything. The #geo-gate is a static loader (index.html:44-47) hidden immediately on first reveal. The strobing-caption failure is structurally impossible now.

### coherence

- **[SURVIVES_MOVED / high]** `coherence-seq-regression-freezes-ws-followers` → web/src/app.js:3033
  - FIX NOW: Unchanged in substance, only line refs move: in applyRelaySnapshot (app.js:3022) treat a FRESH snapshot (relayIsFreshLive true, app.js:3018) whose seq is lower than relay.lastSeq as a legitimate takeover — track relay.lastTs and when snap.ts > relay.lastTs && snap.seq < relay.lastSeq, rebase relay.lastSeq = snap.seq and apply. Also fix sync-worker/test-client.html (still present) whose lastSeq is never reset. Pair with the ignored-publish fast-forward fix.
  - Bug fully intact. Web follower monotonic guard survives at app.js:3033 `if (!force && Number.isFinite(snap.seq) && snap.seq > 0 && snap.seq <= relay.lastSeq) return;` with lastSeq only ever raised at :3034 `relay.lastSeq = Math.max(relay.lastSeq, snap.seq);` or reset by a MANUAL resync tap (relay.lastSeq = -1 at :3010). Worker's designed seq regression on stale takeover survives at sync-worker/src/index.ts:118-121 `const snapshotStale = this.snapshot.seq === 0 || nowSec - this.snapshot.ts > RELAY_LIVE_MAX_AGE_S; if (!snapshotStale && incomingSeq > 0 && incomingSeq <= this.snapshot.seq) { return { ok: true, seq: this.snapshot.seq, ignored: true }; }`. The refactor removed no seq logic; the director-takeover-freeze scenario (backup cantor with a lower wall-clock seq legally regresses the room, healthy-WS followers silently drop every page turn behind a green pill) is unchanged. relayIsFreshLive still exists at app.js:3018-3020 exactly as the fix relies on. Line moved from audited :3407 → :3033 (the big app.js deletion shifted everything up).
- **[SURVIVES_MOVED / high]** `coherence-ignored-publish-response-never-consumed` → src/directorRelaySync.js:87
  - FIX NOW: Unchanged: in doPublish (src/directorRelaySync.js:62) parse the JSON body on res.ok; if body.ignored === true, fast-forward the local counter `seqCounter = Math.max(seqCounter, Number(body.seq) || 0)` (seqCounter exists at :52) so the next page turn wins immediately. Optionally surface a one-shot warning via relayAuthErrorHandler on repeated ignored responses.
  - Fully survives. Worker still returns the drop signal at sync-worker/src/index.ts:120-121 `if (!snapshotStale && incomingSeq > 0 && incomingSeq <= this.snapshot.seq) { return { ok: true, seq: this.snapshot.seq, ignored: true }; }` with HTTP 200. directorRelaySync still treats any res.ok as success and never parses the body: src/directorRelaySync.js:87-88 `if (res && res.ok) { authErrorNotified = false; }` then only 401/403 at :89. `grep -n ignored src/directorRelaySync.js web/src/app.js` returns nothing — the `ignored` field is still read by no client except the e2e test. The auth-error path fires only on 401/403 (:89), so a handoff director publishing into the fresh window still broadcasts into the void for up to 90s+skew with a fully-healthy-looking app. LOC moved 88→87 (comments trimmed slightly).
- **[PARTIALLY_MOOT / low]** `coherence-fleet-webcached-inflated-cross-book` → web/src/app.js:2831
  - CHANGED: The finding's failure — hymns-4 floor pages counted against the standard total — is impossible now; there is no second book. What remains is only the benign single-book count. If precache is interrupted, pagesCached < 371 → webCached false → dashboard correctly shows partial. The cross-book false-green is eliminated by the two-book removal, not by any change to this code.
  - FIX NOW: The book-scoping fix the audit prescribed is now unnecessary (only one book's pages exist). If desired for robustness, change countCachedPageImages (app.js:2793) to union unique /books/standard/pages/ pathnames across caches instead of Math.max per bucket — but this addresses a residual counting nit, not the audited cross-book bug, which is moot.
  - The cross-book inflation MECHANISM is gone. There is only one book now (BOOK_ID='standard' at app.js:138; the hymns-4 floor / precachePublicFloor that supplied the extra 51 pages is deleted — grep precachePublicFloor → 0). countCachedPageImages still counts every `/pages/` entry unscoped at app.js:2802 `keys.filter((r) => r.url.includes("/pages/")).length`, and the fallback still fires at app.js:2831 `webCached: webCached || (totalPages > 0 && pagesCached >= totalPages)` — but every cached page is now under /books/standard/pages/, so pagesCached >= totalPages is now an honest 'this book fully cached' signal. The concrete '381>=371 fakes green' scenario cannot occur single-book. Residual weakness (Math.max across cache buckets at :2802 rather than unique-pathname union) is a much weaker, different concern than the audited cross-book bug.
- **[SURVIVES / medium]** `coherence-footprint-test-red-and-deploy-web-preview-trap` → e2e/repo-minimal-footprint.test.mjs:15
  - FIX NOW: Unchanged: either bless deploy:web (add --branch main or refuse on non-main, then add 'deploy:web' to the test allowlist at :15) or delete the script; re-run the footprint test to green. Do not append the string without resolving the preview-vs-prod trap.
  - Fully survives and VERIFIED RED at HEAD (ran `node --test e2e/repo-minimal-footprint.test.mjs` → '# fail 1'). The allowlist at e2e/repo-minimal-footprint.test.mjs:15-27 is [android, build:web, ios, ios:local, ios:mpad, postinstall, preios, preios:mpad, start, test:e2e, typecheck] — deploy:web is NOT in it, but package.json:15 still has `"deploy:web": "node ./web/build.mjs && npx wrangler pages deploy web/dist --project-name alvernia-reader"` with NO --branch main, so it deploys a PREVIEW not prod. scripts/release.sh still passes --branch main as the blessed path. Both halves of the finding (red suite + preview trap) are unchanged by the refactor.
- **[CHANGED / medium]** `coherence-director-codes-two-hand-synced-stores` → PdfReaderApp.tsx:53
  - CHANGED: Post-refactor the two-store drift is UNCHANGED (native codes vs worker secret, no check, empty-in-non-release builds, warn-not-fail). What changed: the stale Env-doc sub-issue is already fixed, and the Sión public-code sub-issue is deleted. A new wrinkle: superAdminCodes is a second gitignored-baked set that must ALSO stay in sync (it only changes the confirm-dialog wording, so drift there is cosmetic, not a lockout).
  - FIX NOW: Same core fix, updated targets: add a release-time consistency gate comparing sha256 of director-codes.private.json codes (BOTH standardDirectorCodes and superAdminCodes) against a gated worker hash endpoint; make release.sh HARD-FAIL (not warn at :46) when director-codes.private.json is missing; add TRANSMITTER_CODES + FLEET_DASHBOARD_KEY placeholders to sync-worker/.dev.vars.example. Drop the now-obsolete 'update the stale Env doc' step — index.ts:22-24 is already correct.
  - Core bug survives; two sub-parts moot. Two independent stores with no consistency check still exist: native STANDARD_DIRECTOR_CODES baked from gitignored director-codes.private.json (PdfReaderApp.tsx:53-59, committed director-codes.json is still {"standardDirectorCodes":[],"superAdminCodes":[]}) vs worker TRANSMITTER_CODES secret (validTransmitterCodes at sync-worker/src/index.ts:327-333). Drift modes (a)/(b)/(c) all survive; release.sh:46 still only WARNS on missing private file ('standard-director entry disabled in this build'), never hard-fails. MOOT sub-parts: (1) the Sión/book-scoped-code half is gone — validTransmitterCodes no longer seeds SION_DIRECTOR_CODE (index.ts:327-333, comment 'NO plaintext fallback'); (2) the stale Env doc that claimed 'Defaults to the legacy transmitter code plus the four director codes' is FIXED — index.ts:22-24 now correctly documents the fail-closed reality. NEW surface: SUPER_ADMIN_CODES (PdfReaderApp.tsx:61-67) is a THIRD hand-synced code subset (superAdminCodes in director-codes.json) with the same drift risk. .dev.vars.example still documents only RELAY_DIRECTOR_TOKEN (no TRANSMITTER_CODES/FLEET_DASHBOARD_KEY).
- **[MOOT / n/a]** `coherence-wrong-book-flash-on-standard-boot` → deleted
  - Entirely moot — the two-book + geo-gate machine it targets is deleted. index.html:54 first-paint img is now `books/standard/pages/page-001.webp` (was the hardcoded hymns-4 page-02). There is ONE book, so a 'wrong manual flash' is impossible. tryLiftGate with its decode-guard is GONE (grep tryLiftGate → 0); reveal is now the thin liftGateNow/revealReader at app.js:11-16 which reveals immediately, plus a lone setTimeout(liftGateNow, 4000) at app.js:3336. precachePublicFloor, floorBookOnly, initialBookIsKnown, liftGateBackstop, PUBLIC_FLOOR_BOOK all grep to 0. The #geo-gate div is now a vestigial plain loader (index.html:43 'the app is single-book now, no geo').
- **[MOOT / n/a]** `coherence-ws-interleave-stale-totalpages-clamp` → deleted
  - Moot — the book-switch async race it targets is deleted. switchBook, loadBook, hydrateBookData, bookSwitchGeneration all grep to 0 in web/src/app.js. The described race (msg2 clamping page 203 against a stale totalPages=51 while switchBook's loadBook is mid-fetch) cannot occur: there is one book, state.totalPages is set once, and applyRelaySnapshot explicitly ignores snap.bookId (app.js:3026-3028 comment 'there is no book to switch — the director's page renders straight against the one book'). The WS handler is still un-serialized at app.js:3159 `applyRelaySnapshot(JSON.parse(ev.data)).catch(...)`, but with no book-switch gap there is no stale-totalPages window; renderPage clamps against the single stable total.
- **[SURVIVES_MOVED / low]** `coherence-numpad-code-cap-and-leading-zero-corruption` → web/src/app.js:1068
  - FIX NOW: Unchanged: in goToDraftSong (app.js:1096) branch on the RAW draft before normalization — `if (/^\d{5,}$/.test(state.songDraft)) { const code = state.songDraft; ... }` — so codes bypass Number() and keep leading zeros; raise the appendDigit cap at :1068 to 11 (or 12); keep normalizeSongDraftNumber for the 1-4 digit song path only. Keep the max length equal in the worker comment + PdfReaderApp.
  - Survives on the native path. The 10-digit cap is intact at app.js:1068 `if (state.songDraft.length >= 10) return;` (comment :1066 'director / secret codes run up to 10 digits'). Codes still round-trip through Number(): goToDraftSong at :1097 calls normalizeSongDraftNumber (:619 `const parsed = Number(trimmed); ... return parsed;`) then re-stringifies at :1103 `const code = String(songNumber);` before posting director-code to native at :1108 — leading zeros destroyed, 11th digit refused. The 11-digit legacy 12345678840 is per project memory still in the TRANSMITTER_CODES secret for build-367 compat, so an 11-digit code remains un-enterable. NOTE the web branch now flashes 'Código no válido' (:1113) for long codes since the web app is public/single-book, but the NATIVE branch (:1107-1110) still routes the corrupted code — the audited bug lives there. LOC moved 1374→1068.
- **[SURVIVES / low]** `coherence-relay-base-five-hardcoded-copies` → web/src/index.html:36
  - FIX NOW: Unchanged: single-source the base — native src/relayConfig.ts consumed by PdfReaderApp + directorRelaySync; web keeps __RELAY_BASE__ but build.mjs also rewrites the index.html preconnect href from the same relayBase variable. Add an offline e2e assertion that the native constant equals the web build default.
  - Fully survives — five hardcoded copies of https://signovivo-sync.4j4982y8jp.workers.dev remain: web/src/index.html:36-37 (preconnect + dns-prefetch), web/build.mjs:46 (ALVERNIA_RELAY_BASE default → __RELAY_BASE__), web/src/app.js:2763 (unreplaced-token fallback), PdfReaderApp.tsx:45, src/directorRelaySync.js:12. Still one override mechanism (ALVERNIA_RELAY_BASE at build time only). The split-relay scenario (relay moves to sync.signovivo.com, web repointed but native constants stale → director and followers on different relays, both answer 200, congregation dark) is unchanged. The finding's LOC index.html:36 is still exact.
- **[PARTIALLY_MOOT / low]** `coherence-page-changed-clamps-before-totalpages-update` → PdfReaderApp.tsx:603
  - CHANGED: With one book, msg.totalPages == totalPagesRef.current always, so Math.min(page, totalPagesRef.current) never caps against a mismatched total. The cross-book mis-clamp is impossible; only a cosmetic ordering nit remains.
  - FIX NOW: Optional hardening only (no live bug): reorder lines 603-608 to adopt msg.totalPages before clamping. Not required for correctness in the single-book app.
  - Code-ordering smell survives; concrete failure moot. The clamp-before-adopt ordering is intact at PdfReaderApp.tsx:603-608: `let page = Math.max(1, Number(msg.page) || currentPageRef.current); if (totalPagesRef.current > 0) page = Math.min(page, totalPagesRef.current); currentPageRef.current = page; if (typeof msg.totalPages === "number" && msg.totalPages > 0) { totalPagesRef.current = msg.totalPages; }`. BUT the concrete failure (native follower on hymns-4 totalPagesRef=51 receiving standard page 300 → clamped to 51) cannot occur: single-book, isBookId only accepts 'standard' (PdfReaderApp.tsx:74), totalPagesRef always holds the standard total, so msg.totalPages always equals the current ref and the interim clamp is a no-op. The book-changed case that the finding also flagged (never touched totalPagesRef) is DELETED (grep book-changed → 0). So the wrong-order is now harmless.
- **[MOOT / n/a]** `coherence-director-code-asyncstorage-not-securestore` → deleted
  - Moot — the director code is no longer persisted at all. STORED_CODE_KEY / 'director_access_code' grep to 0 in PdfReaderApp.tsx. becomeDirector now writes ONLY the role breadcrumb: PdfReaderApp.tsx:440-442 comment 'We deliberately do NOT persist the director code or a timestamp — there is no auto-restore, so a credential must never sit in storage.' then `AsyncStorage.setItem(STORAGE_KEYS.lastSyncRole, "director")`. The audited multiSet that stored [lastSyncRole, lastDirectorAt, STORED_CODE_KEY=code] is gone (auto-director restore removed in commit 38132dd3). The PII-in-plaintext-AsyncStorage exposure the finding described cannot happen because no phone number ever lands in storage. Residual: expo-secure-store is still an installed-but-unused dep (app.config.js:46, package.json:27) — dead-dependency housekeeping, not the security bug the finding raised.
- **[SURVIVES_MOVED / low]** `coherence-vacuous-90s-staleness-test` → e2e/relay-sync.test.mjs:380
  - FIX NOW: Unchanged: replace with an OFFLINE test file that regex-extracts `RELAY_LIVE_MAX_AGE_S\s*=\s*(\d+)` from both web/src/app.js:2766 and sync-worker/src/index.ts:34 and assert.equal the captures; optionally assert the 12s native heartbeat (PdfReaderApp) < window/2. Do not add it to relay-sync.test.mjs (it publishes to the prod room).
  - Fully survives. The vacuous assertion is intact at e2e/relay-sync.test.mjs:380 `appJs.includes("RELAY_LIVE_MAX_AGE_S") || appJs.includes("90")` — still can't fail on a large file (the literal '90' appears in unrelated numbers). The two load-bearing constants both still exist and must still match: sync-worker/src/index.ts:34 `const RELAY_LIVE_MAX_AGE_S = 90;` and web/src/app.js:2766 `const RELAY_LIVE_MAX_AGE_S = 90;`. The divergence scenario (web window changed to 30s without touching the worker, no test catches it) is unchanged. The test also still lives in the PROD-mutating relay-sync.test.mjs, so the fix must relocate it. Finding LOC :380 unchanged; the referenced source constant lines moved (app.js 3079→2766).

**Tally:** CHANGED=16, SURVIVES_MOVED=43, MOOT=24, PARTIALLY_MOOT=11, SURVIVES=25

---

## 5. New findings introduced by the refactor (full text)

These 14 findings are in code that is NEW or substantially rewritten since the original audit
(the director-role state machine + single-book boot), so the original 117 do not cover them.

### Area: director-role

_Hunted the post-audit (builds 371-374) director-role state machine in PdfReaderApp.tsx + its Swift/relay/web edges. The original A3 CRITICAL (director WebView-reload broadcasts boot page 2) STILL LIVES on the mesh-director path — the refactor only defused its crash-relaunch half (no more auto-restore-director), not the content-process-termination half. Three genuinely NEW hazards were introduced by the refactor: (1) removing auto-director means a director whose app restarts silently drops to follower with NO boot-time re-prompt — a fresh route into the exact 2026-07-01 no-director outage class; (2) onDirectorCode bumps roleGenerationRef on EVERY code entry (before the confirm), so a Cancel during boot's becomeFollower retry-sleep supersedes it and strands the device as a link-less follower; (3) lastDirectorSnapshotRef is never cleared, so the "⚠️ Ya hay un director activo / Tomar el control" takeover warning false-fires for the rest of the session after any director ever broadcast, even when nobody is directing. Persisted lastSyncRole + lastPage.<book> are now dead writes (never read back). Most mesh-dependent items are device-gated (2 physical iPads)._

#### [CRITICAL · DEVICE-GATED · REGRESSION] `new-director-role-webview-reload-broadcasts-boot-page`
- **Where:** `PdfReaderApp.tsx:566`
- **Title:** Mesh director surviving a WebView content-process reload clobbers currentPageRef with the web's boot page 2 and broadcasts it to the whole congregation
- **Problem:** The original audit A3 CRITICAL still lives on the mesh-director path (the refactor only defused its crash-relaunch half by killing auto-restore-director; it did NOT touch the reload half). On the director iPad, any WKWebView content-process termination (routine under iOS memory pressure) or a peer bundleUpdated remount reloads the web WITHOUT changing roleRef — roleRef stays 'director' (onContentProcessDidTerminate at line 930-935 only sets webReadyRef=false + reload()). The reloaded web app boots to DEFAULT_START_PAGE=2 (web/src/app.js:3240,3278 — relay.hasDirector is always false in native file mode, so renderPage(2) runs) and posts BOTH page-changed{page:2} (web app.js:986) and bridge-ready{page:2} (web app.js:3298). Native's page-changed handler sets currentPageRef.current=2 and calls broadcastPage(2) (PdfReaderApp.tsx:605,615); bridge-ready then re-clobbers currentPageRef.current=msg.page(=2) (line 566) and, because roleRef==='director', broadcasts again (line 582-584). Concrete Mass failure: director is on page 250 for the Communion hymn, the WKWebView process is reaped under memory pressure, reloads, and within ~2s EVERY mesh follower iPad AND every signovivo.com phone (via the relay heartbeat) flips 250→2, and the director's own screen shows 2. The persisted sv.book.lastPage.standard (written at line 611-614) is NEVER read back anywhere, so there is no restore to the real page.
- **Evidence:** bridge-ready case: `if (typeof msg.page === "number") currentPageRef.current = msg.page;` (PdfReaderApp.tsx:566) then `if (roleRef.current === "director") { // The director's own page is authoritative — just re-broadcast it. broadcastPage(currentPageRef.current, currentBookRef.current); }` (582-584) — the comment claims authority but the ref was just overwritten by the web's boot page. page-changed case: `let page = Math.max(1, Number(msg.page) || currentPageRef.current); ... currentPageRef.current = page; ... broadcastPage(page, currentBookRef.current);` (603-615) — the clamp caps wild pages but page 2 is valid so it passes through. web/src/app.js:3278 `if (!relay.hasDirector) renderPage(DEFAULT_START_PAGE, { pushToHistory: false });` + 3298-3303 bridge-ready posts `page: state.currentPage` (=2).
- **Fix:** On the director iPad, the native shell — not the web boot — is authoritative for the page across a reload. In BOTH the bridge-ready and page-changed handlers, when roleRef.current==='director' (or explicitTransmitterRef), do NOT adopt msg.page; instead re-assert native's own currentPageRef by injecting a sync-event{page: currentPageRef.current} back into the freshly-loaded web and broadcasting THAT. Guard specifically the first post-reload page-changed (which is the unsolicited boot render, not a real user turn). Cheapest correct version: read back sv.book.lastPage.standard on the reload path, or track a webReloadPendingRef that suppresses the first inbound page-changed for a director and replaces it with currentPageRef.
- **Verify:** Two-iPad test: director on page 250, background/foreground to force a WKWebView reap (or call reload), confirm the follower iPad and a signovivo.com tab stay on 250 (not 2). Unit-level: mock a bridge-ready{page:2} arriving while roleRef==='director' and currentPageRef=250, assert broadcastPage is called with 250 not 2.
- **Regression of:** Original audit finding relay-director-webview-reload-broadcasts-boot-page (A3). The 371-374 refactor removed the auto-restore-director path (so the crash+relaunch variant is gone — boot now always becomeFollower), but the mesh-director WebView-reload variant is untouched and still live.

#### [HIGH · REGRESSION] `new-director-no-auto-restore-silent-demotion-outage`
- **Where:** `PdfReaderApp.tsx:757`
- **Title:** No-auto-director boot silently demotes a director to follower on any app restart, re-opening the 2026-07-01 no-director outage class with no visible re-prompt
- **Problem:** The refactor (38132dd3) removed auto-restore-director entirely: boot now UNCONDITIONALLY calls becomeFollower() (line 757), and STORAGE_KEYS.lastSyncRole — still written on every become* (lines 376, 442) — is NEVER read back anywhere (verified: only setItem/removeItem references exist). Design intent is 'always ask' (Miguel), which is fine for a deliberate relaunch, but it creates a Mass-critical operational hazard identical in OUTCOME to the 2026-07-01 outage (7 devices, all followers, ZERO directors all night). Concrete scenario: mid-Mass or during setup the director's iPad app is killed by iOS (memory pressure / OOM / a crash caught by the ErrorUtils trap) and relaunches. It boots straight to follower and looks completely normal — it renders pages, follows the mesh — giving the director NO signal that they have lost the role. If they don't happen to notice they're no longer broadcasting, the entire congregation (mesh followers + web) freezes on the last page the director sent before the crash, and no device is directing. Unlike the old build, nothing re-promotes them and nothing prompts 'you were directing — resume?'.
- **Evidence:** `if (!didBootstrapRef.current) { didBootstrapRef.current = true; ... becomeFollower(); }` (PdfReaderApp.tsx:751-758) with the comment 'NO auto-director: a device ALWAYS boots as a follower.' becomeDirector persists lastSyncRole='director' at line 442 with the comment 'Record the role (breadcrumb only) ... there is no auto-restore', and grep confirms lastSyncRole is written at 376/442, removed at 487, and read NOWHERE.
- **Fix:** Do not silently restore the director role (respect 'always ask'), but on boot READ lastSyncRole; if it was 'director', surface a one-tap confirm on launch ('Estabas dirigiendo — ¿continuar como director?') that calls becomeDirector with a re-entered/confirmed code, OR at minimum inject a persistent visible banner into the web telling the ex-director they are now a follower and must re-enter their code to resume. Silent demotion of the one role the whole Mass depends on must never be invisible.
- **Verify:** Two-iPad: make iPad A director on page 100, force-quit and relaunch the app; confirm the operator gets a visible prompt/banner rather than a normal-looking follower screen, and that iPad B / web are not left frozen with no director. Unit: assert boot reads lastSyncRole and, when 'director', does not just becomeFollower() with zero UI.
- **Regression of:** commit 38132dd3 (remove auto-director entirely) — the prior build auto-re-promoted a director within DIRECTOR_RESTORE_WINDOW_MS; that safety net is gone with no replacement signal.

#### [HIGH · DEVICE-GATED · REGRESSION] `new-director-code-gen-bump-before-confirm-strands-follower`
- **Where:** `PdfReaderApp.tsx:503`
- **Title:** onDirectorCode bumps roleGenerationRef on every code entry BEFORE the confirm; cancelling during boot's becomeFollower retry-sleep supersedes it and strands the device as a link-less follower
- **Problem:** onDirectorCode increments roleGenerationRef.current at line 503 for EVERY code entry (valid, invalid, or one that leads to a confirm dialog) — before it is known whether becomeDirector will ever run. becomeDirector only runs inside the Alert's onPress (line 545). Concrete race: on a fresh boot, becomeFollower() (line 757) hits a transient mesh startup failure and enters its retry path `await new Promise(r => setTimeout(r, 2000))` (line 384) with roleRef already set to 'follower' (line 372). During that 2s window the operator types their director code and taps enter → line 503 bumps the generation → the confirm Alert appears → operator taps 'Cancelar' → becomeDirector is never called. The sleeping becomeFollower now wakes at line 385 `if (myGen !== roleGenerationRef.current) return;`, sees its generation was superseded, and RETURNS WITHOUT the second startNearbyFollower — so no mesh follower session is ever established. roleRef stays 'follower', the web shows normal follower UI, but the device receives ZERO director pages. Recovery is broken too: the ⟳ resync only re-joins via becomeFollower when roleRef==='off' (line 627), but here roleRef is 'follower', so ⟳ just calls requestCurrentSnapshot() against a mesh session that doesn't exist. The device is stranded until an AppState foreground or another full code entry.
- **Evidence:** `onDirectorCode = useCallback((rawCode) => { roleGenerationRef.current++; const code = digitsOnly(rawCode); ...` (PdfReaderApp.tsx:503) — the bump is unconditional and precedes the empty/soft-reset/isDirectorCode checks and the Alert. becomeFollower's retry: `await new Promise((r) => setTimeout(r, 2000)); if (myGen !== roleGenerationRef.current) return; await startNearbyFollower(DIRECTOR_SESSION);` (384-386). ⟳ recovery gate: `if (roleRef.current === "off" && syncAvailable) becomeFollower();` (627) — does not fire for roleRef==='follower'.
- **Fix:** Move the roleGenerationRef bump OUT of the top of onDirectorCode. Only bump when a role transition is actually committed: inside performSoftReset (already bumps at 479) and inside becomeDirector's onPress (already bumps at 400). The unrecognized-code and Cancel paths must NOT bump, so an in-flight becomeFollower is never falsely superseded. Alternatively, have Cancel re-arm the follower (call becomeFollower()) so a bumped-then-cancelled entry can't strand.
- **Verify:** Two-iPad, or a mock of startNearbyFollower that throws once then resolves: trigger boot becomeFollower into its 2s retry, fire onDirectorCode with a valid code, then invoke the Alert Cancel; assert the second startNearbyFollower still runs (mesh follower session established) and injectEvent role:'follower' fires. Regression-guard: assert a wrong-code entry does not bump the generation while becomeFollower is mid-flight.
- **Regression of:** commit 9473e596 / 38132dd3 — pre-confirm builds called becomeDirector synchronously so the generation bump immediately claimed a real transition; splitting entry (bump) from commit (onPress) opened this cancel-strands-follower gap.

#### [MEDIUM · DEVICE-GATED · REGRESSION] `new-director-livedirector-warning-never-clears`
- **Where:** `PdfReaderApp.tsx:528`
- **Title:** lastDirectorSnapshotRef is never cleared, so the destructive 'Ya hay un director activo / Tomar el control' takeover warning false-fires for the rest of the session after any director ever broadcast
- **Problem:** The liveDirector heuristic that decides whether to show the scary red-destructive takeover confirm reads `Boolean(lastDirectorSnapshotRef.current) && roleRef.current !== 'director'` (line 527-528). lastDirectorSnapshotRef is SET whenever any mesh 'page' arrives (line 773) but is NEVER cleared — not on DIRECTOR_CONFLICT, not on exit-director, not when the director leaves the mesh, not on becomeFollower. So once this device has received even one director snapshot this session, the flag is stuck true. Concrete Mass scenario: Braulio directs the opening hymn, then taps exit after the Gloria; no one is directing. Later Miguel enters his code to lead the recessional. His iPad received Braulio's snapshots earlier, so it shows '⚠️ Ya hay un director activo — Otro dispositivo está dirigiendo AHORA. Si continúas tú tomas el control' with a red destructive 'Tomar el control' button — falsely implying he is about to yank the role from a live director when there is none. At best confusing mid-liturgy; a cautious operator may cancel and delay the recessional. The heuristic also fails the other direction (a genuinely live director this device only just discovered, before its first snapshot, shows the mild '¿Dirigir el coro?' with no takeover warning).
- **Evidence:** `const liveDirector = Boolean(lastDirectorSnapshotRef.current) && roleRef.current !== "director";` (PdfReaderApp.tsx:527-528); title/body/destructive-button all branch on liveDirector (529-544). grep shows lastDirectorSnapshotRef is set at 773 and read at 528/585/636/869 but assigned only once (773) — never reset to null on any teardown/conflict/exit path.
- **Fix:** Make liveDirector reflect a RECENT snapshot, not an ever-set flag: store the snapshot ts alongside page/book and treat liveDirector as true only if the last director packet arrived within ~the mesh-heartbeat window (e.g. <5-8s). Also clear lastDirectorSnapshotRef (or its recency) on DIRECTOR_CONFLICT and exit-director. That way the takeover warning fires only when a director is actually broadcasting right now.
- **Verify:** Two-iPad: A directs, then A exits; on B, wait past the heartbeat window and enter a director code — assert B shows the plain '¿Dirigir el coro?' (not the red takeover warning). Inverse: A is actively broadcasting, B enters a code and asserts the takeover warning DOES show.
- **Regression of:** commit 9473e596 (confirm-before-takeover) — the liveDirector heuristic was introduced here and reuses lastDirectorSnapshotRef, which predates it and was never designed to be cleared.

#### [LOW] `new-director-super-admin-label-gated-by-standard-set`
- **Where:** `PdfReaderApp.tsx:516`
- **Title:** Super-admin label depends on the code also being in STANDARD_DIRECTOR_CODES; a super-admin-only code is silently rejected as 'código incorrecto' — a latent bake/config footgun
- **Problem:** SUPER_ADMIN_CODES (line 61-65) is baked independently from directorCodes.superAdminCodes, and isSuperAdmin (line 526) is only evaluated AFTER the hard gate `const isDirectorCode = STANDARD_DIRECTOR_CODES.has(code); if (!isDirectorCode) { injectEvent role:'none'; return; }` (516-521). Today the private file keeps superAdminCodes a strict subset of standardDirectorCodes, so this is correct. But it is a fragile, unenforced invariant: if a future edit to director-codes.private.json (or release.sh) ever lists a super-admin number that is NOT also in standardDirectorCodes, that number is rejected as 'código incorrecto' and can never direct — despite being the highest-privilege code. There is no build-time assertion that superAdminCodes ⊆ standardDirectorCodes.
- **Evidence:** `const isDirectorCode = STANDARD_DIRECTOR_CODES.has(code); if (!isDirectorCode) { injectEvent({ type: "role", role: "none" }); return; }` (PdfReaderApp.tsx:516-521) precedes `const isSuperAdmin = SUPER_ADMIN_CODES.has(code);` (526). director-codes.private.json comment states 'superAdminCodes is a SUBSET of standardDirectorCodes' — an invariant enforced only by hand.
- **Fix:** Either fold super-admin into the auth gate — `const isDirectorCode = STANDARD_DIRECTOR_CODES.has(code) || SUPER_ADMIN_CODES.has(code);` so a super-admin code is always accepted — or add a build/runtime assertion (in release.sh or at module load) that every superAdminCode is present in standardDirectorCodes, failing the bake if not.
- **Verify:** Add a unit/bake check: for every c in superAdminCodes assert standardDirectorCodes.includes(c). Manually: temporarily add a super-admin-only code and confirm current code rejects it (demonstrating the gap), then confirm the fix accepts it.

#### [LOW · REGRESSION] `new-director-dead-writes-laststate-role-and-page`
- **Where:** `PdfReaderApp.tsx:442`
- **Title:** lastSyncRole and sv.book.lastPage.standard are written on every role change / page turn but never read back — dead writes that mask the lost restore capability
- **Problem:** After the refactor, STORAGE_KEYS.lastSyncRole (written at lines 376 and 442, removed at 487) and STORAGE_KEYS.lastPagePrefix+'standard' (written every page-changed at lines 611-614) are never read anywhere in the app (grep-confirmed: only setItem/removeItem/clearAllBookState). They are pure dead writes on the liturgy-hot page-turn path — small AsyncStorage churn per page turn for data no code consumes. More importantly they are a maintenance trap: a future engineer sees lastSyncRole='director' and lastPage persisted and reasonably assumes restore works, when in fact boot ignores both (see the silent-demotion finding). This is hygiene, but on the director device the per-page-turn AsyncStorage.setItem is needless work during Mass.
- **Evidence:** page-changed persists every turn: `AsyncStorage.setItem(`${STORAGE_KEYS.lastPagePrefix}${currentBookRef.current}`, String(page)).catch(() => {});` (PdfReaderApp.tsx:611-614) with the comment 'Persist per-book last page for restore.' — but no restore reads it. lastSyncRole write at 442 comments 'breadcrumb only'. grep across PdfReaderApp.tsx/src/web confirms zero getItem on either key.
- **Fix:** Either wire the persisted page into the boot/reload restore (which would also help the A3 fix) or delete the dead writes and their misleading 'for restore' comments to stop the per-page-turn AsyncStorage write and remove the maintenance trap.
- **Verify:** Static: grep confirms no reader. If keeping the writes, add the restore read and a test that boot/reload rehydrates the persisted page; if deleting, assert page-changed no longer calls AsyncStorage.setItem.
- **Regression of:** commit 38132dd3 — removed the readers (STORED_CODE_KEY/lastDirectorAt restore + storedBookRef) but left these two writers in place.

### Area: single-book-worker-web

_I hunted the NEW/rewritten single-book web boot (app.js initReader + reveal, ~904 deleted lines), index.html/build.mjs trimming, and the reworked worker fetch handler (geo/X-Hymnal/book-scoping removed, /unlock stubbed). The refactor is unusually well-hardened: the white-screen trap is defended by FOUR independent reveal paths (initReader revealReader, the boot-catch revealReader, applyRelaySnapshot revealReader, and the 4000ms setTimeout(liftGateNow) backstop), the worker wraps every branch AND the whole handler in try/catch that always preserves CORS and degrades to EMPTY_SNAPSHOT, page/seq are Number.isFinite-guarded, renderPage clamps out-of-range director pages, and manifests (pages.json/search-index.json) still match what app.js/sw.js/build.mjs write. bookId is vestigial-but-harmless (still sent by directorRelaySync/DO, read by nobody), /unlock is a dead stub no client calls, and /log is unauthenticated but pre-existing (audited). I found NO critical or high defect newly introduced by this refactor. The reportable items are genuine but low-severity: a fragile padding/clamp fallback that is currently defended-in-practice by inlined #pages-data, a dead inline #books-data blob, and a diverged renderStatus totalPages fallback. No Mass-breaking regression surfaced in this surface._

#### [LOW · REGRESSION] `new-web-boot-padwidth-clamp-fallback-fragile`
- **Where:** `web/src/app.js:206`
- **Title:** pagePadWidth/clampPage use state.totalPages=1 initial value; the `1 || 371` fallback never engages, so a renderPage before the manifest lands would clamp to page 1 and request page-1.webp (404)
- **Problem:** state.totalPages initializes to 1 (app.js:94). pagePadWidth() is `String(state.totalPages || STANDARD_TOTAL_PAGES || 0).length` (app.js:206) — but 1 is TRUTHY, so before the manifest lands the fallback to STANDARD_TOTAL_PAGES (371) never fires and pad width is 1 (page-2.webp, not page-002.webp). Likewise clampPage (app.js:223-227) uses total=1, so any target page clamps to 1. Concrete Mass failure IF a renderPage ever runs before initReader sets state.totalPages=371: a director on page 200 would render page 1 AND request a 404 filename (page-200 -> page-1.webp) that sticks. In CURRENT builds this is unreachable because build.mjs inlines #pages-data (web/build.mjs:682) so `state.totalPages = manifestTotal` (app.js:3236) runs synchronously at the top of initReader before any await or any renderPage, and the native bridge posts pages asynchronously (queued after that synchronous prefix). It only becomes reachable if #pages-data is absent (fetch path awaited, app.js:3231) AND a renderPage fires during the await — a combination current builds don't produce. Reporting as a latent fragility the refactor's single-book simplification left behind: the guard reads defensive but does not actually protect the 1-page window.
- **Evidence:** app.js:94 `totalPages: 1,`; app.js:206 `const pagePadWidth = () => String(state.totalPages || STANDARD_TOTAL_PAGES || 0).length;`; app.js:226 `const total = Number.isFinite(state.totalPages) && state.totalPages > 0 ? state.totalPages : 1;`; app.js:3236 `state.totalPages = manifestTotal;` (only set after manifest parse).
- **Fix:** Initialize state.totalPages to STANDARD_TOTAL_PAGES (371) instead of 1 (app.js:94), OR change the fallback to treat 1 as unset (e.g. `state.totalPages > 1 ? state.totalPages : STANDARD_TOTAL_PAGES`). This makes the pre-manifest pad width and clamp correct even if a renderPage ever races the manifest.
- **Verify:** Unit-test pagePadWidth() and clampPage(200) with state.totalPages===1 — currently returns width 1 and clamps to 1; after the fix returns width 3 and clamps to 200. No device needed.
- **Regression of:** Introduced by the 318d6f39 single-book refactor: the old two-book code hydrated totalPages per-book before paint; the collapsed path left state.totalPages seeded at 1 with a fallback that a value of 1 defeats.

#### [LOW · REGRESSION] `new-web-renderstatus-total-fallback-divergence`
- **Where:** `web/src/app.js:740`
- **Title:** renderStatus/paging use raw state.totalPages (fallback 1) while pageFileName uses STANDARD_TOTAL_PAGES (371) — divergent fallbacks in the same pre-manifest window
- **Problem:** During the (currently sub-frame) window where state.totalPages is still 1, next-song/next-page availability in renderStatus is computed against totalPages=1/totalSongs=0 while pageFileName pads against 371. If a renderPage ever executes in that window (see new-web-boot-padwidth-clamp-fallback-fragile for reachability), nav buttons would briefly disable/enable inconsistently with the actual 371-page book. Same root cause as the padding finding — the single-book collapse left two different 'unknown total' fallbacks (raw state.totalPages vs STANDARD_TOTAL_PAGES) that only agree once the manifest lands.
- **Evidence:** app.js:738-740 `const hasNextSong = currentSongIndex < 0 ? state.totalSongs > 0 : currentSongIndex < state.totalSongs - 1;` and app.js:686 `if (n < 1 || n > state.totalPages) continue;` (prefetch bounded by raw totalPages) vs app.js:206 pageFileName padding to STANDARD_TOTAL_PAGES.
- **Fix:** Same fix as the padding finding — seed state.totalPages to STANDARD_TOTAL_PAGES so all consumers share one consistent pre-manifest total, eliminating the divergence.
- **Verify:** With state.totalPages===1, assert prefetchNeighborPages(200) skips all neighbors (n>1) while pageFileName(201) still targets a valid padded path — divergence visible; after seeding to 371 they agree. No device needed.
- **Regression of:** Same 318d6f39 single-book refactor that seeded state.totalPages=1 with a STANDARD_TOTAL_PAGES fallback only wired into pageFileName.

#### [LOW · REGRESSION] `new-web-dead-books-data-inline-blob`
- **Where:** `web/build.mjs:681`
- **Title:** build.mjs still inlines a #books-data <script> that app.js no longer reads (dead payload)
- **Problem:** The refactor removed the book registry from app.js (no getElementById('books-data'), no loadBookRegistry — confirmed by grep: the only book-registry references left in app.js are '/books.json' in SHELL_ASSETS), but build.mjs still injects `<script id="books-data">${JSON.stringify(booksManifest)}</script>` into every built index.html. It is inert (nothing reads it), so no runtime failure — pure dead weight and a stale-comment hazard: the inline comment (build.mjs:676) still describes it as feeding a live registry, which will mislead the next maintainer into thinking book-switching still exists. No Mass impact; hygiene only.
- **Evidence:** web/build.mjs:681 `\`  <script id="books-data" type="application/json">${JSON.stringify(booksManifest)}</script>\n\` +`; grep for 'books-data' in app.js returns zero references (only '/books.json' in SHELL_ASSETS at app.js:190).
- **Fix:** Drop the #books-data inline script tag from build.mjs (keep only #pages-data, which initReader does read at app.js:3228), and update the surrounding comment. Purely a cleanup — verify the built index.html still parses and initReader still finds #pages-data.
- **Verify:** After removing the tag, run `node web/build.mjs` (read-only build) and grep the built dist/index.html: confirm #pages-data present, #books-data gone, and the site boots. No device needed.
- **Regression of:** 318d6f39 removed the app.js reader of #books-data but left the build.mjs injector, orphaning the blob.

### Area: refactor-coherence-regressions

_Hunted the 371-374 refactor delta (git diff 309a9afa..HEAD) for the classic "big deletion left something half-wired" bug class across web/worker/native. The good news: the deletions were clean — NO dangling references to removed symbols survive in the real working tree (all `.claude/worktrees/*` hits are OTHER tabs' checkouts at older commits and were excluded per multi-tab safety). offlineBooks.ts, the worker fetch handler, directorRelaySync.js, and the app.js reveal path are all internally coherent; the vestigial bookId/mode/`/unlock` fields are harmless (still written/stored, never read for authz/routing; page+seq+ts+auth are the only live semantics). The reveal path is well-guarded (initReader has its own catch→revealReader plus a top-level `setTimeout(liftGateNow,4000)` backstop). The real finds are: (1) a genuine NEW-SURFACE regression in the always-confirm director dialog — `lastDirectorSnapshotRef` is set-once-never-cleared, and commit 38132dd3 newly reuses it as a "is a director live RIGHT NOW" signal, so the destructive-styled "⚠️ Ya hay un director activo / Otro dispositivo está dirigiendo AHORA" warning fires falsely once any director page has EVER arrived this session, even 20 min after that director left; (2) two e2e tests still assert deleted two-book semantics — one (`relay-sync.test.mjs`) is vacuously green AND is the forbidden prod-relay-publishing file wired into `test:e2e`; and (3)/(4) type + doc drift (the `.d.ts` and HANDOFF.md still describe the deleted nonStandard/two-book/geo world). Confirmed the no-mesh-follower boot gap (`if(!syncAvailable)return` before becomeFollower + native skips relay) is PRE-EXISTING (identical in build 370), not a refactor regression — excluded._

#### [HIGH · DEVICE-GATED · REGRESSION] `new-refactor-stale-livedirector-warning`
- **Where:** `PdfReaderApp.tsx:527`
- **Title:** Always-confirm director dialog shows a FALSE "another director is live, take control" warning because lastDirectorSnapshotRef is never cleared
- **Problem:** The new always-confirm flow (commit 38132dd3) computes liveDirector = Boolean(lastDirectorSnapshotRef.current) && roleRef.current !== "director" to decide whether to show the alarming, destructive-styled takeover dialog ('⚠️ Ya hay un director activo' / body 'Otro dispositivo está dirigiendo AHORA. Si continúas, tú tomas el control...' / red 'Tomar el control' button). But lastDirectorSnapshotRef is set-once-never-cleared: it is only ever WRITTEN at line 773 (`lastDirectorSnapshotRef.current = { page, book }`) when any director's mesh page arrives, and NO mesh event ('state' with peerCount:0, 'error', director-gone, disconnect) ever nulls it. In build 370 this ref existed but was used ONLY for best-effort resync (lines 585,636 — where never-clearing is correct). 38132dd3 repurposed it as a live-director presence signal, which it is not. Mass-time failure: the choir practices, a director directs for a song, then steps down (exit-director) or leaves the room; 20 minutes later the actual director for THIS Mass walks up to that same iPad and enters their code to start directing. Instead of the calm '¿Dirigir el coro? — Sí, dirigir', they get a red-alert 'Ya hay un director activo / Otro dispositivo está dirigiendo AHORA / Tomar el control' implying they'd yank control from a phantom peer who isn't there. At worst this makes the real director hesitate/cancel (thinking someone else is already driving) — no director gets promoted, followers freeze, exactly the outage class the 371-374 work was trying to prevent.
- **Evidence:** PdfReaderApp.tsx:527-528 `const liveDirector = Boolean(lastDirectorSnapshotRef.current) && roleRef.current !== "director";` — the ONLY writer is PdfReaderApp.tsx:773 `lastDirectorSnapshotRef.current = { page, book };` (no timestamp: line 105 `useRef<{ page: number; book: BookId } | null>(null)`), and grep shows no assignment of `lastDirectorSnapshotRef.current = null` anywhere; the 'state'/'error' mesh cases (PdfReaderApp.tsx:788,805) never clear it.
- **Fix:** Give the ref a timestamp and treat it as 'live' only within the mesh freshness window (mirror the relay's RELAY_LIVE_MAX_AGE_S / the ~2s mesh heartbeat), OR clear lastDirectorSnapshotRef when a director-departed/no-peers signal arrives (e.g. a 'state' event with peerCount 0 / directorCount 0), OR gate liveDirector on an actually-live signal (roleRef.current === 'follower' AND a recent director heartbeat) rather than the sticky last-known-page ref. The resync call sites (585,636) should keep reading the sticky ref unchanged.
- **Verify:** Two physical iPads: A directs one page (B receives it), A taps exit-director. Wait >2s. On B, enter a valid director code → EXPECTED calm '¿Dirigir el coro?'; CURRENT shows the false '⚠️ Ya hay un director activo / Tomar el control'. Unit-level: after injecting a mesh 'page' then a director-gone, assert the confirm title is '¿Dirigir el coro?' not the takeover string.
- **Regression of:** 38132dd3 (remove auto-director + always-ask + super-admin) newly introduced the liveDirector takeover-warning that reads the pre-existing set-once-never-cleared lastDirectorSnapshotRef as a live-presence signal

#### [MEDIUM · REGRESSION] `new-refactor-relay-test-asserts-dead-twobook`
- **Where:** `e2e/relay-sync.test.mjs:185`
- **Title:** relay-sync e2e test still asserts deleted two-book semantics (mode:nonStandard / bookId:hymns-4 round-trip) — vacuously green AND it publishes to the PRODUCTION Mass room
- **Problem:** The 'mode and bookId are persisted in state' test publishes {mode:'nonStandard', bookId:'hymns-4'} to the live relay and asserts they round-trip. Post-refactor the app is single-book: 'nonStandard' and 'hymns-4' are deleted concepts; the worker only stores these fields verbatim as vestigial passthrough (index.ts:127-128 slice to 64 chars, never read). So the test stays GREEN not because the behavior is correct but because the DO echoes any string — it now asserts a dead concept and would MASK a legitimate future hardening (e.g. if someone makes the worker reject non-'standard' bookId, this test goes red for the wrong reason and looks like a real regression). Worse, this whole file targets BASE=signovivo-sync...workers.dev ROOM='alvernia-main' — the SAME room the live Mass followers subscribe to — and it is wired into `test:e2e` (package.json:17 `node --test ./e2e/*.test.mjs`). Anyone running `npm run test:e2e` publishes page 55 (and every other test page) into the live room mid-Mass, flipping every signovivo.com follower's page — the exact forbidden hazard, now made more likely because the stale hymns-4 assertion invites 'let me just fix that test'.
- **Evidence:** e2e/relay-sync.test.mjs:187 `await publish({ v: 1, page: 55, seq, mode: "nonStandard", bookId: "hymns-4" });` then :189-190 `assert.equal(snap.mode, "nonStandard"); assert.equal(snap.bookId, "hymns-4");`. File header :1-8 'live against the deployed Cloudflare Worker'; :16-17 `BASE=...workers.dev`, `ROOM="alvernia-main"`. package.json:17 `"test:e2e": "node --test ./e2e/*.test.mjs"`. Worker only stores verbatim: index.ts:127-128 `mode: String(...).slice(0,64), bookId: String(...).slice(0,64)`, and applyRelaySnapshot (app.js:687) 'kept on the wire for build-373 compat' — bookId is never read for routing.
- **Fix:** Change the assertion to the single-book reality (publish mode:'standard', bookId:'standard' and assert those), or delete the dead-behavior test outright per the 'prefer deleting dead-behavior tests' rule. Independently, this file must never run against the prod room in CI — point it at a throwaway room name or a preview worker, or gate it behind an explicit opt-in env var, so `test:e2e` can't flip live Mass followers.
- **Verify:** Static: grep e2e/relay-sync.test.mjs for 'hymns-4'/'nonStandard' → should be zero after fix. Do NOT run the file against alvernia-main (forbidden). Verify the room constant is no longer 'alvernia-main' before allowing it in test:e2e.
- **Regression of:** 318d6f39 (remove two-book+geo) deleted the hymns-4/nonStandard concepts but left this test asserting them; the test's prod-room targeting predates the refactor

#### [LOW · REGRESSION] `new-refactor-contract-test-pins-dead-nonstandard-dts`
- **Where:** `e2e/nearby-sync-contract.test.mjs:23`
- **Title:** nearby-sync-contract test pins the now-dead nonStandard type in the .d.ts, coupling green CI to deleted two-book behavior
- **Problem:** The contract test asserts the native bridge .d.ts still declares `context?: { mode?: "standard" | "nonStandard"; bookId?: string }`. After the refactor AppMode/BookId were narrowed to just 'standard' in offlineBooks.ts (AppMode='standard', BookId='standard'), but src/nearbyDirectorSync.d.ts was NOT updated and still declares the 'nonStandard' union (lines 15,30). The test therefore keeps CI green by REQUIRING the stale type to remain. This is dead-behavior coupling: if someone correctly narrows the .d.ts to match offlineBooks.ts, this assertion goes red for no real reason; conversely it prevents the type from ever being cleaned. Not a Mass-time crash, but it locks in the drift and can misdirect a future engineer into thinking two-book is still live in the mesh contract.
- **Evidence:** e2e/nearby-sync-contract.test.mjs:23 `assert.match(dtsSyncSource, /context\?: \{ mode\?: "standard" \| "nonStandard"; bookId\?: string \}/);` matched against src/nearbyDirectorSync.d.ts:15 `context?: { mode?: "standard" | "nonStandard"; bookId?: string }` and :30 `mode?: "standard" | "nonStandard";` — while src/offlineBooks.ts:16-17 now declares `export type AppMode = "standard"; export type BookId = "standard";`.
- **Fix:** Narrow the .d.ts to `mode?: "standard"` (or keep a permissive `string` for wire tolerance) and update this assertion to match, so the mesh contract reflects the single-book reality instead of pinning the deleted union.
- **Verify:** grep src/nearbyDirectorSync.d.ts for 'nonStandard' → zero after fix; re-run node --test e2e/nearby-sync-contract.test.mjs (safe, static file-read only, no network) and confirm green.
- **Regression of:** 318d6f39 narrowed AppMode/BookId in offlineBooks.ts but left src/nearbyDirectorSync.d.ts and this contract assertion on the old union

#### [LOW · REGRESSION] `new-refactor-dts-appmode-drift`
- **Where:** `src/nearbyDirectorSync.d.ts:30`
- **Title:** src/nearbyDirectorSync.d.ts still declares mode: "standard" | "nonStandard" after AppMode was narrowed to "standard" only
- **Problem:** The mesh bridge type declaration still exposes `mode?: "standard" | "nonStandard"` (both the sendPageUpdate context at :15 and the listener event at :30) even though offlineBooks.ts AppMode is now `"standard"` only and PdfReaderApp.modeForBook always returns 'standard'. This is harmless at runtime (the field is vestigial passthrough) but it lets TypeScript callers still type-narrow on a 'nonStandard' branch that can never occur, and it disagrees with the source of truth in offlineBooks.ts — the kind of type-vs-runtime drift that misleads the next engineer into thinking two-book mesh sync is still a thing. No Mass-time failure on its own; hygiene/coherence only.
- **Evidence:** src/nearbyDirectorSync.d.ts:15 `context?: { mode?: "standard" | "nonStandard"; bookId?: string }` and :30 `mode?: "standard" | "nonStandard";` vs src/offlineBooks.ts:16 `export type AppMode = "standard";` and PdfReaderApp.tsx:77 `const modeForBook = (_book: BookId): "standard" => "standard";`.
- **Fix:** Narrow the .d.ts mode to `"standard"` (or `string` if you want to stay tolerant of old wire values), keeping it consistent with offlineBooks.AppMode. Coordinate with the contract-test fix above (they must land together or CI goes red).
- **Verify:** tsc --noEmit still passes; grep confirms 'nonStandard' gone from src/nearbyDirectorSync.d.ts. Coordinate with e2e/nearby-sync-contract.test.mjs line 23.
- **Regression of:** 318d6f39 narrowed AppMode in offlineBooks.ts but did not update this bridge .d.ts

#### [LOW · REGRESSION] `new-refactor-handoff-doc-describes-deleted-app`
- **Where:** `HANDOFF.md:105`
- **Title:** HANDOFF.md still documents the deleted two-book + IP-geo + geo-gate architecture as current
- **Problem:** HANDOFF.md (the cold-tab kickoff doc) still describes the app as two-book, geo-gated: 'Book selection is STRICTLY geo-IP', 'standard = Manual Alvernia (371pp), hymns-4 = Himnos de Sión (51pp)', and a whole '## Blank-white geo-gate' section describing X-Hymnal / geoJustResolved / switchBook / 8s backstop — ALL deleted in 318d6f39/39b60313. A future session (or Miguel on a fresh tab) reading this will believe hymns-4/Sión, IP-geo book resolution, and the geo-gate state machine still exist, and could 'fix' the vestigial #geo-gate/bookId back into a live feature or chase geo bugs that no longer exist. It's untracked scratch, but per the next-tab handoff protocol it's the authoritative orientation doc, so stale content here actively misleads. No runtime failure.
- **Evidence:** HANDOFF.md:105 '**Book selection is STRICTLY geo-IP...**', :109 '`standard` = "Manual Alvernia" (371 pp), `hymns-4` = "Himnos de Sión" (51 pp)', :117 '## Blank-white geo-gate (the "no wrong-manual flash" feature)', :123 'Gate covers until geo resolves (`relayPollOnce` reads `X-Hymnal`)' — all describe systems deleted from app.js/index.ts/PdfReaderApp at HEAD.
- **Fix:** Rewrite or delete the stale sections of HANDOFF.md to reflect build 374's single-public-book reality (no hymns-4/Sión, no geo, #geo-gate is a vestigial loader hidden immediately, /unlock is a no-op, bookId/mode are dead wire fields), or delete HANDOFF.md if it's finished scratch.
- **Verify:** grep HANDOFF.md for 'hymns-4|Sión|geo-IP|X-Hymnal|switchBook' → zero after cleanup. Cross-check against the delta map's removed_concepts list.
- **Regression of:** 318d6f39/39b60313 removed the two-book+geo system but HANDOFF.md was not updated (it predates the refactor)


---

_Generated from workflow run `wf_01aaa1f7-7f5`. Original findings: `audit-findings-raw.md`. Plan: `app-hardening-plan.md`._
